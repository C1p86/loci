# Requirements Archive: loci v2.0 — Remote CI

**Archived:** 2026-06-01
**Milestone:** v2.0 Remote CI — Agents + Web Dashboard
**Status:** ✅ SHIPPED 2026-04-19
**Coverage:** 99 requirements → 99 mapped → 99 Complete. No orphans.

---

## Backward Compatibility (non-negotiable)

- [x] **BC-01**: `xci` senza `--agent` è osservabilmente identico a v1.0 — **Validated v2.0** (302 v1 tests green throughout all phases)
- [x] **BC-02**: La suite di test v1 (202 test) passa invariata come required check CI su ogni PR che tocchi `packages/xci/` — **Validated v2.0**
- [x] **BC-03**: Il bundle `packages/xci/dist/cli.mjs` resta sotto 200KB; `ws` e `reconnecting-websocket` sono `external[]` — **Validated v2.0** (ws-fence 3 layers active; size-limit gate deferred — see Tech Debt)
- [x] **BC-04**: Cold-start `xci --version` senza `--agent` resta sotto 300ms — **Validated v2.0**

## Packaging & Distribution (PKG)

- [x] **PKG-01**: Monorepo con pnpm workspaces; 3 package npm: `xci`, `@xci/server`, `@xci/web` — **Validated v2.0 (Phase 6)**
- [x] **PKG-02**: Build orchestrato da Turborepo — **Validated v2.0 (Phase 6)**
- [x] **PKG-03**: Versioning coordinato via `@changesets/cli` — **Validated v2.0 (Phase 6)**
- [x] **PKG-04**: Docker image `xci/server` pubblicata, basata su `node:22-slim`, multi-stage build — **Validated v2.0 (Phase 14)**
- [x] **PKG-05**: Docker image include `@xci/server` + build statico di `@xci/web` — **Validated v2.0 (Phase 14)**
- [x] **PKG-06**: Docker image gira come utente non-root, ha healthcheck HTTP, gestisce SIGTERM/SIGINT come PID 1 — **Validated v2.0 (Phase 14)**
- [x] **PKG-07**: Migrazioni Drizzle applicate con programmatic migrator al boot; `drizzle-kit` resta devDep — **Validated v2.0 (Phase 14)**
- [x] **PKG-08**: CI smoke-test dell'image pubblicata (boot, healthcheck, migrazione, signup end-to-end) prima di tag release — **Validated v2.0 (Phase 14)**

## Auth & Org Model (AUTH)

- [x] **AUTH-01**: Signup utente via email + password; hashing con `@node-rs/argon2` (Argon2id, OWASP 2024 params: m=19456/t=2/p=1) — **Validated v2.0 (Phase 7)**
- [x] **AUTH-02**: Verifica email al signup: token single-use con expiry 24h — **Validated v2.0 (Phase 7)**
- [x] **AUTH-03**: Login email+password → sessione DB-side (randomBytes(32)); cookie `httpOnly + secure + sameSite=strict` — **Validated v2.0 (Phase 7)**
- [x] **AUTH-04**: Password reset: token single-use, expiry 1h — **Validated v2.0 (Phase 7)**
- [x] **AUTH-05**: CSRF protection su tutte le route mutation — **Validated v2.0 (Phase 7)** (per-route opt-in; signup/login CSRF-exempt)
- [x] **AUTH-06**: Rate limiting su signup, login, password-reset, webhook ingress — **Validated v2.0 (Phase 7)**
- [x] **AUTH-07**: Ogni utente appartiene ad almeno un Org; Org personale creata automaticamente al signup — **Validated v2.0 (Phase 7)**
- [x] **AUTH-08**: Ruoli per Org: Owner (unique, non-removable), Member, Viewer — **Validated v2.0 (Phase 7)**
- [x] **AUTH-09**: Owner può invitare membri via email; invito ha token expiry 7g — **Validated v2.0 (Phase 7)**
- [x] **AUTH-10**: Isolation multi-tenant: repository layer enforcea filtro `org_id` su ogni query; test fixture multi-org copre tutte le repo function — **Validated v2.0 (Phase 7)**
- [x] **AUTH-11**: Email transport pluggable (nodemailer abstract transport); default SMTP configurabile via env — **Validated v2.0 (Phase 7)**
- [x] **AUTH-12**: Logout invalida la sessione DB-side; nessuna riusabilità del cookie dopo logout — **Validated v2.0 (Phase 7)**

## Agent Authentication (ATOK)

- [x] **ATOK-01**: Owner/Member può generare Registration Token dalla UI; token single-use, expiry 24h, scoped all'org — **Validated v2.0 (Phase 8)**
- [x] **ATOK-02**: `xci --agent <url> --token <registration-token>` emette Agent Credential permanente (TOFU) persista localmente — **Validated v2.0 (Phase 8)**
- [x] **ATOK-03**: Agent Credential trasmessa nel body del frame WS di handshake, mai nella URL — **Validated v2.0 (Phase 8)** (security regression test active)
- [x] **ATOK-04**: Owner/Member può revocare l'Agent Credential dalla UI; revoke immediato (chiude WS attiva) — **Validated v2.0 (Phase 8)**
- [x] **ATOK-05**: Ad ogni reconnect il server verifica la Agent Credential; se revocata, WS chiusa con reason "revoked" — **Validated v2.0 (Phase 8)**
- [x] **ATOK-06**: Tutte le comparazioni di token/HMAC usano `crypto.timingSafeEqual()`; mai `===` — **Validated v2.0 (Phase 8)**

## Agent Lifecycle (AGENT)

- [x] **AGENT-01**: Agente apre WS persistente; keepalive ping ogni 25s, pong timeout 10s — **Validated v2.0 (Phase 8)**
- [x] **AGENT-02**: Auto-reconnect con exponential backoff (min 1s, max 30s, jitter) via `reconnecting-websocket` — **Validated v2.0 (Phase 8)**
- [x] **AGENT-03**: Agente pubblica label all'handshake: `os`, `arch`, `node_version`, `hostname` + custom via `--label key=value` — **Validated v2.0 (Phase 8)**
- [x] **AGENT-04**: Hostname default = `os.hostname()`; overridable dalla UI — **Validated v2.0 (Phase 8)**
- [x] **AGENT-05**: UI mostra stato agente: online (heartbeat < 60s), offline (heartbeat stale), draining — **Validated v2.0 (Phase 8)**
- [x] **AGENT-06**: Drain mode: admin segna "draining" → server non dispatcha nuove task → task correnti completano — **Validated v2.0 (Phase 8)**
- [x] **AGENT-07**: Task-state reconciliation al reconnect: agente dichiara run_id in esecuzione; server sincronizza con DB — **Validated v2.0 (Phase 10)** (stub in Phase 8, activated in Phase 10 D-18)
- [x] **AGENT-08**: Graceful shutdown (SIGINT/SIGTERM): invia `goodbye` frame, completa run correnti, chiude WS, exit 0 — **Validated v2.0 (Phase 8)**

## Task Definitions (TASK)

- [x] **TASK-01**: Task server-side usa lo stesso YAML DSL di xci: alias + single / sequential / parallel + `${NAME}` + platform blocks — **Validated v2.0 (Phase 9)**
- [x] **TASK-02**: Parser YAML condiviso tra `xci` e `@xci/server` (xci/dsl subpath) — **Validated v2.0 (Phase 9)**
- [x] **TASK-03**: Task ha: `name`, `description`, `yaml_definition`, `label_requirements`, `org_id` — **Validated v2.0 (Phase 9)**
- [x] **TASK-04**: Task validata al salvataggio: YAML parseable, composizione ciclica rilevata, placeholder resolution al dispatch-time — **Validated v2.0 (Phase 9)**
- [x] **TASK-05**: Editor task nella UI con syntax highlighting YAML e validation error inline — **Validated v2.0 (Phase 13)**
- [x] **TASK-06**: Risoluzione `${VAR}` al dispatch-time con precedenza: param override → org-level secrets → agent-local — **Validated v2.0 (Phase 9)**

## Task Dispatch (DISP)

- [x] **DISP-01**: Task triggerata entra in dispatch queue in-memory — **Validated v2.0 (Phase 10)**
- [x] **DISP-02**: Selezione agente idoneo: online + label_requirements soddisfatte; least-busy + round-robin — **Validated v2.0 (Phase 10)**
- [x] **DISP-03**: Frame `dispatch` all'agente contiene: run_id, task definition snapshot, params risolti — **Validated v2.0 (Phase 10)**
- [x] **DISP-04**: TaskRun stati: queued → dispatched → running → succeeded/failed/cancelled/timed_out/orphaned — **Validated v2.0 (Phase 10)**
- [x] **DISP-05**: Concurrency per-agent default 1; per-org limit dal Plan — **Validated v2.0 (Phase 10)**
- [x] **DISP-06**: Timeout: default 1h, configurabile per-task; scaduto → frame `cancel` + run `timed_out` — **Validated v2.0 (Phase 10)**
- [x] **DISP-07**: Cancellazione manuale dalla UI → frame `cancel` all'agente → subprocess killed → run `cancelled` — **Validated v2.0 (Phase 10)**
- [x] **DISP-08**: Startup reconciliation: TaskRun in stato queued/dispatched senza agente → ri-queued o `orphaned` — **Validated v2.0 (Phase 10)**
- [x] **DISP-09**: "Run con param override" dalla UI senza persistere sulla task — **Validated v2.0 (Phase 10)**

## Log Streaming (LOG)

- [x] **LOG-01**: Agente stream stdout/stderr in frame `log_chunk` con sequence number — **Validated v2.0 (Phase 11)**
- [x] **LOG-02**: Server persiste chunk in Postgres; retention configurabile; default Free 30 giorni — **Validated v2.0 (Phase 11)**
- [x] **LOG-03**: UI sottoscrive WS per log live con autoscroll pausabile; indicatore connessione WS — **Validated v2.0 (Phase 11/13)**
- [x] **LOG-04**: Ogni chunk ha timestamp assoluto (origine agente); UI mostra/nasconde con toggle — **Validated v2.0 (Phase 13)**
- [x] **LOG-05**: Download log completo come file `.log` plaintext (endpoint autenticato, org-scoped) — **Validated v2.0 (Phase 11)**
- [x] **LOG-06**: Redazione pre-persist: valori di org secrets + agent-local secrets → `***`; varianti base64 coperte — **Validated v2.0 (Phase 11)**
- [x] **LOG-07**: Backpressure: subscriber UI lento non blocca stream agente; buffer drop-head — **Validated v2.0 (Phase 11)**
- [x] **LOG-08**: Retention cleanup job giornaliero cancella chunk più vecchi del retention del Plan — **Validated v2.0 (Phase 11)**

## Secrets Management (SEC)

- [x] **SEC-01**: Org-level secrets cifrati con envelope encryption: MEK dal process env cifra DEK per-org; DEK cifra il valore con AES-256-GCM — **Validated v2.0 (Phase 9)**
- [x] **SEC-02**: IV random per ogni encrypt call; unit test verifica `notDeepEqual(encrypt(k,"x").iv, encrypt(k,"x").iv)` — **Validated v2.0 (Phase 9)**
- [x] **SEC-03**: Auth tag AES-GCM validato al decrypt; fallimento → errore esplicito — **Validated v2.0 (Phase 9)**
- [x] **SEC-04**: Nessun ruolo vede i valori decifrati in UI, solo metadata; no plaintext endpoint — **Validated v2.0 (Phase 9)** (AJV additionalProperties:false + grep CI gate)
- [x] **SEC-05**: Per-agent secrets restano locali in `.xci/secrets.yml` come v1 — **Preserved v2.0**
- [x] **SEC-06**: Dispatch: server decifra org secrets → invia nel param bundle all'agente; agente merge con local secrets (agent-local wins) — **Validated v2.0 (Phase 10)**
- [x] **SEC-07**: Audit log org-scoped: create/update/rotate/delete di ogni secret (solo metadata) — **Validated v2.0 (Phase 9)**
- [x] **SEC-08**: Endpoint admin per rotate MEK senza cambio DEK — **Validated v2.0 (Phase 9)**

## Trigger Plugins (PLUG)

- [x] **PLUG-01**: Interfaccia plugin stabile a 3 metodi: `verify → parse → mapToTask` con TypeScript interface esportata — **Validated v2.0 (Phase 12)**
- [x] **PLUG-02**: Plugin bundled at build time; nessun install dinamico runtime — **Validated v2.0 (Phase 12)**
- [x] **PLUG-03**: Plugin GitHub: endpoint `/hooks/github/:orgToken`, signature HMAC-SHA256 verificata con `timingSafeEqual`; eventi `push` e `pull_request` — **Validated v2.0 (Phase 12)**
- [x] **PLUG-04**: Plugin Perforce: endpoint `/hooks/perforce/:orgToken`; xci CLI emette script Node-free (`.sh`/`.bat`/`.ps1`) — **Validated v2.0 (Phase 12)**
- [x] **PLUG-05**: Mapping event → task config esplicita per-task (no naming convention) — **Validated v2.0 (Phase 12)**
- [x] **PLUG-06**: Dead Letter Queue: eventi non processati in DLQ; UI li elenca; retry manuale — **Validated v2.0 (Phase 12)**
- [x] **PLUG-07**: Idempotency: `delivery_id` tracciato; duplicate delivery ignorato con log warning — **Validated v2.0 (Phase 12)**
- [x] **PLUG-08**: Scrubbing: request body scrubbato di token/header sensibili prima della persistenza in DLQ — **Validated v2.0 (Phase 12)**

## Dashboard UX (UI)

- [x] **UI-01**: SPA React 19 + Vite 8 + Tailwind 4 + shadcn/ui + TanStack Query + Zustand — **Validated v2.0 (Phase 13)** (Vite 6.3.4 used; plan was forward-compatible with v8)
- [x] **UI-02**: Vista "Agents": lista con stato, labels, hostname (rename inline), azione "Drain", last-seen — **Validated v2.0 (Phase 13)**
- [x] **UI-03**: Vista "Tasks": lista + editor YAML con syntax highlighting, validation inline, save — **Validated v2.0 (Phase 13)**
- [x] **UI-04**: Vista "Run": stato corrente, log live con autoscroll pausabile, download link, timestamp toggle — **Validated v2.0 (Phase 13)**
- [x] **UI-05**: Vista "History": tabella paginata con filtri (status, task, date range) — **Validated v2.0 (Phase 13)**
- [x] **UI-06**: Settings Org: members + ruoli + inviti — **Validated v2.0 (Phase 13)**
- [x] **UI-07**: Settings Plugin: config per-org per GitHub (webhook URL + HMAC secret) e Perforce — **Validated v2.0 (Phase 13)**
- [x] **UI-08**: Indicatore connessione WS sempre visibile — **Validated v2.0 (Phase 13)**
- [x] **UI-09**: Empty states first-run: istruzioni per registrare il primo agente — **Validated v2.0 (Phase 13)**
- [x] **UI-10**: Role Viewer: UI read-only state-driven (bottoni disabled con tooltip, non nascosti) — **Validated v2.0 (Phase 13)**
- [x] **UI-11**: Responsive layout: funziona da 1024px desktop in su — **Validated v2.0 (Phase 13)**

## Billing / Quota Stub (QUOTA)

- [x] **QUOTA-01**: Entity `OrgPlan` con fields: plan_name, max_agents, max_concurrent_tasks, log_retention_days — **Validated v2.0 (Phase 7)**
- [x] **QUOTA-02**: Plan "Free" di default: max_agents=5, max_concurrent_tasks=5, log_retention_days=30 — **Validated v2.0 (Phase 7)**
- [x] **QUOTA-03**: Enforcement registrazione agente: oltre max_agents → rifiutata con errore user-visible — **Validated v2.0 (Phase 10)**
- [x] **QUOTA-04**: Enforcement concurrent: dispatch oltre max_concurrent_tasks → coda standard — **Validated v2.0 (Phase 10)**
- [x] **QUOTA-05**: Retention cleanup usa log_retention_days del Plan corrente — **Validated v2.0 (Phase 11)**
- [x] **QUOTA-06**: Settings Org mostra uso corrente: agents N/5, concurrent X/5, retention Y days — **Validated v2.0 (Phase 13)**
- [x] **QUOTA-07**: Nessuna integrazione Stripe in v2.0; piano "Free" è l'unico disponibile — **Validated v2.0**

## Build-Status Badge (BADGE)

- [x] **BADGE-01**: Endpoint pubblico `/badge/:orgSlug/:taskSlug.svg` → SVG passing/failing/unknown — **Validated v2.0 (Phase 13)**
- [x] **BADGE-02**: `Cache-Control: public, max-age=30` sulla risposta — **Validated v2.0 (Phase 13)**
- [x] **BADGE-03**: Task non esistente o org privata: badge "unknown" (200 + SVG grigio), no 404 — **Validated v2.0 (Phase 13)**
- [x] **BADGE-04**: Toggle per-task "Expose badge" (default: off) — **Validated v2.0 (Phase 13)**

---

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| BC-01..04 | 06-monorepo-setup | Complete |
| PKG-01..03 | 06-monorepo-setup | Complete |
| AUTH-01..12 | 07-database-auth | Complete |
| QUOTA-01, QUOTA-02, QUOTA-07 | 07-database-auth | Complete |
| ATOK-01..06 | 08-agent-ws-protocol | Complete |
| AGENT-01..08 | 08-agent-ws-protocol | Complete |
| TASK-01..06 | 09-task-definitions-secrets | Complete |
| SEC-01..08 | 09-task-definitions-secrets | Complete |
| DISP-01..09 | 10-dispatch-quota | Complete |
| QUOTA-03..06 | 10-dispatch-quota | Complete |
| LOG-01..08 | 11-log-streaming | Complete |
| PLUG-01..08 | 12-plugin-webhooks | Complete |
| UI-01..11 | 13-web-dashboard | Complete |
| BADGE-01..04 | 13-web-dashboard | Complete |
| PKG-04..08 | 14-docker-publishing | Complete |

**v2.0 coverage:** 99 requirements → 99 mapped → 99 Complete. No orphans.

---

_Archived: 2026-06-01 | See full v2.0 roadmap: [v2.0-ROADMAP.md](v2.0-ROADMAP.md)_
