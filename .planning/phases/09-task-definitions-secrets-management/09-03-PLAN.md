---
phase: 09-task-definitions-secrets-management
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - packages/server/src/crypto/tokens.ts
  - packages/server/src/repos/tasks.ts
  - packages/server/src/repos/secrets.ts
  - packages/server/src/repos/secret-audit-log.ts
  - packages/server/src/repos/for-org.ts
  - packages/server/src/repos/index.ts
  - packages/server/src/repos/admin.ts
  - packages/server/src/repos/__tests__/tasks.isolation.test.ts
  - packages/server/src/repos/__tests__/secrets.isolation.test.ts
  - packages/server/src/repos/__tests__/secret-audit-log.isolation.test.ts
  - biome.json
autonomous: true
requirements:
  - TASK-03
  - SEC-01
  - SEC-06
  - SEC-07
  - SEC-08
  - TASK-06

must_haves:
  truths:
    - "forOrg(orgId).tasks exposes list/getById/create/update/delete scoped to that org; no cross-org leak"
    - "forOrg(orgId).secrets exposes list (metadata only)/getById (metadata)/create/update/delete/resolveByName; only resolveByName returns plaintext"
    - "forOrg(orgId).secretAuditLog exposes list (with pagination) and an append helper used by the secrets repo"
    - "secrets.create writes ciphertext + iv + tag + aad AND a secret_audit_log entry in a SINGLE db.transaction (D-22)"
    - "secrets.update re-encrypts with a NEW iv (SEC-02) and writes a corresponding audit log entry in the same transaction"
    - "secrets.delete writes a tombstone audit row (secretId NULL, secretName preserved) in the same transaction"
    - "adminRepo.getOrgDek and adminRepo.rotateMek exist, cross-org, delegate to crypto/secrets.ts"
    - "Two-org isolation tests exist for tasks, secrets, secret-audit-log repos — auto-discovery meta-test is green"
    - "biome.json overrides forbid `import 'xci'` and `import 'xci/agent'` from packages/server/src/**, and forbid `import '@xci/server'` from packages/xci/src/**"
    - "makeTasksRepo / makeSecretsRepo / makeSecretAuditLogRepo are NOT exported from repos/index.ts (D-01 discipline)"
  artifacts:
    - path: "packages/server/src/repos/tasks.ts"
      provides: "makeTasksRepo(db, orgId) — list, getById, create, update, delete"
      contains: "export function makeTasksRepo"
    - path: "packages/server/src/repos/secrets.ts"
      provides: "makeSecretsRepo(db, orgId, mek) — list (metadata), getById (metadata), create (encrypt+audit), update (re-encrypt+audit), delete (tombstone audit), resolveByName (plaintext + 'resolve' audit)"
      contains: "export function makeSecretsRepo"
    - path: "packages/server/src/repos/secret-audit-log.ts"
      provides: "makeSecretAuditLogRepo(db, orgId) — list with pagination; append helper"
      contains: "export function makeSecretAuditLogRepo"
    - path: "packages/server/src/repos/for-org.ts"
      provides: "forOrg(orgId) factory including tasks, secrets, secretAuditLog"
      contains: "secrets: makeSecretsRepo"
    - path: "packages/server/src/repos/admin.ts"
      provides: "adminRepo.getOrgDek + adminRepo.rotateMek cross-org helpers"
      contains: "async rotateMek"
    - path: "packages/server/src/repos/__tests__/tasks.isolation.test.ts"
      provides: "two-org isolation coverage for all tasks repo methods"
      min_lines: 40
    - path: "packages/server/src/repos/__tests__/secrets.isolation.test.ts"
      provides: "two-org isolation coverage including encrypt/decrypt round-trip"
      min_lines: 60
    - path: "packages/server/src/repos/__tests__/secret-audit-log.isolation.test.ts"
      provides: "two-org isolation coverage for audit log list"
      min_lines: 30
    - path: "biome.json"
      provides: "cross-package import fences (D-37 + D-38)"
      contains: "D-37"
  key_links:
    - from: "packages/server/src/repos/secrets.ts"
      to: "packages/server/src/crypto/secrets.ts"
      via: "encryptSecret/decryptSecret/getOrCreateOrgDek import"
      pattern: "from '../crypto/secrets\\.js'"
    - from: "packages/server/src/repos/for-org.ts"
      to: "makeSecretsRepo(db, orgId, mek)"
      via: "mek Buffer passed through from the call site (fastify.mek read by routes)"
      pattern: "makeSecretsRepo\\(db, orgId"
    - from: "packages/server/src/repos/admin.ts rotateMek"
      to: "db.transaction with FOR UPDATE on org_deks"
      via: "single transaction unwrap-then-rewrap per row; idempotent via mek_version guard"
      pattern: "for\\('update'\\)"
    - from: "biome.json override 4/5"
      to: "cross-package fence enforcement"
      via: "noRestrictedImports.paths with exact-string specifiers (Pitfall 6)"
      pattern: "D-37|D-38"
---

<objective>
Build the org-scoped data access layer for Phase 9: three new forOrg repos (tasks, secrets, secret-audit-log), two new adminRepo helpers for cross-org DEK/MEK operations, auto-discovered two-org isolation tests, and the cross-package Biome import fence that permits only `xci/dsl` from `@xci/server`.

Purpose: everything in Waves 4–5 (routes, dispatch resolver, rotate-mek admin endpoint) calls into this layer. The architectural invariant SEC-04 (NO plaintext returned by any API) is enforced here: `secrets.list`/`getById` return metadata only; `resolveByName` is the single plaintext-producing path and is callable only by server code (dispatch in Phase 10), never from a route handler. The D-22 audit-log atomicity (same-transaction writes) and the D-31 auto-discovery isolation test contract are both cemented in this plan.

Output:
- 3 new org-scoped repos + 3 isolation tests (two-org fixture) + auto-discovery meta-test automatically picks them up
- for-org.ts extended with 3 new factory entries + MEK plumbing
- admin.ts extended with getOrgDek + rotateMek (atomic single-transaction)
- biome.json overrides 4 and 5 (D-37 + D-38) — cross-package import fence active
- tokens.ts generateId prefix union extended with 'tsk' | 'sec' | 'sal'
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-task-definitions-secrets-management/09-CONTEXT.md
@.planning/phases/09-task-definitions-secrets-management/09-RESEARCH.md
@.planning/phases/09-task-definitions-secrets-management/09-PATTERNS.md
@.planning/phases/09-task-definitions-secrets-management/09-01-SUMMARY.md
@.planning/phases/09-task-definitions-secrets-management/09-02-SUMMARY.md

@packages/server/src/db/schema.ts
@packages/server/src/repos/for-org.ts
@packages/server/src/repos/index.ts
@packages/server/src/repos/admin.ts
@packages/server/src/repos/users.ts
@packages/server/src/repos/org-invites.ts
@packages/server/src/repos/__tests__/users.isolation.test.ts
@packages/server/src/repos/__tests__/org-invites.isolation.test.ts
@packages/server/src/repos/__tests__/isolation-coverage.isolation.test.ts
@packages/server/src/test-utils/two-org-fixture.ts
@packages/server/src/test-utils/db-harness.ts
@packages/server/src/crypto/tokens.ts
@packages/server/src/crypto/secrets.ts
@packages/server/src/errors.ts
@biome.json

<interfaces>
forOrg repo factory shape (from packages/server/src/repos/users.ts pattern):
- export function makeXxxRepo(db: PostgresJsDatabase, orgId: string) returns { list, getById, create, update, delete }
- every query includes eq(table.orgId, orgId) in its WHERE clause
- create uses generateId('prefix') + satisfies NewXxx + insert; catches PG 23505 and rethrows as domain-specific ConflictError
- export type XxxRepo = ReturnType<typeof makeXxxRepo>

Phase 9 generateId prefixes (extend the discriminated union in packages/server/src/crypto/tokens.ts):
- 'tsk' — task
- 'sec' — secret
- 'sal' — secret audit log entry

Drizzle transaction (existing — admin.ts signupTx pattern):
- await db.transaction(async (tx) => { await tx.insert(...); await tx.insert(...); });
- throw inside tx -> automatic rollback

AES envelope helpers from Plan 09-02 (packages/server/src/crypto/secrets.ts):
- encryptSecret(dek, plaintext, aad) returns { ciphertext, iv, tag }
- decryptSecret(dek, ct, iv, tag, aad) returns string (throws SecretDecryptError)
- wrapDek(mek, dek) returns { wrapped, iv, tag }
- unwrapDek(mek, wrapped, iv, tag) returns Buffer
- getOrCreateOrgDek(db, orgId, mek) returns Promise<Buffer>

secret_audit_log action enum (schema.ts): 'create' | 'update' | 'rotate' | 'delete' | 'resolve'

AAD for secrets (D-16): the string `${orgId}:${name}` — stored verbatim in secrets.aad column; passed to setAAD on both encrypt and decrypt.

Biome noRestrictedImports.paths: exact-string specifier keys (Pitfall 6 — no globs). Value is { message: string }.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tokens.ts prefix union + tasks/secrets/secret-audit-log repos + for-org wiring</name>
  <files>
    packages/server/src/crypto/tokens.ts
    packages/server/src/repos/tasks.ts
    packages/server/src/repos/secrets.ts
    packages/server/src/repos/secret-audit-log.ts
    packages/server/src/repos/for-org.ts
    packages/server/src/repos/index.ts
  </files>
  <read_first>
    - packages/server/src/crypto/tokens.ts (generateId prefix union — add 'tsk' | 'sec' | 'sal')
    - packages/server/src/repos/users.ts (lines 1-31 — factory shape, makeUsersRepo return object)
    - packages/server/src/repos/org-invites.ts (create with satisfies + 23505 catch; listPending pagination pattern; revoke conditional WHERE pattern)
    - packages/server/src/repos/for-org.ts (the current 9-factory return object — inject the 3 new ones alphabetically or at the end)
    - packages/server/src/repos/index.ts (how makeRepos composes forOrg + admin)
    - packages/server/src/repos/admin.ts signupTx (lines 48-89) — exact db.transaction pattern for same-tx audit log
    - packages/server/src/crypto/secrets.ts (Plan 09-02 artifact — function signatures)
    - packages/server/src/db/schema.ts (tasks, secrets, orgDeks, secretAuditLog + inferred types)
    - packages/server/src/errors.ts (Plan 09-02 artifacts: TaskNotFoundError, TaskNameConflictError, SecretNotFoundError, SecretNameConflictError)
    - .planning/phases/09-task-definitions-secrets-management/09-PATTERNS.md repos/tasks.ts + repos/secrets.ts + repos/secret-audit-log.ts sections
    - RESEARCH Pitfall 7 (PG 23505 -> ConflictError), Pitfall 8 (mek passed as Buffer, never re-parsed)
  </read_first>
  <behavior>
    - generateId('tsk') returns 'xci_tsk_' + base32 random suffix; same shape for 'sec', 'sal'
    - makeTasksRepo(db, 'org_a').list() returns tasks WHERE org_id='org_a' only
    - makeTasksRepo(db, 'org_a').create({name:'t', yamlDefinition:'a:\n  cmd: x', ...}) succeeds once; second call with same name throws TaskNameConflictError
    - makeSecretsRepo(db, 'org_a', mek).create({name:'PWD', value:'sekret', createdByUserId: uid}) inserts one row in secrets AND one row in secret_audit_log (action='create') inside the same transaction
    - makeSecretsRepo(...).list() returns ONLY { id, name, createdAt, updatedAt, lastUsedAt } — never ciphertext, iv, tag, aad, value
    - makeSecretsRepo(...).update(secretId, {value: 'new'}) re-encrypts with a NEW iv (different from the stored one) and writes an 'update' audit entry in the same tx
    - makeSecretsRepo(...).delete(secretId, actorUserId) deletes the secrets row AND writes an audit entry with secretId=NULL (tombstone) + secretName preserved, all in the same tx
    - makeSecretsRepo(...).resolveByName('PWD', actorUserId) returns the plaintext string AND writes a 'resolve' audit entry + updates lastUsedAt; if secret missing throws SecretNotFoundError; if decrypt fails throws SecretDecryptError
    - makeSecretAuditLogRepo(db, orgId).list({limit, offset}) returns entries scoped to orgId only, newest-first via ORDER BY created_at DESC
  </behavior>
  <action>
    Step A — packages/server/src/crypto/tokens.ts:
    - Locate the generateId prefix union (IdPrefix type or similar). Extend it to include 'tsk' | 'sec' | 'sal'.
    - Keep the existing generateId implementation unchanged — only the type union grows.

    Step B — packages/server/src/repos/tasks.ts (new file). Mirror packages/server/src/repos/users.ts structure exactly:
    - Imports: and + eq from drizzle-orm; PostgresJsDatabase type; tasks + NewTask + Task from ../db/schema.js; generateId from ../crypto/tokens.js; TaskNotFoundError + TaskNameConflictError + DatabaseError from ../errors.js.
    - export function makeTasksRepo(db: PostgresJsDatabase, orgId: string) returning:
      - list() -> SELECT id, name, description, labelRequirements, createdAt, updatedAt FROM tasks WHERE orgId = $orgId ORDER BY createdAt DESC (D-10 — list view omits yamlDefinition and createdByUserId for lean payload)
      - getById(taskId) -> SELECT * FROM tasks WHERE orgId AND id; returns row or undefined (route handler throws TaskNotFoundError)
      - create({ name, description = '', yamlDefinition, labelRequirements = [], createdByUserId }) -> generateId('tsk'); `const payload = { id, orgId, ...params } satisfies NewTask;` insert; catch PG 23505 -> TaskNameConflictError; else DatabaseError. Returns { id }.
      - update(taskId, params: Partial<{ name, description, yamlDefinition, labelRequirements }>) -> UPDATE WHERE orgId AND id SET ..., updatedAt=now(). Handles 23505 -> TaskNameConflictError.
      - delete(taskId) -> DELETE WHERE orgId AND id; returns rows-affected count (route treats 0 as TaskNotFoundError).
    - export type TasksRepo = ReturnType<typeof makeTasksRepo>;
    - Do NOT export anything else from this file.

    Step C — packages/server/src/repos/secret-audit-log.ts (new file):
    - Imports: and + desc + eq from drizzle-orm; PostgresJsDatabase type; secretAuditLog + NewSecretAuditLogEntry from ../db/schema.js; generateId from ../crypto/tokens.js.
    - export function makeSecretAuditLogRepo(db: PostgresJsDatabase, orgId: string) returning:
      - list({ limit = 100, offset = 0 }: { limit?: number; offset?: number } = {}) -> SELECT * FROM secret_audit_log WHERE orgId ORDER BY createdAt DESC LIMIT $limit OFFSET $offset. Clamp limit to max 1000 (D-23).
      - append({ secretId, secretName, action, actorUserId }) -> INSERT row with generateId('sal'). Accepts a tx arg variant: the append helper should accept an OPTIONAL `tx?: PostgresJsDatabase | TransactionShape` first arg and fall back to db. Since the secrets repo calls append INSIDE a transaction, it must pass tx; the routes repo (Plan 09-06 rotate-mek flow) calls append outside any transaction.

      Simpler: expose TWO factories from the file — (a) makeSecretAuditLogRepo(db, orgId) for read, and (b) an exported standalone helper `writeSecretAuditEntry(tx, orgId, payload)` that the secrets repo + rotation logic call inside their own transactions. Choose the shape that keeps type safety and avoids leaking transaction types through the read path.

    - export type SecretAuditLogRepo = ReturnType<typeof makeSecretAuditLogRepo>;

    Step D — packages/server/src/repos/secrets.ts (new file). Mirror org-invites.ts factory shape but embed encryption inside the repo (D-18).
    - Imports: and + desc + eq from drizzle-orm; PostgresJsDatabase; secrets + NewSecret from ../db/schema.js; generateId from ../crypto/tokens.js; encryptSecret + decryptSecret + getOrCreateOrgDek from ../crypto/secrets.js; SecretNotFoundError + SecretNameConflictError + SecretDecryptError + DatabaseError from ../errors.js; writeSecretAuditEntry from ./secret-audit-log.js (or import the factory and inline the insert — planner picks, but KEEP same-tx discipline).
    - export function makeSecretsRepo(db: PostgresJsDatabase, orgId: string, mek: Buffer) returning:
      - list() -> SELECT id, name, createdAt, updatedAt, lastUsedAt WHERE orgId. METADATA ONLY — no ciphertext, no iv, no tag, no aad.
      - getById(secretId) -> SELECT id, name, createdAt, updatedAt, lastUsedAt WHERE orgId AND id. Metadata only. Returns row or undefined.
      - create({ name, value, createdByUserId }) — wrapped in db.transaction:
        1. dek = await getOrCreateOrgDek(tx, orgId, mek)
        2. aad = `${orgId}:${name}`
        3. { ciphertext, iv, tag } = encryptSecret(dek, value, aad)
        4. id = generateId('sec')
        5. try { tx.insert(secrets).values({ id, orgId, name, ciphertext, iv, authTag: tag, aad, createdByUserId } satisfies NewSecret) } catch PG 23505 -> throw SecretNameConflictError; else DatabaseError
        6. writeSecretAuditEntry(tx, { id: generateId('sal'), orgId, secretId: id, secretName: name, action: 'create', actorUserId: createdByUserId })
        Returns { id, name }.
      - update(secretId, { value, actorUserId }) — wrapped in db.transaction:
        1. Look up existing row (orgId + id) with SELECT name for aad rebuild. If no row -> SecretNotFoundError.
        2. dek = await getOrCreateOrgDek(tx, orgId, mek)
        3. aad = `${orgId}:${existingName}` (D-19 + Pitfall 3 — name is immutable; rebuild AAD from the stored name exactly)
        4. { ciphertext, iv, tag } = encryptSecret(dek, value, aad)  — NEW iv per SEC-02
        5. UPDATE secrets SET ciphertext, iv, authTag, updatedAt=now() WHERE orgId AND id
        6. writeSecretAuditEntry(tx, { action: 'update', secretId, secretName: existingName, actorUserId })
      - delete(secretId, actorUserId) — wrapped in db.transaction:
        1. SELECT name WHERE orgId AND id. If missing -> SecretNotFoundError.
        2. DELETE FROM secrets WHERE orgId AND id
        3. writeSecretAuditEntry(tx, { action: 'delete', secretId: null, secretName: existingName, actorUserId })  // tombstone
      - resolveByName(name, actorUserId) — D-33/Phase 10 call-site; NOT called by any current route.
        1. SELECT ciphertext + iv + authTag + aad WHERE orgId AND name. If missing -> SecretNotFoundError.
        2. dek = await getOrCreateOrgDek(db, orgId, mek)  (can run outside tx — read only)
        3. plaintext = decryptSecret(dek, row.ciphertext, row.iv, row.authTag, row.aad)  (throws SecretDecryptError on tag mismatch)
        4. db.transaction(tx -> UPDATE secrets SET lastUsedAt=now() AND writeSecretAuditEntry(tx, action:'resolve'))
        5. return plaintext
        CRITICAL: this is the ONLY method in this file that returns a plaintext secret string. No route handler in Phase 9 calls this (Phase 10 dispatch is the first consumer).
    - export type SecretsRepo = ReturnType<typeof makeSecretsRepo>;
    - Do NOT log `value`, `plaintext`, `ciphertext`, `iv`, `tag`, `dek`, or `mek` anywhere in this file.

    Step E — packages/server/src/repos/for-org.ts: extend the returned factory object with three new entries. Because makeSecretsRepo needs mek, change makeForOrg's signature to accept `mek: Buffer` alongside db (or pass through via a curried factory — planner picks per PATTERNS.md Shared Patterns A "approach 2" is less invasive: read fastify.mek at the route call site and pass it into makeRepos).

    Recommended shape: change `export function makeForOrg(db: PostgresJsDatabase, mek: Buffer)` to a curried factory `(orgId) => ({ ... tasks: makeTasksRepo(db, orgId), secrets: makeSecretsRepo(db, orgId, mek), secretAuditLog: makeSecretAuditLogRepo(db, orgId), ...existing })`. Update makeRepos in repos/index.ts so callers pass `makeRepos(fastify.db, fastify.mek)` — every existing route continues to work unchanged, only adding the mek argument at the call site. This is safer than adding mek as an invisible curry.

    Step F — packages/server/src/repos/index.ts: update makeRepos signature from `(db: PostgresJsDatabase)` to `(db: PostgresJsDatabase, mek: Buffer)` and pass mek into makeForOrg. Do NOT export makeTasksRepo / makeSecretsRepo / makeSecretAuditLogRepo — only forOrg and admin (D-01 architectural spine).

    NOTE: after this change every existing route in packages/server/src/routes/** that calls `makeRepos(fastify.db)` now needs to call `makeRepos(fastify.db, fastify.mek)`. Scan all routes (routes/auth/*, routes/orgs/*, routes/agents/*) and update them. The mek decorator from Plan 09-02 guarantees fastify.mek is present at every route registration time.
  </action>
  <verify>
    <automated>cd /home/developer/projects/loci && pnpm --filter @xci/server exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - tokens.ts generateId prefix union includes 'tsk' | 'sec' | 'sal'
    - Three new repo files exist with makeTasksRepo / makeSecretsRepo / makeSecretAuditLogRepo factories
    - for-org.ts returns `{ ...existing, tasks, secrets, secretAuditLog }` when called
    - repos/index.ts: `grep -E "^export" packages/server/src/repos/index.ts` shows only forOrg + admin (NOT any make*Repo)
    - Every `makeRepos(` call in packages/server/src/routes/** passes two args (db, mek) — grep for `makeRepos\\(fastify\\.db\\)` returns 0 matches; `makeRepos\\(fastify\\.db, fastify\\.mek\\)` matches all route handlers that use makeRepos
    - `grep -c "db.transaction" packages/server/src/repos/secrets.ts` >= 3 (create, update, delete use same-tx audit writes — D-22)
    - `grep -c "ciphertext\|authTag\|aad" packages/server/src/repos/secrets.ts list()` block — zero matches in the list method's SELECT clause (metadata only)
    - `pnpm --filter @xci/server exec tsc --noEmit` exits 0
    - Server unit tests still green (pre-existing tests unaffected)
  </acceptance_criteria>
  <done>Three new forOrg repos implemented; for-org.ts and index.ts threaded with mek; all existing route call sites updated to pass mek; same-transaction audit discipline enforced in secrets.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: adminRepo extensions (getOrgDek + rotateMek) + 3 isolation tests + Biome fence overrides</name>
  <files>
    packages/server/src/repos/admin.ts
    packages/server/src/repos/__tests__/tasks.isolation.test.ts
    packages/server/src/repos/__tests__/secrets.isolation.test.ts
    packages/server/src/repos/__tests__/secret-audit-log.isolation.test.ts
    biome.json
  </files>
  <read_first>
    - packages/server/src/repos/admin.ts (signupTx lines 48-89 is the atomic-transaction analog; findValidRegistrationToken is the cross-org read analog)
    - packages/server/src/repos/__tests__/users.isolation.test.ts (EXACT shape the new isolation tests must mirror)
    - packages/server/src/repos/__tests__/org-invites.isolation.test.ts (inline-seed pattern — do NOT extend seedTwoOrgs signature)
    - packages/server/src/repos/__tests__/isolation-coverage.isolation.test.ts (the auto-discovery meta-test — confirms it picks up new repos automatically)
    - packages/server/src/test-utils/two-org-fixture.ts (seedTwoOrgs returns orgA + orgB + their owner users)
    - packages/server/src/test-utils/db-harness.ts (getTestDb + resetDb)
    - packages/server/src/crypto/secrets.ts (unwrapDek + wrapDek — used by rotateMek)
    - packages/server/src/db/schema.ts (orgDeks table + the mekVersion column for idempotency)
    - .planning/phases/09-task-definitions-secrets-management/09-RESEARCH.md FA-6 (rotateMek FOR UPDATE + idempotency via mek_version)
    - .planning/phases/09-task-definitions-secrets-management/09-PATTERNS.md repos/admin.ts + biome.json sections
    - Pitfall 6 (biome paths are exact strings, not globs)
  </read_first>
  <behavior>
    - adminRepo.getOrgDek('org_a', mek) returns the 32-byte DEK buffer for org_a (creates it if absent — delegates to getOrCreateOrgDek)
    - adminRepo.rotateMek(oldMek, newMek): with N>=1 org_dek rows, all are re-wrapped under newMek in a single transaction; each row's mek_version increments; returns { rotated: N, mekVersion: previousVersion+1 }
    - rotateMek is idempotent: if a row already has mek_version >= newVersion it is skipped (D-28)
    - After rotation, decrypting every secret with the NEW mek yields the same plaintext as before rotation (D-26 — verified in Plan 09-06 integration test; this plan only needs the repo code to compile and be called with the right FOR UPDATE shape)
    - Two-org isolation meta-test (isolation-coverage.isolation.test.ts) auto-discovers tasks / secrets / secret-audit-log repo files and requires matching *.isolation.test.ts peers — this plan adds the peers so the meta-test stays green
    - biome.json override 4 fires on `import 'xci'` or `import 'xci/agent'` inside packages/server/src/** but NOT on `import 'xci/dsl'`
    - biome.json override 5 fires on `import '@xci/server'` inside packages/xci/src/**
  </behavior>
  <action>
    Step A — packages/server/src/repos/admin.ts: add 2 methods to the adminRepo return object.

    getOrgDek(orgId: string, mek: Buffer): Promise<Buffer>
    - Delegates to getOrCreateOrgDek from ../crypto/secrets.js — just a thin wrapper that makes the cross-org intent visible at the adminRepo surface for Phase 10 dispatch and this plan's rotation flow. (D-30)

    rotateMek(oldMek: Buffer, newMek: Buffer): Promise<{ rotated: number; mekVersion: number }>
    Wrap in a single `db.transaction(async (tx) => {...})` (D-25 atomic across all org_deks rows). Inside:
    1. rows = await tx.select().from(orgDeks).for('update')    // Drizzle SELECT FOR UPDATE; if `.for('update')` is not available in drizzle-orm 0.45.2, fall back to `sql\`FOR UPDATE\`` via db.execute (RESEARCH assumption A3)
    2. firstVersion = rows[0]?.mekVersion ?? 0
    3. newVersion = firstVersion + 1
    4. for each row:
       - If row.mekVersion >= newVersion: continue  (D-28 idempotent skip)
       - dek = unwrapDek(oldMek, row.wrappedDek, row.wrapIv, row.wrapTag)
       - { wrapped, iv, tag } = wrapDek(newMek, dek)
       - UPDATE orgDeks SET wrappedDek, wrapIv, wrapTag, mekVersion=newVersion, updatedAt=sql`now()` WHERE orgId=row.orgId
       - rotated++
    5. Outside the transaction: return { rotated, mekVersion: newVersion }
    Catch any throw from the transaction and wrap in `throw new MekRotationError('rotateMek transaction failed', err)`. Do NOT include the DEK or key bytes in the error message.

    Do NOT add any other method to adminRepo in this plan (rotate-mek route + platform-admin middleware land in Plan 09-06).

    Step B — Create packages/server/src/repos/__tests__/tasks.isolation.test.ts (mirror users.isolation.test.ts shape exactly):
    - Imports: vitest (beforeEach, describe, expect, it); getTestDb + resetDb from ../../test-utils/db-harness.js; seedTwoOrgs from ../../test-utils/two-org-fixture.js; makeTasksRepo from ../tasks.js.
    - describe('tasks repo isolation (D-04)', () => { beforeEach(async () => resetDb()); ... })
    - One `it` per exported factory method:
      - list scoped to orgA never returns orgB task
      - getById(otherOrgTaskId) in orgA returns undefined (does NOT leak row)
      - create in orgA with same name as an existing orgB task SUCCEEDS (names unique per-org, not globally)
      - update with a different org's task id does NOT modify that row (rows-affected = 0)
      - delete with a different org's task id does NOT delete that row
    - Fixture: inside each test seed orgA / orgB tasks via makeTasksRepo directly — mirror org-invites.isolation.test.ts inline pattern. Do NOT extend seedTwoOrgs.

    Step C — Create packages/server/src/repos/__tests__/secrets.isolation.test.ts (similar shape, but also verify encrypt/decrypt doesn't leak across orgs):
    - Generate a test mek via randomBytes(32) in a beforeAll block.
    - Create orgA secret PWD='secretA' via makeSecretsRepo(db, orgA.id, mek).create(...)
    - Create orgB secret PWD='secretB' via makeSecretsRepo(db, orgB.id, mek).create(...)
    - Verify: makeSecretsRepo(db, orgA.id, mek).list() returns only orgA's secret; same for orgB
    - Verify: makeSecretsRepo(db, orgA.id, mek).resolveByName('PWD', orgA.ownerUser.id) returns 'secretA' (NOT 'secretB')
    - Verify: cross-org ciphertext attack: TRY to call makeSecretsRepo(db, orgA.id, mek).getById(orgB.secretId) — returns undefined (repo is org-scoped)
    - Verify: audit log entries are created for create/update/delete in orgA, and orgB's audit log never includes orgA entries
    - Include a test: after `update`, the stored iv differs from the iv stored pre-update (SEC-02 per-op)
    - Mirror users.isolation.test.ts for imports + structure

    Step D — Create packages/server/src/repos/__tests__/secret-audit-log.isolation.test.ts:
    - Seed orgA + orgB; create a few audit entries via the secrets repo in each org
    - Verify makeSecretAuditLogRepo(db, orgA.id).list({}) returns only orgA entries, newest-first
    - Verify pagination: list({limit:2, offset:0}) vs list({limit:2, offset:2}) returns disjoint sets
    - Verify limit clamping: list({limit: 10000}) is clamped to 1000 (D-23)

    Step E — biome.json: APPEND two new override blocks (alongside the existing Phase 6 + Phase 7 overrides). Do NOT edit the existing overrides.

    Override 4 (D-37 — server imports only xci/dsl):
    - includes: ["packages/server/src/**/*.ts"]
    - noRestrictedImports.paths:
      - "xci": { "message": "D-37: @xci/server must import from 'xci/dsl' only, never the CLI entry point." }
      - "xci/agent": { "message": "D-37: @xci/server must not import from the agent subpath. Only 'xci/dsl' is permitted." }
      Do NOT list 'xci/dsl' — it is intentionally ALLOWED.

    Override 5 (D-38 — xci must not import from @xci/server):
    - includes: ["packages/xci/src/**/*.ts"]
    - noRestrictedImports.paths:
      - "@xci/server": { "message": "D-38: packages/xci must never import from @xci/server. Reverse dependency is forbidden." }

    Pitfall 6 reminder: these are exact-string keys, not globs. That is why 'xci' and 'xci/agent' must be listed individually.
  </action>
  <verify>
    <automated>cd /home/developer/projects/loci && pnpm --filter @xci/server exec tsc --noEmit && pnpm biome check biome.json packages/server/src/repos/admin.ts packages/server/src/repos/__tests__/tasks.isolation.test.ts packages/server/src/repos/__tests__/secrets.isolation.test.ts packages/server/src/repos/__tests__/secret-audit-log.isolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - admin.ts exports getOrgDek and rotateMek methods on adminRepo
    - `grep -c "for('update')\\|FOR UPDATE" packages/server/src/repos/admin.ts` >= 1 (rotateMek uses SELECT FOR UPDATE)
    - `grep -q "mekVersion >= newVersion" packages/server/src/repos/admin.ts` (idempotency guard — D-28)
    - 3 new *.isolation.test.ts files exist
    - biome.json has at least 4 entries in the overrides array (2 pre-existing + 2 new)
    - biome.json new overrides include exact strings "xci", "xci/agent" (server) and "@xci/server" (xci)
    - `grep -q "D-37" biome.json` AND `grep -q "D-38" biome.json`
    - `pnpm biome check packages/server/src packages/xci/src` exits 0 (no pre-existing code violates the new rules; if any does, that is a planning surprise — report and stop)
    - Tests compile (tsc --noEmit exits 0); actual test EXECUTION happens on the integration config which requires testcontainers (verified in 09-06 closeout)
  </acceptance_criteria>
  <done>adminRepo has getOrgDek + rotateMek; three new isolation tests wired; Biome cross-package fence (D-37 + D-38) live. Auto-discovery meta-test remains green because the new files match the *.isolation.test.ts naming convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route handler -> forOrg(orgId).secrets | The route supplies orgId (from session); the repo is the last line of defence before the DB. No route may reach makeSecretsRepo directly. |
| secrets.resolveByName plaintext -> downstream caller | This method is the SOLE plaintext-producing path. Its callers (Phase 10 dispatch) accept accountability for log discipline. |
| adminRepo.rotateMek -> all org_deks rows | Single cross-org operation; a bug here silently bricks every org's secrets. FOR UPDATE + single transaction + mek_version guard are mandatory. |
| biome.json lint fence -> compile-time import check | Biome runs in CI pre-merge (unit+lint job). A violation prevents merge; the fence is pre-emptive, not runtime-enforced. |
| Test seed data -> testcontainers DB | Two-org fixture + resetDb TRUNCATE + single-suite isolation (Phase 7 D-22). Tests must not leak orgA/orgB fixtures across it() blocks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Elevation of Privilege | Route bypasses forOrg via direct repo import | mitigate | repos/index.ts does NOT export makeTasksRepo / makeSecretsRepo / makeSecretAuditLogRepo; existing D-01 Biome override (biome.json lines 70-110) already blocks direct file imports across the full repo path — new repos MUST also be listed there (update in this plan) |
| T-09-03-02 | Information Disclosure | secrets.list returns ciphertext or value | mitigate | list method's SELECT clause includes ONLY id/name/createdAt/updatedAt/lastUsedAt; getById same discipline; CI-verifiable via grep on the file |
| T-09-03-03 | Information Disclosure | Log statement inside repo serialises an encrypted row | mitigate | No log calls in secrets.ts; decryption errors throw SecretDecryptError (zero-arg ctor); Pino redact paths (Plan 09-02) strip *.ciphertext / *.dek / *.mek from any accidental log emission |
| T-09-03-04 | Tampering | Cross-org row move via DB tampering | mitigate | AAD = `${orgId}:${name}` stored + reconstructed in resolveByName from orgId (derived from forOrg closure) + row.name; a moved row's stored aad mismatches the reconstructed aad, causing tag failure on decrypt (RESEARCH §FA-3) |
| T-09-03-05 | Tampering | Audit log write skipped for delete action | mitigate | secrets.delete wraps DELETE + writeSecretAuditEntry in a single db.transaction — tx throw rolls back the delete if audit write fails (D-22) |
| T-09-03-06 | Tampering | rotateMek partial commit window | mitigate | Single db.transaction over all rows + FOR UPDATE lock + mek_version idempotency guard ensures either fully-rotated or fully-rolled-back DB state (RESEARCH §FA-6) |
| T-09-03-07 | Denial of Service | rotateMek blocks concurrent secret reads | accept | SELECT FOR UPDATE holds row locks for the transaction duration. For v2.0's low-volume rotation (manual, via runbook per D-27), brief contention is acceptable — rotation is expected to take seconds, not minutes |
| T-09-03-08 | Information Disclosure | Cross-package import leaks xci CLI internals | mitigate | biome.json overrides 4+5 (D-37 + D-38) enforce one-way dependency: server imports only xci/dsl; xci never imports @xci/server |
| T-09-03-09 | Tampering | Levenshtein suggestion leaks alias names across orgs | mitigate | validateAliasRefs operates on a single CommandMap parsed from ONE task's yamlDefinition — scope is per-save-request, no cross-org alias pool |
</threat_model>

<verification>
Cumulative verification after both tasks:

```bash
cd /home/developer/projects/loci
pnpm --filter @xci/server exec tsc --noEmit
pnpm biome check packages/server/src packages/xci/src biome.json

# Unit suite (no DB)
pnpm --filter @xci/server test:unit

# Integration suite (Linux only — gated by testcontainers availability)
pnpm --filter @xci/server test:integration
# Expect: tasks.isolation / secrets.isolation / secret-audit-log.isolation all pass
# Expect: isolation-coverage meta-test still green

# Fence verification (synthetic import check)
grep -rn "from 'xci'" packages/server/src 2>&1 | grep -v "from 'xci/dsl'"  # should be empty
grep -rn "from '@xci/server'" packages/xci/src 2>&1                        # should be empty

# D-01 spine check
grep -E "^export" packages/server/src/repos/index.ts  # only forOrg + admin (no make*Repo)
```
</verification>

<success_criteria>
- 3 new org-scoped repos work in two-org fixture (isolation tests green)
- Auto-discovery meta-test picks up tasks/secrets/secret-audit-log (D-31)
- adminRepo has getOrgDek + rotateMek with atomic single-transaction + idempotency guard
- biome.json overrides 4 + 5 enforce D-37 + D-38; pre-existing code passes
- for-org.ts + repos/index.ts + all routes threaded with mek without behavior regressions
- secrets.ts list/getById return METADATA ONLY; resolveByName is the sole plaintext path (called only by Phase 10 dispatch, not by any Phase 9 route)
- Every mutating secrets repo method writes an audit log entry in the same db.transaction (D-22)
- No plaintext / ciphertext / DEK / MEK appears in any log statement
- All existing server tests still green
</success_criteria>

<output>
After completion, create `.planning/phases/09-task-definitions-secrets-management/09-03-SUMMARY.md` covering:
- New repo files + factory method lists
- adminRepo method signatures added (getOrgDek, rotateMek)
- Isolation test names per repo (auto-discovery confirmation)
- biome.json override diff
- All call sites of makeRepos updated to pass mek
- D-01 spine verification (no repos exported from index.ts except forOrg + admin)
- Any deviation from planner guidance (e.g. if .for('update') was not available and raw sql was used instead — note it)
</output>
