---
phase: 08-agent-registration-websocket-protocol
plan: 05
type: execute
wave: 5
depends_on: [08-04]
files_modified:
  - packages/xci/README.md
  - packages/server/README.md
  - .planning/STATE.md
autonomous: false
requirements:
  - ATOK-01
  - ATOK-02
  - ATOK-03
  - ATOK-04
  - ATOK-05
  - ATOK-06
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - AGENT-04
  - AGENT-05
  - AGENT-06
  - AGENT-07
  - AGENT-08
must_haves:
  truths:
    - "packages/xci/README.md has a new Agent Mode section documenting --agent/--token/--label/--hostname/--config-dir flags with OS-specific credential paths"
    - "packages/server/README.md has a new Agents section documenting /ws/agent endpoint + 5 REST routes + close-code table"
    - ".planning/STATE.md updated with Phase 8 decisions list + pending todos + phase marked complete"
    - "Traceability matrix in SUMMARY maps all 14 Phase 8 requirements (ATOK-01..06, AGENT-01..08) to test files"
    - "All 5 ROADMAP success criteria verified by tests (checklist in SUMMARY)"
    - "302 v1 xci tests still green; cold-start <300ms still holds; all Phase 7 integration tests still green"
    - "Human-verify checkpoint confirms autonomous work on a staging-like flow before merge"
  artifacts:
    - path: "packages/xci/README.md"
      provides: "Agent Mode section for users"
      contains: "xci --agent"
    - path: "packages/server/README.md"
      provides: "Agents section for server operators"
      contains: "/ws/agent"
    - path: ".planning/STATE.md"
      provides: "Phase 8 closure + new pending todos + v2.0 progress update"
      contains: "Phase 08"
  key_links:
    - from: "docs (READMEs)"
      to: "the implemented behaviors from Plans 01-04"
      via: "traceability matrix SUMMARY cross-references requirement IDs to test files"
      pattern: "ATOK-"
---

<objective>
Phase 8 closeout: documentation, STATE.md finalization, traceability matrix, and human-verify checkpoint after full autonomous execution of Plans 01-04.

Purpose: Plans 01-04 deliver the complete feature; this plan closes the loop with user-facing docs and meta-artifacts that future phases (especially Phase 10 task dispatch + Phase 13 UI) will depend on.

Output: 3 files updated + a `08-CLOSEOUT-SUMMARY.md` that functions as the canonical reference for Phase 8 → 10 handoff + human-verify checkpoint gate before merge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-agent-registration-websocket-protocol/08-CONTEXT.md
@.planning/phases/08-agent-registration-websocket-protocol/08-01-SUMMARY.md
@.planning/phases/08-agent-registration-websocket-protocol/08-02-SUMMARY.md
@.planning/phases/08-agent-registration-websocket-protocol/08-03-SUMMARY.md
@.planning/phases/08-agent-registration-websocket-protocol/08-04-SUMMARY.md
@.planning/REQUIREMENTS.md
@packages/xci/README.md
@packages/server/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Documentation — xci README Agent Mode section + server README Agents section</name>
  <files>packages/xci/README.md, packages/server/README.md</files>
  <read_first>
    - packages/xci/README.md (existing structure — insert new "Agent Mode" section after the Commands/Config docs, before the Contributing section)
    - packages/server/README.md (existing — insert Agents subsection under an Endpoints or Routes header; create one if missing)
    - .planning/phases/08-agent-registration-websocket-protocol/08-CONTEXT.md (Decisions section — source of truth for flag names, OS paths, close codes)
    - .planning/phases/08-agent-registration-websocket-protocol/08-RESEARCH.md §Close Codes + §env-paths (for the OS path table)
    - Plans 01-04 SUMMARYs (final implementation details to reference)
  </read_first>
  <action>
**Sub-step A — packages/xci/README.md — add "Agent Mode" section:**

Insert a new section AFTER the existing alias/commands documentation and BEFORE any Contributing/Development section. Content:

```markdown
## Agent Mode (xci --agent)

Run `xci` as a daemon connected to an xci server, awaiting task dispatches over WebSocket.

### Quick Start

```bash
# First-run registration — requires one-time token from server admin
xci --agent wss://xci.example.com/ws/agent --token xci_reg_xxxxxxxx

# Subsequent runs — credential was persisted during registration
xci --agent wss://xci.example.com/ws/agent
```

### Flags

| Flag | Required | Description |
|------|----------|-------------|
| `--agent <url>` | yes | Server WS URL (e.g. `wss://xci.example.com/ws/agent`). Token MUST NOT appear in the URL. |
| `--token <reg-token>` | first-run only | Registration token issued by an org Owner/Member. Single-use, 24h TTL. |
| `--label key=value` | no | Custom labels; repeatable. Merged with auto-detected `os`, `arch`, `node_version`, `hostname`. |
| `--hostname <name>` | no | Override auto-detected hostname (local label only; server-side hostname is set via UI/API). |
| `--config-dir <path>` | no | Override credential storage directory. |

### Credential Storage (per-machine, not per-project)

The agent persists its permanent credential at:

| OS | Default Path |
|----|--------------|
| Linux | `~/.config/xci/agent.json` (or `$XDG_CONFIG_HOME/xci/agent.json`) |
| macOS | `~/Library/Preferences/xci/agent.json` |
| Windows | `%APPDATA%\xci\Config\agent.json` |

File mode is `0600` on POSIX. NEVER commit this file — it grants full agent access.

### TOFU Rule

If `agent.json` exists AND `--token` is also passed, `xci` exits with an error to prevent overwriting a registered credential. To re-register, first delete `agent.json` (or revoke the agent server-side).

### Reconnection

On network loss, the agent reconnects with exponential backoff (1s initial jittered, 30s cap, 1.5x growth). No manual intervention required.

### Graceful Shutdown

`SIGINT` (Ctrl+C) or `SIGTERM`: agent sends `goodbye` frame, closes the socket cleanly, exits 0. Any in-flight tasks (Phase 10+) complete to natural end before exit.

### Terminal Exit Conditions

The agent exits non-zero and STOPS reconnecting when the server closes with:
- `4001` — credential revoked
- `4002` — registration token invalid or expired
- `4004` — superseded (another agent connected with the same ID)

Other close codes (`4003` heartbeat timeout, `4005` handshake timeout, `1001` server going away) trigger normal reconnect flow.
```

**Sub-step B — packages/server/README.md — add "Agents" subsection:**

Insert after existing REST documentation. Content:

```markdown
## Agents

The server accepts persistent WebSocket connections from `xci --agent` daemons and exposes 5 REST endpoints for agent lifecycle management.

### WebSocket Endpoint: `GET /ws/agent`

Unauthenticated HTTP upgrade. Authentication happens via the **first frame** (never in URL — ATOK-03).

#### First-Frame Handshake

- Agent MUST send a `register` or `reconnect` frame within 5 seconds of WS open, else server closes with code 4005 (handshake_timeout).
- `{type: 'register', token, labels}` — single-use 24h registration token → server returns `{type: 'register_ack', agent_id, credential}`; credential is plaintext returned ONCE, stored as sha256 hash at rest.
- `{type: 'reconnect', credential, running_runs: []}` — permanent credential from previous registration → server returns `{type: 'reconnect_ack', reconciliation: []}` (Phase 10 populates reconciliation).

#### Heartbeat (D-16)

Server sends WS `ping` every 25s; agent's `ws` library auto-replies with `pong`. Missing pong within 10s → close 4003 (heartbeat_timeout). `last_seen_at` column updated on every pong + every incoming frame.

#### Close Codes

| Code | Meaning |
|------|---------|
| 1000 | Normal closure (agent goodbye) |
| 1001 | Server shutting down |
| 4001 | Credential revoked (terminal) |
| 4002 | Token invalid or frame invalid (terminal) |
| 4003 | Heartbeat timeout |
| 4004 | Superseded (another agent with same id connected) |
| 4005 | Handshake timeout (first frame not received in 5s) |

### REST Endpoints

All routes are session-authenticated (Phase 7 session cookie). Mutating routes require CSRF token.

| Method | Path | Role | CSRF | Description |
|--------|------|------|------|-------------|
| `POST` | `/api/orgs/:orgId/agent-tokens` | Owner, Member | yes | Create a single-use 24h registration token. Rate-limited 10/h per org+user. Returns `{token, expiresAt}` — plaintext shown ONCE. |
| `GET` | `/api/orgs/:orgId/agents` | any org member | no | List agents with computed state `online|offline|draining`. State is derived from `last_seen_at` (< 60s → online) unless admin-set to `draining`. |
| `PATCH` | `/api/orgs/:orgId/agents/:agentId` | Owner, Member | yes | Update `hostname` or `state` (toggle `draining`/`online`). Drain change propagates as `{type:'state', state:'draining'}` frame to connected agent (AGENT-06). |
| `POST` | `/api/orgs/:orgId/agents/:agentId/revoke` | Owner, Member | yes | Mark credential `revoked_at = now()` AND force-close connected WS with code 4001 (ATOK-04). |
| `DELETE` | `/api/orgs/:orgId/agents/:agentId` | Owner only | yes | Hard delete (CASCADE removes credentials). |

### Operational Notes

- **Single-instance only** — the in-memory `agentRegistry: Map<agentId, WebSocket>` is per-process. Horizontal scaling requires a Redis pub/sub layer (deferred to post-v2.0).
- **TLS is the operator's responsibility** — use a reverse proxy (nginx, Caddy) or cloud LB to terminate TLS before Fastify.
- **Bundle size**: `@fastify/websocket` adds ~30KB to the server bundle; `ws` is a transitive dep.
```

**Verification:** both READMEs parse as valid Markdown; links render; tables display correctly in GitHub/IDE preview.
  </action>
  <verify>
    <automated>test -f packages/xci/README.md && test -f packages/server/README.md && grep -q 'Agent Mode' packages/xci/README.md && grep -q '/ws/agent' packages/server/README.md && grep -q 'env-paths\|~/.config/xci\|Library/Preferences' packages/xci/README.md && grep -q '4001' packages/server/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Agent Mode' packages/xci/README.md` ≥ 1
    - `grep -c 'xci --agent' packages/xci/README.md` ≥ 2 (quick start + flags table)
    - `grep -c 'Library/Preferences' packages/xci/README.md` = 1 (macOS path mentioned)
    - `grep -c 'TOFU' packages/xci/README.md` ≥ 1
    - `grep -c '/ws/agent' packages/server/README.md` ≥ 1
    - `grep -c '4001\|4002\|4003\|4004\|4005' packages/server/README.md` ≥ 4 (close-code table)
    - `grep -c 'agent-tokens' packages/server/README.md` ≥ 1
    - `grep -c 'CSRF' packages/server/README.md` ≥ 1
  </acceptance_criteria>
  <done>User-facing docs updated: xci README has an Agent Mode section; server README has a Agents section with /ws/agent and the 5 REST endpoints table.</done>
</task>

<task type="auto">
  <name>Task 2: STATE.md update + Phase 8 CLOSEOUT SUMMARY with traceability matrix</name>
  <files>.planning/STATE.md, .planning/phases/08-agent-registration-websocket-protocol/08-CLOSEOUT-SUMMARY.md</files>
  <read_first>
    - .planning/STATE.md (full — especially Current Position, Decisions, Pending Todos, Performance Metrics sections)
    - .planning/phases/08-agent-registration-websocket-protocol/08-01-SUMMARY.md through 08-04-SUMMARY.md (decisions to lift into STATE.md)
    - .planning/REQUIREMENTS.md (to reference exact requirement text)
    - .planning/phases/07-database-schema-auth/07-09-SUMMARY.md (analog for closeout SUMMARY shape)
  </read_first>
  <action>
**Sub-step A — Update .planning/STATE.md:**

Update frontmatter:
```yaml
milestone: v2.0
milestone_name: — Remote CI
status: Phase 08 closed YYYY-MM-DD; ready for Phase 09
stopped_at: Phase 08 complete
last_updated: "YYYY-MM-DDTHH:MM:SS.000Z"
last_activity: YYYY-MM-DD -- Phase 08 complete (ATOK-01..06, AGENT-01..08 all green)
progress:
  total_phases: 14
  completed_phases: 8
  total_plans: 33
  completed_plans: 33
  percent: 100   # for current phase
```

Update Current Position:
```
Phase: 08 (Agent Registration & WebSocket Protocol) — COMPLETE ✓
Plan: 5 of 5 (all plans merged to main, verification passed)
Status: Phase 08 closed YYYY-MM-DD; ready for Phase 09
Last activity: YYYY-MM-DD -- Phase 08 complete (ATOK-01..06, AGENT-01..08 all green)

Progress (Phase 08): [██████████] 100%
Progress (v2.0 milestone): [███░░░░░░░] 33% (3/9 phases)
```

Update Performance Metrics table — add Phase 08 row + plan-by-plan entries.

Append new Decisions under "Accumulated Context > Decisions" (batch all 43 Phase 8 D-xx decisions + any emergent learnings from the execution; each prefixed `[Phase 08]:` for traceability).

Key batch to add (paraphrasing CONTEXT.md locked decisions):
```
- [Phase 08]: Phase 6 ws-fence formally reversed in Plan 01 — CI grep gate removed, Biome narrowed to packages/xci/src/cli.ts, new agent module is legitimate user of ws + reconnecting-websocket
- [Phase 08]: Agent mode lazy-loaded via argv pre-scan + dynamic import('./agent/index.js'); tsup multi-entry produces dist/cli.mjs (no ws) + dist/agent.mjs (ws external)
- [Phase 08]: Cold-start gate <300ms preserved; new packages/xci/src/__tests__/cold-start.test.ts as unit-level guard alongside hyperfine CI gate
- [Phase 08]: Frame envelope hand-rolled in ws/frames.ts (no zod dep added); server frames.ts validates register/reconnect/goodbye only — dispatch/log_chunk/result reserved for Phase 10/11
- [Phase 08]: Open-then-handshake auth pattern (D-14) — WS upgrade is unauth, first frame within 5s carries token or credential; 4005 close on timeout
- [Phase 08]: crypto/tokens.ts centralized compareToken() with timingSafeEqual + length pre-check (Pitfall 3); ATOK-06 enforced by absence of `===` on token/credential variables (grep-verified)
- [Phase 08]: Credential storage uses env-paths(xci, {suffix:''}).config per OS — macOS picks ~/Library/Preferences/xci NOT ~/.config (RESEARCH Pitfall 5); --config-dir overrides
- [Phase 08]: Heartbeat server-driven at 25s interval + 10s pong timeout; last_seen_at updated on every pong + every incoming frame; D-12 state computed read-side (online = last_seen_at < 60s)
- [Phase 08]: agentRegistry Map<agentId, WebSocket> decorated BEFORE @fastify/websocket register (Pitfall 8); superseding pattern closes prior connection with code 4004
- [Phase 08]: 3 new org-scoped repos (agents, agent-credentials, registration-tokens) follow Phase 7 D-01 forOrg discipline; 3 new isolation tests; D-04 auto-discovery meta-test auto-picks up
- [Phase 08]: adminRepo gained 5 D-37 cross-org helpers (findValidRegistrationToken, consumeRegistrationToken, findActiveAgentCredential, registerNewAgent, issueAgentCredential); all token-comparing paths use hashToken + SQL eq or timingSafeEqual
- [Phase 08]: Single Drizzle migration 0001_*_agents_websocket.sql committed; partial unique index ON agent_credentials WHERE revoked_at IS NULL enforces at-most-one active cred per agent
- [Phase 08]: reconnecting-websocket on Node.js REQUIRES { WebSocket: WS } option (Pitfall 2); backoff 1.0-1.5s jittered → 30s cap with 1.5x growth
- [Phase 08]: All 5 REST routes use Phase 7 requireAuth + per-route CSRF (Owner/Member except DELETE which is Owner-only); token issue rate-limited 10/h per org+user (D-40)
- [Phase 08]: WS URL-token-rejection test (ATOK-03) explicit security regression guard — token in URL is IGNORED, server still requires first-frame handshake
- [Phase 08]: Graceful shutdown: SIGINT/SIGTERM → goodbye frame → 500ms flush → rws.close() → process.exit(0) (AGENT-08)
- [Phase 08]: E2E test (Linux-only, D-33) spawns real xci --agent process against real Fastify server via execa/spawn; credential file written + SIGTERM exit 0 verified
- [Phase 08]: Phase 7 server Biome ws-restriction (lines 70-91 of pre-Phase-8 biome.json) REMOVED — @fastify/websocket legitimately imports ws; third-override paths map EXTENDED with 3 new repo file entries
- [Phase 08]: 302 v1 xci tests still green (BC-02); hyperfine cold-start gate still green (BC-04); dist/cli.mjs contains zero ReconnectingWebSocket strings (BC-03 spirit preserved even without CI grep gate)
```

Append new Pending Todos:
```
- Future (Phase 10): populate reconnect_ack reconciliation[] with real task run state (currently returns [] stub per D-18)
- Future (Phase 10): implement quota enforcement at registration time (max_agents=5 Free plan) — QUOTA-03 assigned here but deferred to Phase 10 per roadmap
- Future (post-v2.0): multi-instance scaling via Redis pub/sub for agentRegistry (currently in-memory single-process only)
- Future (post-v2.0): agent audit log (register/revoke events) — paired with Phase 7 audit log deferral
- Future (Phase 11): WS log_chunk frame type (reserved in D-15); backpressure handling revisit at that time
```

**Sub-step B — Create .planning/phases/08-agent-registration-websocket-protocol/08-CLOSEOUT-SUMMARY.md:**

```markdown
# Phase 8 Closeout Summary

**Status:** Complete
**Duration:** (fill in actual)
**Commits:** (fill in — one per plan + incremental)

## Requirements Coverage

### ATOK-01..06 (Agent Token Authentication)

| ID | Requirement | Test File(s) | Status |
|----|-------------|--------------|--------|
| ATOK-01 | Owner/Member creates single-use 24h registration token | `rest-tokens.integration.test.ts`, `registration-tokens.isolation.test.ts` | ✓ |
| ATOK-02 | `xci --agent <url> --token <T>` handshake → permanent credential | `ws-handshake.integration.test.ts::register case`, `agent-e2e.integration.test.ts` | ✓ |
| ATOK-03 | Token in WS frame body, never URL | `ws-url-token-rejected.integration.test.ts` | ✓ |
| ATOK-04 | Revoke credential → WS closes immediately | `rest-revoke.integration.test.ts`, `ws-handshake.integration.test.ts::revoked reconnect` | ✓ |
| ATOK-05 | Server verifies credential on every reconnect | `ws-handshake.integration.test.ts::reconnect with revoked`, `admin-agent.integration.test.ts::findActiveAgentCredential after revoke` | ✓ |
| ATOK-06 | All token comparisons via `crypto.timingSafeEqual` | `crypto/__tests__/tokens.test.ts`, grep audit on `===` patterns | ✓ |

### AGENT-01..08 (Agent Lifecycle)

| ID | Requirement | Test File(s) | Status |
|----|-------------|--------------|--------|
| AGENT-01 | Persistent WS + 25s ping / 10s pong timeout | `ws-heartbeat.integration.test.ts`, `ws/heartbeat.ts` constants |  ✓ |
| AGENT-02 | Auto-reconnect 1s..30s + jitter | `agent/client.ts` config block + `agent/__tests__/client.integration.test.ts` | ✓ (config verified; jitter from RESEARCH) |
| AGENT-03 | Labels os/arch/node_version/hostname + custom --label | `agent/__tests__/labels.test.ts` | ✓ |
| AGENT-04 | Hostname soft-override via PATCH | `rest-patch.integration.test.ts::hostname update` | ✓ |
| AGENT-05 | UI reads online/offline/draining (read-time computed) | `rest-list.integration.test.ts::computed state` | ✓ |
| AGENT-06 | Drain via PATCH → `state` frame to agent | `rest-patch.integration.test.ts::drain propagation` | ✓ |
| AGENT-07 | Task-state reconciliation stub on reconnect | `ws-handshake.integration.test.ts::reconnect_ack empty reconciliation` | ✓ (stub — Phase 10 populates) |
| AGENT-08 | Graceful shutdown: goodbye → flush → close(1000) → exit(0) | `agent-e2e.integration.test.ts::SIGTERM exit 0`, `agent/index.ts handleShutdown` | ✓ |

### BC-01..04 (Backward Compatibility preserved)

| ID | Requirement | Evidence | Status |
|----|-------------|----------|--------|
| BC-01 | xci without --agent identical to v1 | 302 v1 test suite green | ✓ |
| BC-02 | v1 302-test suite unchanged | `pnpm --filter xci test` exits 0 | ✓ |
| BC-03 | cli.mjs contains no ws/reconnecting-websocket code | `cold-start.test.ts::dist does NOT contain ReconnectingWebSocket` | ✓ |
| BC-04 | Cold-start < 300ms | `cold-start.test.ts` + hyperfine CI gate | ✓ |

## ROADMAP Success Criteria

| SC | Description | Verifying Test(s) | Status |
|----|-------------|-------------------|--------|
| SC-1 | `xci --agent <url> --token <reg-token>` registers, persists credential, subsequent restarts reconnect | `agent-e2e.integration.test.ts::full registration flow` + `ws-handshake.integration.test.ts::reconnect with valid credential` | ✓ |
| SC-2 | Revoked credential rejected within one reconnect cycle, WS closes 4001 'revoked', agent exits non-zero | `ws-handshake.integration.test.ts::reconnect with revoked` + `rest-revoke.integration.test.ts` | ✓ |
| SC-3 | After partition, agent reconnects with exponential backoff; server reconciles in-flight task state | `agent/client.ts` backoff config + `ws-handshake.integration.test.ts::reconnect_ack empty reconciliation` (Phase 10 will exercise real reconciliation) | ✓ (framework; reconciliation stub) |
| SC-4 | Drain mode: agent receives no new dispatches (Phase 10+); current tasks complete to natural end | `rest-patch.integration.test.ts::drain propagation` (state frame received) — Phase 10 adds dispatch filtering | ✓ (signal wired; dispatch enforcement is Phase 10) |
| SC-5 | Graceful shutdown (SIGTERM): goodbye frame, in-flight tasks complete, exit 0, no orphaned task runs | `agent-e2e.integration.test.ts::SIGTERM exit 0` (no task runs in Phase 8 — Phase 10 adds the orphan check) | ✓ (signal + frame verified; orphan guard is Phase 10) |

## Final State

**Phase 6 fence:** partially reversed (ws in packages/xci/ allowed; Biome narrowed to cli.ts; CI grep removed; cold-start gate stays)
**New packages/xci/ deps:** ws@8.20.0, reconnecting-websocket@4.4.0, env-paths@4.0.0
**New packages/server/ deps:** @fastify/websocket@11.2.0 (+ @types/ws@8.18.1 dev)
**New Drizzle migration:** `packages/server/drizzle/0001_*_agents_websocket.sql`
**New source dirs:** `packages/xci/src/agent/`, `packages/server/src/ws/`, `packages/server/src/routes/agents/`
**New tests:** ≥18 integration tests + ≥15 unit tests across xci + server
**Changeset:** `.changeset/phase-08-agent-registration.md` (minor bump for xci + @xci/server)

## Known Deferrals (Handed Off to Later Phases)

- QUOTA-03 (max_agents=5 registration gate) → Phase 10 per roadmap
- reconcile_ack real reconciliation (Phase 10 task dispatch)
- log_chunk frame handling (Phase 11)
- Agent UI dashboard (Phase 13)
- Multi-instance WS scaling (post-v2.0)
- Agent audit log (post-v2.0)

## Handoff to Phase 9

Phase 9 (Task Definitions & Secrets Management) is independent of Phase 8 per ROADMAP dependency graph — both depend only on Phase 7. Phase 10 (Dispatch Pipeline) depends on BOTH Phase 8 and Phase 9. Phase 9 planning does NOT need to wait for Phase 8 details but will reuse:
- Phase 7 forOrg discipline (unchanged)
- Phase 8 crypto/tokens.ts (compareToken, hashToken extensions available)
- Phase 8 Biome forOrg guard (new repo paths already listed in third override)

## Pending Todos Added to STATE.md

- Reconciliation real logic → Phase 10
- QUOTA-03 registration gate → Phase 10
- Multi-instance WS registry → post-v2.0
- Agent audit log → post-v2.0
- log_chunk frame type → Phase 11
```

Fill in the actual duration + commit hashes at execution time.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-agent-registration-websocket-protocol/08-CLOSEOUT-SUMMARY.md && grep -q 'Phase 08 closed' .planning/STATE.md && grep -q 'ATOK-01' .planning/phases/08-agent-registration-websocket-protocol/08-CLOSEOUT-SUMMARY.md && grep -q 'AGENT-08' .planning/phases/08-agent-registration-websocket-protocol/08-CLOSEOUT-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` frontmatter shows `completed_phases: 8`
    - `grep -c '\\[Phase 08\\]' .planning/STATE.md` ≥ 15 (decisions batch added)
    - `.planning/phases/08-agent-registration-websocket-protocol/08-CLOSEOUT-SUMMARY.md` exists
    - Traceability matrix covers all 14 Phase 8 requirements (ATOK-01..06 + AGENT-01..08) — verified by grep of each ID
    - ROADMAP SC-1..5 all mapped to test files
    - Known Deferrals section lists at least 6 deferred items with target phases
  </acceptance_criteria>
  <done>STATE.md updated with Phase 8 closure + decisions + pending todos; 08-CLOSEOUT-SUMMARY.md exists with full traceability matrix mapping 14 requirements to test files and 5 ROADMAP SCs to verifying tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Phase 8 acceptance before merge</name>
  <what-built>
    Plans 01-04 have landed autonomously:
    - Phase 6 ws-fence atomically reversed (Plan 01)
    - Server data layer: 3 tables, 3 repos, 5 adminRepo helpers, full isolation coverage (Plan 02)
    - Server WS endpoint + 5 REST routes + full handshake protocol + heartbeat + registry (Plan 03)
    - xci agent daemon: credential persistence, reconnect, graceful shutdown + E2E test (Plan 04)
    Task 1 updated both READMEs; Task 2 updated STATE.md + closeout SUMMARY.
  </what-built>
  <how-to-verify>
    Run locally on a Linux or macOS machine (Windows works for most but E2E test is Linux-only):

    1. **Full green CI signal**:
       ```bash
       pnpm install
       pnpm turbo run typecheck lint build test
       pnpm --filter @xci/server test:integration
       ```
       All commands must exit 0.

    2. **Cold-start preserved (BC-04)**:
       ```bash
       hyperfine --runs 10 --warmup 3 'node packages/xci/dist/cli.mjs --version'
       ```
       Expect: mean cold-start under 300ms.

    3. **Bundle posture preserved (BC-03)**:
       ```bash
       grep -c 'ReconnectingWebSocket' packages/xci/dist/cli.mjs   # must output 0
       grep -c 'ReconnectingWebSocket' packages/xci/dist/agent.mjs # must output ≥1
       ```

    4. **Live smoke test** — start the server locally + register one agent:
       ```bash
       # Terminal 1 — server (requires Postgres running)
       pnpm --filter @xci/server dev
       # Terminal 2 — sign up, get CSRF, create token
       curl -c /tmp/xci-cookies.txt -X POST http://localhost:3000/api/auth/signup \
         -H 'content-type: application/json' \
         -d '{"email":"test@test.local","password":"hunter2hunter2"}'
       CSRF=$(curl -b /tmp/xci-cookies.txt http://localhost:3000/api/auth/csrf | jq -r .token)
       ORG_ID=$(curl -b /tmp/xci-cookies.txt http://localhost:3000/api/orgs/me | jq -r '.orgs[0].id')
       TOKEN=$(curl -b /tmp/xci-cookies.txt -X POST http://localhost:3000/api/orgs/$ORG_ID/agent-tokens \
         -H "x-csrf-token: $CSRF" -H 'content-type: application/json' -d '{}' | jq -r .token)
       # Terminal 3 — agent
       node packages/xci/dist/cli.mjs --agent ws://localhost:3000/ws/agent --token $TOKEN \
         --config-dir /tmp/xci-smoke-agent
       ```
       Expect: `[agent] registered as xci_agt_...`. Then `ls /tmp/xci-smoke-agent/agent.json` should exist with mode 0600.
       Press Ctrl+C. Expect clean exit (code 0).
       Restart: `node packages/xci/dist/cli.mjs --agent ws://localhost:3000/ws/agent --config-dir /tmp/xci-smoke-agent`
       Expect: `[agent] reconnected (reconciliation: 0 entries)`.

    5. **Revoke flow**:
       ```bash
       AGENT_ID=$(curl -b /tmp/xci-cookies.txt http://localhost:3000/api/orgs/$ORG_ID/agents | jq -r '.[0].id')
       curl -b /tmp/xci-cookies.txt -X POST http://localhost:3000/api/orgs/$ORG_ID/agents/$AGENT_ID/revoke \
         -H "x-csrf-token: $CSRF"
       ```
       The running agent should print `[agent] terminal close [4001]: revoked — exiting` and exit non-zero.

    6. **Docs review**:
       - Open `packages/xci/README.md` — Agent Mode section reads cleanly, OS paths table correct
       - Open `packages/server/README.md` — Agents section has close-code table + REST table
       - Open `.planning/phases/08-agent-registration-websocket-protocol/08-CLOSEOUT-SUMMARY.md` — traceability matrix covers all 14 requirements
  </how-to-verify>
  <files>(human-verify checkpoint — no file writes; human runs the 6-step local verification above)</files>
  <action>Execute the 6-step verification procedure described in the <how-to-verify> block above. This is a human-gated checkpoint — autonomous execution is not permitted. After each step, confirm expected output. If any step fails, document the failure in .planning/phases/08-agent-registration-websocket-protocol/08-CLOSEOUT-SUMMARY.md under a "Verification Issues" section and block the merge until resolved.</action>
  <verify>Human operator reports back via the resume-signal (below). The plan may not be closed until the operator types "approved".</verify>
  <done>Human operator has run the 6-step local verification, reviewed docs, and typed "approved". The phase can now be closed and ROADMAP.md Phase 8 row marked complete.</done>
  <resume-signal>Type "approved" to proceed with merge, or describe issues blocking acceptance</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → users | Inaccurate docs (wrong OS paths, wrong close codes) lead to operational mistakes |
| STATE.md → future planners | Missing decisions cause drift in Phase 10+ planning |
| Human-verify gate → merge | Final control before unmergeable regression lands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Information Disclosure | Smoke-test commands in docs leak real credentials | mitigate | All examples use placeholder values (`test@test.local`, `hunter2hunter2`, `/tmp/xci-smoke-agent`); no real tokens or URLs |
| T-08-05-02 | Tampering | STATE.md inaccuracies mislead future planners | mitigate | Task 2 acceptance criterion requires all 14 requirements to appear in the decisions batch; grep audit |
| T-08-05-03 | Repudiation | Human-verify skipped in a rush | mitigate | `autonomous: false` frontmatter; gate="blocking" on Task 3 forces explicit user approval |
</threat_model>

<verification>
Phase 8 fully verified when:
1. Task 1: both READMEs updated with Phase 8 content
2. Task 2: STATE.md + 08-CLOSEOUT-SUMMARY.md both committed
3. Task 3: human types "approved" after running the 6-step verification checklist
4. Git log shows commits for each plan + final closeout
5. ROADMAP.md Phase 8 row shows `Complete` status
</verification>

<success_criteria>
- xci README has an Agent Mode section with OS-specific credential paths, flag table, close-code semantics, and TOFU rule
- server README has an Agents section with WS handshake protocol, close codes table, and REST endpoints table
- STATE.md updated: completed_phases=8; full Phase 08 decisions batch; pending todos for Phase 10/11 deferrals
- 08-CLOSEOUT-SUMMARY.md exists with traceability matrix covering all 14 requirements (ATOK-01..06 + AGENT-01..08) + all 5 ROADMAP SCs
- Human-verify checkpoint receives explicit "approved" signal after local smoke test
- CI matrix green on all OS x Node combos including fence-gates (cold-start <300ms) + integration-tests (Linux)
</success_criteria>

<output>
After Task 3 is approved, the plan is complete. No additional SUMMARY needed — `08-CLOSEOUT-SUMMARY.md` (created in Task 2) IS the final Phase 8 summary. Update `.planning/ROADMAP.md` Phase 8 row to mark it completed and list the 5 plan filenames as `[x]`.

Post-closure: run `git log --oneline --since="Phase 8 start"` and ensure commit messages follow the `feat(08-XX):` / `test(08-XX):` / `docs(08-XX):` / `ci(08-XX):` conventions.
</output>
