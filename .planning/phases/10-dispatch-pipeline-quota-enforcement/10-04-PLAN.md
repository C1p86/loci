---
phase: 10-dispatch-pipeline-quota-enforcement
plan: 04
type: execute
wave: 4
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - packages/server/src/routes/runs/index.ts
  - packages/server/src/routes/runs/trigger.ts
  - packages/server/src/routes/runs/list.ts
  - packages/server/src/routes/runs/get.ts
  - packages/server/src/routes/runs/cancel.ts
  - packages/server/src/routes/runs/usage.ts
  - packages/server/src/routes/index.ts
  - packages/server/src/app.ts
  - packages/server/src/__tests__/routes/runs-trigger.integration.test.ts
  - packages/server/src/__tests__/routes/runs-cancel.integration.test.ts
  - packages/server/src/__tests__/routes/runs-list-get.integration.test.ts
  - packages/server/src/__tests__/routes/runs-usage.integration.test.ts
autonomous: true
requirements: [DISP-03, DISP-04, DISP-07, DISP-09, QUOTA-04, QUOTA-05, QUOTA-06]
tags: [rest, routes, trigger, cancel, quota, param_overrides, dispatch_resolver]
must_haves:
  truths:
    - "POST /api/orgs/:orgId/tasks/:taskId/runs triggers a run: inserts task_runs row with state='queued' + enqueues to DispatchQueue in one transaction"
    - "Trigger route resolves params via dispatch-resolver (Phase 9) at request time and stores resolved snapshot in task_runs.task_snapshot JSONB"
    - "param_overrides DISP-09: UI can override ${VAR} at trigger time without mutating the task definition"
    - "QUOTA-04 queue-depth enforcement: if queued + active >= max_concurrent_tasks * 2 (per D-07 threshold), reject trigger with 429 RunQuotaExceededError"
    - "POST /api/orgs/:orgId/runs/:runId/cancel: Owner/Member/triggerer + CSRF; cancelRunTimer → send cancel frame → CAS to cancelled (or wait for agent's result frame up to 30s)"
    - "GET /api/orgs/:orgId/runs: paginated by queued_at DESC + limit + since cursor; filter by state + taskId"
    - "GET /api/orgs/:orgId/runs/:runId: returns full row including task_snapshot but EXCLUDING param_overrides (SEC-04 spirit — overrides may contain secrets)"
    - "GET /api/orgs/:orgId/usage: returns {agents:{used,max}, concurrent:{used,max}, retention_days} for QUOTA-06 + QUOTA-05 exposure"
    - "Pino redaction extended for req.body.param_overrides[*] and task_snapshot params — no plaintext secret in logs"
  artifacts:
    - path: "packages/server/src/routes/runs/trigger.ts"
      provides: "POST /api/orgs/:orgId/tasks/:taskId/runs: body {param_overrides?, timeout_seconds?}; resolves params + snapshots task + enqueues"
      contains: "dispatchQueue.enqueue"
    - path: "packages/server/src/routes/runs/cancel.ts"
      provides: "POST /api/orgs/:orgId/runs/:runId/cancel: cancelRunTimer + send cancel frame + CAS (transitionally cancelled)"
      contains: "cancelRunTimer"
    - path: "packages/server/src/routes/runs/list.ts"
      provides: "GET /api/orgs/:orgId/runs: cursor pagination by queued_at"
      contains: "since"
    - path: "packages/server/src/routes/runs/get.ts"
      provides: "GET /api/orgs/:orgId/runs/:runId: full row minus param_overrides"
      contains: "getById"
    - path: "packages/server/src/routes/runs/usage.ts"
      provides: "GET /api/orgs/:orgId/usage"
      contains: "retention_days"
    - path: "packages/server/src/app.ts"
      provides: "runs routes registered + Pino redaction extended"
      contains: "param_overrides"
  key_links:
    - from: "packages/server/src/routes/runs/trigger.ts"
      to: "packages/server/src/services/dispatch-resolver.ts"
      via: "resolveTaskParams({ task, runOverrides, orgSecrets }) at request time"
      pattern: "resolveTaskParams"
    - from: "packages/server/src/routes/runs/trigger.ts"
      to: "packages/server/src/services/dispatcher.ts"
      via: "fastify.dispatchQueue.enqueue(entry) AFTER DB insert, SAME transaction boundary"
      pattern: "dispatchQueue.enqueue"
    - from: "packages/server/src/routes/runs/cancel.ts"
      to: "packages/server/src/services/timeout-manager.ts + agentRegistry"
      via: "cancelRunTimer(runId) + registry.get(agentId)?.send(cancel frame)"
      pattern: "cancelRunTimer"
---

<objective>
Wire the REST route surface for Phase 10: trigger a run (with optional param overrides and timeout), list/get runs, cancel a run, and expose per-org usage. This plan owns roadmap SC #5 (param overrides) and QUOTA-04/05/06 (concurrent quota at dispatch, retention exposure, usage display). Pulls together Plan 10-01 (repo + error types), Plan 10-02 (dispatch frame type), and Plan 10-03 (DispatchQueue + cancelRunTimer) into the public API.

Purpose: Plan 10-03 created the dispatcher but nobody fills the queue. Plan 10-04 is the entry point — every manually-triggered run flows through POST /runs, and every manually-cancelled run flows through POST /runs/:runId/cancel. Without this plan, the dispatcher has nothing to dispatch and the state machine has no external drivers.

Output: 6 new route files under `packages/server/src/routes/runs/`, Pino redaction extended, 4 integration test files. Uses the Phase 7 D-06 plugin wiring pattern (requireAuth + CSRF + rate-limit per the invites analog).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dispatch-pipeline-quota-enforcement/10-CONTEXT.md
@.planning/phases/10-dispatch-pipeline-quota-enforcement/10-RESEARCH.md
@.planning/phases/10-dispatch-pipeline-quota-enforcement/10-01-SUMMARY.md
@.planning/phases/10-dispatch-pipeline-quota-enforcement/10-02-SUMMARY.md
@.planning/phases/10-dispatch-pipeline-quota-enforcement/10-03-SUMMARY.md
@packages/server/src/routes/orgs/invites.ts
@packages/server/src/routes/index.ts
@packages/server/src/services/dispatch-resolver.ts
@packages/server/src/app.ts

<interfaces>
From packages/server/src/routes/orgs/invites.ts (EXEMPLAR — use shape verbatim):
```typescript
export const invitesRoute: FastifyPluginAsync = async (fastify) => {
  fastify.post<{ Params: {orgId: string}; Body: {email:string; role:'member'|'viewer'} }>(
    '/:orgId/invites',
    {
      onRequest: [fastify.csrfProtection],
      preHandler: [fastify.requireAuth],
      schema: { body: { type:'object', required:['email','role'], additionalProperties:false, ... } },
    },
    async (req, reply) => {
      requireOwnerAndOrgMatch(req);        // shared helper
      const orgId = req.org?.id; ...
      const repos = makeRepos(fastify.db, fastify.mek);
      // ... DB work ...
      return reply.status(201).send({...});
    },
  );
};
```

From packages/server/src/services/dispatch-resolver.ts (Phase 9 pure resolver):
```typescript
export function resolveTaskParams(input: ResolveInput): ResolveOutput {
  // Input: { task: {id, name, yamlDefinition}, runOverrides, orgSecrets }
  // Output: { resolvedYaml, unresolved: readonly string[] }
  // D-34 precedence: runOverrides > orgSecrets > unresolved (left for agent .xci/secrets.yml)
}
```

From CONTEXT D-27 (route list — verbatim):
- POST /api/orgs/:orgId/tasks/:taskId/runs — Owner/Member + CSRF + rate-limit. Body: {param_overrides?, timeout_seconds?}. Returns 201 {runId, state:'queued'}.
- GET /api/orgs/:orgId/runs — any member; ?state&taskId&limit&since. Paginated cursor (D-27 + RESEARCH Open Q #2).
- GET /api/orgs/:orgId/runs/:runId — any member; full row.
- POST /api/orgs/:orgId/runs/:runId/cancel — Owner/Member or triggerer + CSRF. Per D-25.
- GET /api/orgs/:orgId/usage — any member (D-13).

From CONTEXT D-07 (QUOTA-04 queue depth):
```
max_queue_threshold = max_concurrent_tasks * 2
if countQueued(orgId) + countActive(orgId) >= max_queue_threshold → reject 429
```
This is a "reject at intake" gate, different from the dispatcher's concurrency filter (which just leaves the run in queue). The explicit reject prevents unbounded queue growth under label-mismatch scenarios.

From CONTEXT D-25 (cancel flow):
- Check Owner/Member OR `run.triggeredByUserId === currentUserId` (self-cancel allowed for Member who triggered)
- Set cancelled_by_user_id
- cancelRunTimer(runId)  [from Plan 10-03 timeout-manager]
- send `{type:'cancel', run_id, reason:'manual'}` to agent via fastify.agentRegistry.get(run.agentId)
- CAS updateStateMulti(runId, ['dispatched','running','queued'], 'cancelled', {cancelledByUserId, finishedAt:now()})
  - if run was still 'queued', it never reached an agent — just dequeue + CAS
  - if 'dispatched' or 'running', agent will also send a result frame with cancelled:true — but CAS guard ensures the terminal state isn't overwritten (once in 'cancelled', the result frame is CAS-rejected debug-logged)
- D-26 idempotency: cancelling an already-terminal run returns 200 with current state, no-op

From CONTEXT D-13 (QUOTA-06 usage shape):
```
{
  agents: { used: N, max: 5 },
  concurrent: { used: X, max: 5 },
  retention_days: 30
}
```

From RESEARCH §Security Domain (Pino redaction):
```typescript
// Extend app.ts Pino logger options:
redact: {
  paths: [
    ...existingPaths,   // already set in Phase 9 for secrets
    'req.body.param_overrides.*',   // new in Plan 10-04
    'req.body.param_overrides',     // whole object safety net
    '*.taskSnapshot.params',        // for downstream logging of queue entries
    '*.params',                     // catch-all for dispatch/queue contexts
  ],
  remove: false,       // mask with '***' not delete (per Phase 9 pattern)
}
```

From Phase 9 existing secrets repo (check for getByOrg method):
```typescript
// packages/server/src/repos/secrets.ts should have:
async resolveAll(dek: Buffer): Promise<Record<string, string>>
// Returns all decrypted {name: plaintext} for the org
```
(If Phase 9 didn't ship this exact name, check the actual API in packages/server/src/repos/secrets.ts. Adjust the call sites accordingly — the concept is "return a plain object with all org secret names → plaintext values for the current org".)

From CONTEXT D-11 (QUOTA-04 second phase):
The dispatcher's per-agent concurrency filter (Plan 10-03) already prevents over-dispatch per agent. D-11 adds an EXPLICIT check at TRIGGER time: if queued + active >= max_concurrent*2, reject with 429. This is to prevent unbounded queue growth (a label-mismatched run sits in queue forever — eventually the queue eats RAM if unbounded).

Role check helper (mirror invites.ts requireOwnerAndOrgMatch, but inclusive of Member):
```typescript
function requireMemberOrAbove(req: FastifyRequest): void {
  const urlOrgId = (req.params as {orgId: string}).orgId;
  if (!req.org) throw new SessionRequiredError();
  if (req.org.id !== urlOrgId) throw new OrgMembershipRequiredError(urlOrgId);
  if (req.org.role === 'viewer') throw new RoleInsufficientError('member');
}

function requireAnyMember(req: FastifyRequest): void {
  // Any role (owner/member/viewer) as long as org matches — for read endpoints
  const urlOrgId = (req.params as {orgId: string}).orgId;
  if (!req.org) throw new SessionRequiredError();
  if (req.org.id !== urlOrgId) throw new OrgMembershipRequiredError(urlOrgId);
}
```
These helpers may already exist in the Phase 9 routes — check `packages/server/src/routes/tasks/` and reuse.

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trigger route (POST /runs) + cancel route (POST /runs/:runId/cancel) + role helpers</name>
  <files>packages/server/src/routes/runs/trigger.ts, packages/server/src/routes/runs/cancel.ts, packages/server/src/routes/runs/index.ts, packages/server/src/__tests__/routes/runs-trigger.integration.test.ts, packages/server/src/__tests__/routes/runs-cancel.integration.test.ts</files>
  <behavior>
    Trigger route (POST):
    - Test 1 (happy path): Owner posts to /api/orgs/:orgId/tasks/:taskId/runs with empty body → 201 + {runId, state:'queued'}; DB row inserted with state='queued'; task_snapshot populated with name/description/yamlDefinition/labelRequirements; param_overrides={}; timeout_seconds=3600 (default from task.defaultTimeoutSeconds ?? 3600); dispatchQueue.countByOrg(orgId) incremented.
    - Test 2 (param overrides — DISP-09): body={param_overrides:{DEPLOY_HOST:'staging.example.com'}} → ${DEPLOY_HOST} in yamlDefinition substituted in the task_snapshot JSONB; source `tasks` row UNCHANGED.
    - Test 3 (timeout override): body={timeout_seconds:600} → DB row has timeout_seconds=600.
    - Test 4 (task.defaultTimeoutSeconds honored): seed task with default_timeout_seconds=900; empty body → run timeout_seconds=900.
    - Test 5 (timeout cap 86400): body={timeout_seconds:100000} → AJV rejects with 400 (cap via schema maximum).
    - Test 6 (Viewer rejected): Viewer posts → 403 RoleInsufficientError.
    - Test 7 (CSRF required): missing CSRF header → 403.
    - Test 8 (rate-limit): 10 POSTs in quick succession from same session → 11th returns 429 (verify @fastify/rate-limit wired — reuse existing agent-invite pattern from Phase 8).
    - Test 9 (task from other org): POST with taskId owned by orgB while user is in orgA → 404 (forOrg scoping).
    - Test 10 (QUOTA-04 queue depth — D-07): org has max_concurrent_tasks=5; seed 9 queued runs + 1 active (total 10 = 5*2); POST 11th → 429 RunQuotaExceededError {used:10, max:10, planName:'free'}.
    - Test 11 (params resolved at trigger time): seed task with ${SECRET_KEY} in yamlDefinition + an orgSecret named SECRET_KEY='xyz'; POST trigger; DB task_snapshot.yamlDefinition has literal 'xyz' NOT '${SECRET_KEY}'.
    - Test 12 (params log redaction): perform Test 11 with Pino capture stream; assert no 'xyz' string appears in any log line; param_overrides redacted to `***`.

    Cancel route (POST /:runId/cancel):
    - Test 13 (Owner cancels queued run): seed queued run; Owner POSTs cancel → 200; DB state='cancelled'; cancelled_by_user_id set; dispatchQueue does NOT still contain the runId.
    - Test 14 (Member who triggered cancels own run): Member A triggers run; later Member A cancels → 200 (self-cancel OK per D-25).
    - Test 15 (Member who did NOT trigger is rejected): Member B cancels Member A's run → 403 RoleInsufficientError (unless Member B is Owner — re-check D-25 rule: "Owner/Member + CSRF; can also be the user who triggered if Member". Interpretation: Owner always, OR anyone who triggered it. Member-cancelling-another-Member's-run is REJECTED).
    - Test 16 (Running run cancel): seed run in state='running' with agent_id=agentX + mock WS in registry; Owner cancels → cancelRunTimer called; `{type:'cancel', run_id, reason:'manual'}` sent to mock WS; response 200 with optimistic body (state='cancelling' or similar — actual CAS to 'cancelled' happens when agent sends result frame with cancelled:true).
    - Test 17 (Cancel idempotent — D-26): run already in state='succeeded'; cancel → 200 with `{state:'succeeded', message:'already terminal'}` (no state change, no cancel frame sent).
    - Test 18 (cancel nonexistent run): runId not in org → 404.
  </behavior>
  <action>
    1. Create `packages/server/src/routes/runs/trigger.ts`:
       - Export `triggerRunRoute: FastifyPluginAsync`.
       - Route: `POST /:orgId/tasks/:taskId/runs` with onRequest [fastify.csrfProtection], preHandler [fastify.requireAuth], schema with AJV body validation (param_overrides: object of string→string, maxProperties 50; timeout_seconds: integer minimum 1 maximum 86400). additionalProperties:false.
       - Handler steps:
         1. `requireMemberOrAbove(req)` — reject Viewer.
         2. Load task: `const task = await repos.forOrg(orgId).tasks.getById(taskId);` — 404 if missing.
         3. QUOTA-04 queue depth check: `const active = await repos.admin.countConcurrentByOrg(orgId); const queued = fastify.dispatchQueue.countByOrg(orgId); const plan = await repos.forOrg(orgId).orgPlans.getByOrg(orgId); if (active + queued >= plan.maxConcurrentTasks * 2) throw new RunQuotaExceededError(...)`.
         4. Resolve params: `const dek = await repos.admin.getOrgDek(orgId, fastify.mek); const orgSecrets = await repos.forOrg(orgId).secrets.resolveAll(dek); const { resolvedYaml } = resolveTaskParams({task:{id:taskId, name:task.name, yamlDefinition:task.yamlDefinition}, runOverrides: req.body.param_overrides ?? {}, orgSecrets});`. This is the DISP-09 / SC-5 implementation — task_snapshot captures the resolved YAML, not the raw task.
         5. Build task_snapshot JSONB: `{taskId, name:task.name, description:task.description, yamlDefinition: resolvedYaml, labelRequirements: task.labelRequirements}`.
         6. Compute timeout: `const timeoutSeconds = req.body.timeout_seconds ?? task.defaultTimeoutSeconds ?? 3600;`.
         7. Insert task_runs row via repos.forOrg(orgId).taskRuns.create({taskId, taskSnapshot, paramOverrides: req.body.param_overrides ?? {}, triggeredByUserId: req.user.id, timeoutSeconds}). Returns the new row with id.
         8. Enqueue in DispatchQueue: `fastify.dispatchQueue.enqueue({runId: newRun.id, orgId, taskSnapshot, params: {...orgSecrets, ...(req.body.param_overrides ?? {})}, labelRequirements: task.labelRequirements, timeoutSeconds});`. The dispatch frame will include these resolved params.
         9. Reply 201 with `{runId: newRun.id, state: 'queued'}`.
       - IMPORTANT: Steps 7+8 are NOT in a DB transaction — the DB insert is atomic but the in-memory enqueue happens after. If the server crashes between 7 and 8, Plan 10-03's boot reconciliation re-enqueues queued runs on next boot. Accept this; a real transaction spanning DB + in-memory is meaningless anyway.
    2. Create `packages/server/src/routes/runs/cancel.ts`:
       - Route: POST `/:orgId/runs/:runId/cancel` with CSRF + requireAuth.
       - Handler steps:
         1. Load run: `const run = await repos.forOrg(orgId).taskRuns.getById(runId);` — 404 if missing.
         2. Idempotent guard (D-26): `if (['succeeded','failed','cancelled','timed_out','orphaned'].includes(run.state)) return reply.status(200).send({state: run.state, message: 'already terminal'});`
         3. Authz: Owner is always allowed. Member is allowed iff `run.triggeredByUserId === req.user.id`. Viewer always rejected. Throw RoleInsufficientError otherwise.
         4. `cancelRunTimer(runId)` — Plan 10-03 timeout-manager.
         5. Send cancel frame IF agent connected: `if (run.agentId) { const ws = fastify.agentRegistry.get(run.agentId); if (ws?.readyState===WS.OPEN) ws.send(JSON.stringify({type:'cancel', run_id: runId, reason:'manual'})); }`
         6. CAS transition. If state was 'queued', transition queued→cancelled directly + dequeue from in-memory queue. If 'dispatched' or 'running', transition optimistically to 'cancelled' with cancelled_by_user_id; the agent will also send a result frame with cancelled:true which will CAS-fail (silent debug) because we already transitioned.
         
         Actually reconsider: per CONTEXT D-25 "transitions to cancelled on agent's result OR after 30s timeout". Two valid designs:
         (a) Optimistic: cancel route transitions immediately to 'cancelled'; agent's result frame is CAS-rejected. Simpler, faster UI response, but agent might still be running if the cancel frame didn't reach it.
         (b) Wait-for-confirm: cancel route sends frame, marks state='cancelling' (NEW state — NOT in our 8-state enum!) or leaves state unchanged, timer resolves in 30s.
         
         Since our enum doesn't have 'cancelling' and CONTEXT says "transitions to cancelled on agent's result OR after 30s timeout", the design is: CAS state unchanged immediately; set cancelled_by_user_id so downstream code sees the intent; rely on agent's result frame (which will update state through the normal handleResultFrame path — but that only transitions to 'cancelled' if result.cancelled=true, which agent sets on cancel-initiated kills per D-18). 30s fallback: register a setTimeout that CAS-transitions to 'cancelled' if still in dispatched/running at that point.

         REVISED handler for dispatched/running state:
           - Set cancelled_by_user_id via direct column update (NOT a state transition): `UPDATE task_runs SET cancelled_by_user_id=X WHERE id=runId AND org_id=orgId AND cancelled_by_user_id IS NULL`. This is a NON-state-machine mutation — we're annotating the intent.
           - cancelRunTimer(runId) + send cancel frame.
           - Register a 30s fallback timer that will CAS (dispatched|running → cancelled). If agent's result-with-cancelled arrives first, that CAS wins and the fallback timer is cancelled on next-frame-handling (handleResultFrame calls cancelRunTimer; we can use the SAME timer registry since this fallback IS a run timer — just a shorter one). Actually, simpler: in cancel route, register a NEW timer specifically for this cancel-fallback with cancelRunTimer replacing the main timer. When the fallback fires, CAS to 'cancelled' with exitCode -1.
           
           Hmm this is tricky because the timeout-manager only has one timer per run. If we replace the 3600s timer with a 30s cancel-fallback, we lose the original timeout. For Plan 10, accept: cancel replaces the run's timer; either agent's result arrives within 30s (timer cancelled) or CAS to cancelled at 30s (timer fired). We don't need both timers.
           
       - Response: 200 with `{state: <whatever the current state is>, cancelled_by: req.user.id}`.
    3. Create `packages/server/src/routes/runs/index.ts` as the barrel that registers all 5 route plugins:
       ```typescript
       export const runsRoutes: FastifyPluginAsync = async (fastify) => {
         await fastify.register(triggerRunRoute);
         await fastify.register(cancelRunRoute);
         await fastify.register(listRunsRoute);    // Task 2
         await fastify.register(getRunRoute);       // Task 2
         await fastify.register(usageRoute);        // Task 2
       };
       ```
    4. Tests (runs-trigger.integration.test.ts + runs-cancel.integration.test.ts) use the existing Phase 7 + Phase 8 + Phase 9 integration test harness (testcontainers + buildApp + makeAuthCookie helper). Cover all 18 behaviors above.

    WHY resolve params AT TRIGGER TIME (not at dispatch tick time): the task_snapshot stores the RESOLVED YAML so that if the org secret SECRET_KEY is rotated between trigger and dispatch, the run uses the value that was current at trigger time. This is the reproducibility property from D-01. The tradeoff: resolved secrets sit in DB JSONB in plaintext. This is accepted per DISP-03 rationale ("org secrets decifrati in plaintext, OK su WS-over-TLS") and SEC-04 spirit (we don't RETURN them via API; storage-at-rest relies on PG encryption at the deployment layer per PKG-06).

    WHY param_overrides stored separately (not merged into task_snapshot at trigger): audit and introspection. A future admin querying "what overrides did this user apply?" can see param_overrides without having to diff against the base task. Merged version lives in task_snapshot.yamlDefinition.

    WHY cancel doesn't transition 'dispatched|running' → 'cancelled' immediately: D-25 is explicit that cancelled state is reached via agent result OR 30s fallback. The reason: if the agent is mid-deploy when cancel arrives, marking 'cancelled' before the agent confirms means the UI shows "cancelled" while side effects are still in flight. The 30s wait is the agent's window to report back (agent sends result with cancelled:true immediately after SIGTERM → subprocess exits). In practice, agent will usually beat the 30s timer.
  </action>
  <verify>
    <automated>pnpm --filter @xci/server test:integration -- routes/runs-trigger routes/runs-cancel</automated>
  </verify>
  <done>All 18 tests green; DISP-09 param override path proven; QUOTA-04 queue depth gate works; cancel flow handles queued/dispatched/running + idempotency + self-cancel authz.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: List / get / usage read endpoints + route registration in app.ts + Pino redaction</name>
  <files>packages/server/src/routes/runs/list.ts, packages/server/src/routes/runs/get.ts, packages/server/src/routes/runs/usage.ts, packages/server/src/routes/index.ts, packages/server/src/app.ts, packages/server/src/__tests__/routes/runs-list-get.integration.test.ts, packages/server/src/__tests__/routes/runs-usage.integration.test.ts</files>
  <behavior>
    List route (GET /api/orgs/:orgId/runs):
    - Test 1 (basic list): seed 3 runs; Viewer/Member/Owner all GET → 200 with `{runs: [...], nextCursor: null}` in queued_at DESC order.
    - Test 2 (state filter): seed 2 queued + 1 succeeded + 1 failed; `?state=queued` → 2 queued only; `?state=succeeded,failed` (CSV) → 2 entries.
    - Test 3 (taskId filter): seed 3 runs across 2 tasks; `?taskId=X` → only task X runs.
    - Test 4 (limit): `?limit=2` → 2 rows; nextCursor set to the 2nd row's queued_at ISO.
    - Test 5 (since cursor): seed 5 runs 1s apart; `?limit=2&since=<2nd run's queued_at>` → returns rows 3-4 (queued_at < cursor).
    - Test 6 (limit clamped to 200 max): `?limit=10000` → AJV rejects (400).
    - Test 7 (cross-org isolation): orgA runs never appear when user is in orgB.
    - Test 8 (param_overrides NOT in response): response bodies omit param_overrides field (SEC-04 spirit).

    Get route (GET /api/orgs/:orgId/runs/:runId):
    - Test 9 (returns full row minus param_overrides + task_snapshot included): response has taskSnapshot object but no paramOverrides key.
    - Test 10 (404 cross-org): request runId belonging to orgB while authed as orgA → 404.

    Usage route (GET /api/orgs/:orgId/usage):
    - Test 11 (QUOTA-06 shape): Owner GETs /usage → `{agents:{used:2,max:5}, concurrent:{used:1,max:5}, retention_days:30}`.
    - Test 12 (any member role allowed): Viewer also receives 200 (informational endpoint).
    - Test 13 (reflects current state): after registering a 3rd agent, usage.agents.used = 3.

    Pino redaction:
    - Test 14 (log inspection): POST trigger with {param_overrides:{DEPLOY:'secret123'}}; capture pino logs; no line contains 'secret123'; redaction marker present.
  </behavior>
  <action>
    1. Create `packages/server/src/routes/runs/list.ts`:
       - Route: GET `/:orgId/runs`.
       - preHandler [fastify.requireAuth].
       - Querystring schema (AJV): state (string, maybe CSV — split and validate each against enum), taskId (string), limit (integer min 1 max 200 default 50), since (string ISO 8601 optional).
       - Handler:
         - requireAnyMember(req) (Viewer OK for reads).
         - Parse state (if present) into TaskRunState[] via `req.query.state?.split(',')`.
         - `const runs = await repos.forOrg(orgId).taskRuns.list({state, taskId, since: since ? new Date(since) : undefined, limit});`
         - Build response excluding param_overrides: `runs.map(r => { const { paramOverrides, ...rest } = r; return rest; })`.
         - nextCursor: `runs.length === limit ? runs[runs.length-1].queuedAt.toISOString() : null`.
         - reply 200 with `{runs: masked, nextCursor}`.
    2. Create `packages/server/src/routes/runs/get.ts`:
       - Route: GET `/:orgId/runs/:runId`.
       - Handler: requireAnyMember → getById → 404 if missing → strip param_overrides → reply 200.
    3. Create `packages/server/src/routes/runs/usage.ts`:
       - Route: GET `/:orgId/usage`.
       - Handler:
         - requireAnyMember (any role — informational).
         - `const plan = await repos.forOrg(orgId).orgPlans.getByOrg(orgId);`
         - `const agentsUsed = await repos.admin.countAgentsByOrg(orgId);`
         - `const concurrentUsed = await repos.admin.countConcurrentByOrg(orgId);`
         - reply 200 with `{agents:{used: agentsUsed, max: plan.maxAgents}, concurrent:{used: concurrentUsed, max: plan.maxConcurrentTasks}, retention_days: plan.logRetentionDays}`.
    4. Edit `packages/server/src/routes/index.ts` (the routes barrel):
       - Register `runsRoutes` plugin on the `/api/orgs` prefix to mirror invites (invites registers at `/api/orgs` too).
       - Verify no conflict with other `/:orgId/runs` routes — this is the first owner of that path segment.
    5. Edit `packages/server/src/app.ts`:
       - Extend Pino redact.paths per <interfaces>. Reuse the existing Phase 9 redaction array if present; don't overwrite, just `[...existing, ...new]`. Paths to add:
         - `req.body.param_overrides`
         - `req.body.param_overrides.*`
         - `*.taskSnapshot.params`
         - `*.params`
         - `*.paramOverrides`
       - Ensure `remove: false` so values become `***` not deleted.
       - If Phase 9 already set up pino redaction correctly and these paths are covered via the `*.params` glob, no change needed — verify first by grepping app.ts.
    6. Create `packages/server/src/__tests__/routes/runs-list-get.integration.test.ts` + `runs-usage.integration.test.ts` covering the 14 behaviors.
       - Pino log capture for Test 14: use vitest's `vi.spyOn(fastify.log, 'info')` or wire a custom Pino destination that captures writes into an array for assertion.

    WHY strip param_overrides from responses: per SEC-04 spirit ("no API returns plaintext"), param_overrides may contain plaintext secrets passed by the UI (typical use case: runtime credential for a one-off deploy). Returning them in GET /runs would re-leak them via any user with list permission. Stripping is an architectural invariant — enforced here at the route layer.

    WHY Viewer CAN read /runs and /usage but CANNOT trigger: Viewer is the read-only role (AUTH-08). Listing runs/usage is read; triggering runs or cancelling mutates. The auth helpers `requireAnyMember` vs `requireMemberOrAbove` make this explicit.

    WHY limit clamped at 200: server-side protection against UI passing `limit=100000` to exhaust memory. 200 is generous for a single API call; Phase 13 UI uses 50 by default per "typical dashboard" heuristic. Max enforced via AJV schema `maximum: 200` (returns 400 before hitting the handler).

    WHY state can be CSV: UI "show all active" filter wants state=queued,dispatched,running. Handling the split at the route layer (rather than the repo) keeps repo simpler and lets AJV enum-validate each part.
  </action>
  <verify>
    <automated>pnpm --filter @xci/server test:integration -- routes/runs-list-get routes/runs-usage</automated>
  </verify>
  <done>List/get/usage endpoints green; 14 tests pass; Pino redaction confirmed (no plaintext in logs); routes registered in app.ts; Viewer can read but not mutate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User session → run mutation | requireMemberOrAbove / requireAnyMember / Owner-OR-triggerer guards |
| param_overrides body → DB task_snapshot JSONB | AJV shape validation + Pino redaction; plaintext accepted by design (DISP-03 SEC-04 spirit) |
| Viewer role → state mutation | Architecturally blocked via requireMemberOrAbove — Viewer restricted to GET endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Information Disclosure | param_overrides leaked via GET /runs or /runs/:runId response | mitigate | All response handlers strip `paramOverrides` field before reply.send — enforced at route layer (Task 2 list/get); no central filter to forget. Integration Test 8 + Test 9 verify absence. |
| T-10-04-02 | Information Disclosure | Plaintext secret in param_overrides logged by Pino via req.body | mitigate | Pino redact.paths extended in app.ts: `req.body.param_overrides`, `req.body.param_overrides.*`, `*.params`, `*.paramOverrides`, `*.taskSnapshot.params`. remove:false → '***' mask. Test 14 verifies log absence. |
| T-10-04-03 | Elevation of Privilege | Viewer triggers run (should be Owner/Member only) | mitigate | requireMemberOrAbove throws RoleInsufficientError('member') when req.org.role === 'viewer'. Route-level guard; no runtime bypass. Test 6. |
| T-10-04-04 | Elevation of Privilege | Member cancels another Member's run | mitigate | Cancel handler checks `req.org.role === 'owner' || run.triggeredByUserId === req.user.id`. Member-of-other-run gets 403. Test 15. |
| T-10-04-05 | Denial of Service | Unbounded triggering fills DB + queue | mitigate | QUOTA-04 queue-depth gate: `active + queued >= maxConcurrent * 2 → 429` (D-07). Also @fastify/rate-limit on POST /runs (Test 8). |
| T-10-04-06 | Denial of Service | GET /runs with limit=1000000 exhausts server memory | mitigate | AJV schema `limit: {maximum: 200}` — rejected with 400 before handler. Test 6. |
| T-10-04-07 | Spoofing | User posts runId for a run in another org to /cancel | mitigate | forOrg(orgId).taskRuns.getById(runId) returns undefined for cross-org; handler returns 404. Test 10 + cancel cross-org test. |
| T-10-04-08 | Information Disclosure | /usage endpoint leaks plan details to Viewer | accept | Plan internals (max_agents, max_concurrent, retention_days) are not secret — they're UI hints for the org's users. Same information is in the error message of QUOTA-03. Viewer intentionally sees this (Test 12). |
| T-10-04-09 | Tampering | CSRF bypass on POST /runs or /cancel | mitigate | onRequest: [fastify.csrfProtection] on all POST routes (Phase 7 D-34 pattern). Test 7. |
| T-10-04-10 | Repudiation | Cancel/trigger actions not attributed | mitigate | `triggered_by_user_id` column set on INSERT (Task 1); `cancelled_by_user_id` column updated on cancel (Task 1); both visible in GET /runs/:runId for forensic review. |
</threat_model>

<verification>
- `pnpm --filter @xci/server exec tsc --noEmit` clean
- `pnpm --filter @xci/server exec biome check packages/server/src/routes/runs/ packages/server/src/app.ts` clean
- `pnpm --filter @xci/server test:integration` — all new route tests green + all prior tests still pass
- `pnpm --filter xci test` green (BC-01/BC-02)
- Manual: POST /runs with `curl -X POST -H 'x-csrf-token:...' -b cookie:... -d '{"param_overrides":{"DEPLOY_HOST":"staging"}}'` → 201 body matches; GET /runs/:runId shows task_snapshot.yamlDefinition contains 'staging' (not '${DEPLOY_HOST}').
</verification>

<success_criteria>
Goal-backward: "Users can trigger runs with param overrides, cancel runs, list and read runs, and see org usage; param_overrides and plaintext secrets never leak via API response or Pino log."
- [ ] DISP-03 satisfied: dispatch frame built with resolved params, sent from Plan 10-03 tickDispatcher; Plan 10-04 produces the inputs
- [ ] DISP-04 satisfied: TaskRun fully persisted with state machine driven by trigger (queued) → dispatcher (dispatched) → agent (running, succeeded/failed/cancelled)
- [ ] DISP-07 satisfied: POST /runs/:runId/cancel: Owner/Member-triggerer + CSRF + cancelRunTimer + cancel frame + CAS
- [ ] DISP-09 satisfied (ROADMAP SC #5): param_overrides preserved per-run; task_snapshot.yamlDefinition has resolved values; tasks row unchanged
- [ ] QUOTA-04 satisfied: queue-depth intake gate (active+queued >= max*2 → 429)
- [ ] QUOTA-05 satisfied: retention_days exposed via /usage endpoint
- [ ] QUOTA-06 satisfied: /usage returns agents.used/max + concurrent.used/max + retention_days
- [ ] Pino redaction extended to cover param_overrides + params + taskSnapshot.params
- [ ] 6 route files registered under /api/orgs path; handlers use existing requireAuth + CSRF pattern
- [ ] Viewer can read /runs + /usage; cannot POST trigger or cancel
- [ ] Member can trigger any task; can cancel only own triggers (Owner bypasses)
- [ ] BC-01/BC-02: v1 xci + Phase 8 + Phase 9 all green
- [ ] BC-04: cold-start <300ms (server-only changes)
</success_criteria>

<output>
After completion, create `.planning/phases/10-dispatch-pipeline-quota-enforcement/10-04-SUMMARY.md` with:
- 5 route endpoints documented (method, path, body shape, response shape, auth guard)
- Redaction paths diff (what was added to app.ts Pino config)
- QUOTA-04 threshold formula + test reference (D-07 max_concurrent*2)
- Cancel flow state transition notes (queued→immediate-cancel vs dispatched/running→wait-for-agent-result-OR-30s)
- Handoff note: Plan 10-05 wires agent-side handlers to send the dispatch frame's payload to the v1 executor and stream log_chunks + final result.
</output>
</content>
</invoke>