---
phase: 07-database-schema-auth
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - packages/server/src/errors.ts
  - packages/server/src/__tests__/errors.test.ts
  - packages/server/src/crypto/tokens.ts
  - packages/server/src/crypto/password.ts
  - packages/server/src/crypto/__tests__/tokens.test.ts
  - packages/server/src/crypto/__tests__/password.test.ts
  - packages/server/src/config/env.schema.ts
  - packages/server/src/email/transport.ts
  - packages/server/src/email/templates/verify-email.ts
  - packages/server/src/email/templates/password-reset.ts
  - packages/server/src/email/templates/invite.ts
  - packages/server/src/email/templates/invite-revoked.ts
  - packages/server/src/email/templates/role-changed.ts
  - packages/server/src/email/__tests__/transport.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-11]
user_setup: []

must_haves:
  truths:
    - "XciServerError hierarchy has 7 category base classes (validation/authn/authz/notfound/conflict/ratelimit/internal) mirroring v1 LociError pattern"
    - "oneOfEachConcrete() factory test proves every concrete error subclass has a unique `code` string"
    - "Category → HTTP-status exhaustive switch causes TS compile error when a category is added without updating the switch"
    - "generateToken() returns a 32-byte randomBytes base64url string (never logged)"
    - "generateId('prefix') returns 'xci_<prefix>_<20-char-base64url>' format"
    - "hashPassword() produces Argon2id hash with m=19456,t=2,p=1 (OWASP 2024 / D-31)"
    - "verifyPassword(hash, pw) returns true for correct password, false for wrong (constant-time)"
    - "argon2SelfTest() warns if hash <100ms or >2000ms (D-31 Specifics)"
    - "Env schema validates DATABASE_URL, SESSION_COOKIE_SECRET (≥32 chars), EMAIL_TRANSPORT enum; optional SMTP_*"
    - "Email transport factory supports log, stub, smtp kinds; stub.captured captures sent messages for tests"
    - "5 email templates exist: verify-email, password-reset, invite, invite-revoked, role-changed — each exports a factory returning {subject, html, text}"
  artifacts:
    - path: "packages/server/src/errors.ts"
      provides: "XciServerError hierarchy — 7 area base classes + concrete subclasses for Phase 7 flows"
      exports: ["XciServerError", "ValidationError", "AuthnError", "AuthzError", "NotFoundError", "ConflictError", "RateLimitError", "InternalError", "HttpStatus", "httpStatusFor"]
    - path: "packages/server/src/__tests__/errors.test.ts"
      provides: "instanceof chain, unique code, Error.cause propagation, exhaustive status mapping tests"
      contains: "oneOfEachConcrete"
    - path: "packages/server/src/crypto/tokens.ts"
      provides: "generateToken() + generateId(prefix) per D-33"
      exports: ["generateToken", "generateId"]
    - path: "packages/server/src/crypto/password.ts"
      provides: "hashPassword, verifyPassword, argon2SelfTest per D-31 (OWASP 2024 m=19456,t=2,p=1)"
      exports: ["hashPassword", "verifyPassword", "argon2SelfTest"]
    - path: "packages/server/src/config/env.schema.ts"
      provides: "@fastify/env JSON schema + FastifyInstance.config type augmentation per D-07"
      exports: ["envSchema"]
    - path: "packages/server/src/email/transport.ts"
      provides: "Three-kind transport factory (log/stub/smtp) per D-29"
      exports: ["createTransport", "EmailMessage", "EmailTransport"]
    - path: "packages/server/src/email/templates/verify-email.ts"
      provides: "verify-email template factory {subject, html, text}"
      exports: ["verifyEmailTemplate"]
    - path: "packages/server/src/email/templates/password-reset.ts"
      provides: "password-reset template"
      exports: ["passwordResetTemplate"]
    - path: "packages/server/src/email/templates/invite.ts"
      provides: "org invite template"
      exports: ["inviteTemplate"]
    - path: "packages/server/src/email/templates/invite-revoked.ts"
      provides: "invite revoked notification template"
      exports: ["inviteRevokedTemplate"]
    - path: "packages/server/src/email/templates/role-changed.ts"
      provides: "role change notification template"
      exports: ["roleChangedTemplate"]
  key_links:
    - from: "packages/server/src/errors.ts httpStatusFor"
      to: "HttpStatus const"
      via: "exhaustive switch on category field"
      pattern: "switch.*category"
    - from: "packages/server/src/crypto/password.ts"
      to: "@node-rs/argon2"
      via: "hash/verify with ARGON2_OPTS const"
      pattern: "memoryCost: 19456"
    - from: "packages/server/src/email/transport.ts"
      to: "nodemailer SMTP transport"
      via: "kind === 'smtp' branch"
      pattern: "nodemailer.createTransport"
---

<objective>
Create four foundational subsystems consumed by later plans:
1. **XciServerError hierarchy** (D-08) mirroring v1 LociError pattern — 7 category base classes + concrete subclasses with unique `code` strings and an exhaustive category-to-HTTP-status switch. Tested by `oneOfEachConcrete()` factory.
2. **Crypto primitives** (D-31, D-33) — `generateToken()`, `generateId(prefix)`, `hashPassword()`, `verifyPassword()`, `argon2SelfTest()` with OWASP 2024 Argon2id params.
3. **Env schema** (D-07) — @fastify/env JSON schema + FastifyInstance.config module augmentation.
4. **Email transport + 5 templates** (D-29, D-30) — pluggable log/stub/smtp transport factory and TS-literal template factories.

Purpose: Plans 04 (app factory + plugins + repos) and 06 (auth routes) import from every subsystem here. This plan is pure unit code (no DB, no HTTP) so it parallels Plan 02 in Wave 2.

Output: All listed files exist; unit tests pass; typecheck green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-database-schema-auth/07-CONTEXT.md
@.planning/phases/07-database-schema-auth/07-RESEARCH.md
@.planning/phases/07-database-schema-auth/07-PATTERNS.md
@.planning/phases/07-database-schema-auth/07-01-PLAN.md
@packages/xci/src/errors.ts
@packages/xci/src/__tests__/errors.test.ts

<interfaces>
<!-- RESEARCH §@fastify/env JSON schema, lines 754-799 -->
<!-- RESEARCH §Token generator (D-33), lines 1128-1143 -->
<!-- RESEARCH §@node-rs/argon2 password service with warmup (D-31), lines 1089-1124 -->
<!-- RESEARCH §Nodemailer abstract transport (D-29), lines 1145-1196 -->
<!-- PATTERNS §packages/server/src/errors.ts — EXACT structural match to packages/xci/src/errors.ts -->

xci v1 pattern to mirror (packages/xci/src/errors.ts lines 34-70):
```typescript
export abstract class XciError extends Error {
  public readonly code: string;
  public abstract readonly category: XciErrorCategory;
  public readonly suggestion?: string;
  constructor(message: string, options: XciErrorOptions) {
    super(message, options.cause !== undefined ? { cause: options.cause } : undefined);
    this.name = new.target.name;
    this.code = options.code;
    if (options.suggestion !== undefined) this.suggestion = options.suggestion;
  }
}
export abstract class ConfigError extends XciError {
  public readonly category = 'config' as const;
}
// ...
```

xci v1 ExitCode exhaustive switch (packages/xci/src/errors.ts around line 229):
```typescript
export function exitCodeFor(err: XciError): ExitCode {
  switch (err.category) {
    case 'config': return ExitCode.CONFIG_ERROR;
    case 'command': return ExitCode.COMMAND_ERROR;
    // ... exhaustive — TS errors if a case is missing
  }
}
```

Server equivalents:
- HttpStatus const with BAD_REQUEST/UNAUTHORIZED/FORBIDDEN/NOT_FOUND/CONFLICT/RATE_LIMITED/INTERNAL
- `httpStatusFor(err: XciServerError): number` — exhaustive switch on category
- Categories: 'validation' | 'authn' | 'authz' | 'notfound' | 'conflict' | 'ratelimit' | 'internal'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: XciServerError hierarchy + oneOfEachConcrete unit tests</name>
  <files>packages/server/src/errors.ts, packages/server/src/__tests__/errors.test.ts</files>
  <read_first>
    - packages/xci/src/errors.ts (ENTIRE FILE — structural template; mirror HttpStatus + hierarchy + subclasses + exhaustive switch)
    - packages/xci/src/__tests__/errors.test.ts (ENTIRE FILE — structural template; mirror oneOfEachConcrete factory + all test groups)
    - .planning/phases/07-database-schema-auth/07-CONTEXT.md D-08 (XciServerError hierarchy), D-10 (secrets never in error bodies — mirror ShellInjectionError pattern)
    - .planning/phases/07-database-schema-auth/07-PATTERNS.md §packages/server/src/errors.ts (SECTION IS THE SPEC — contains exact subclasses to create)
    - .planning/phases/07-database-schema-auth/07-PATTERNS.md §packages/server/src/__tests__/errors.test.ts (test conventions)
    - .planning/phases/07-database-schema-auth/07-RESEARCH.md §Security Domain (which errors carry secrets-like data)
  </read_first>
  <behavior>
    - XciServerError abstract base extends Error with readonly code, abstract category, optional suggestion (mirror xci lines 34-48)
    - 7 area abstract base classes: ValidationError, AuthnError, AuthzError, NotFoundError, ConflictError, RateLimitError, InternalError — each sets readonly category = '<area>' as const
    - Concrete subclasses cover Phase 7 flows (listed in action)
    - HttpStatus const object: BAD_REQUEST=400, UNAUTHORIZED=401, FORBIDDEN=403, NOT_FOUND=404, CONFLICT=409, RATE_LIMITED=429, INTERNAL=500
    - httpStatusFor(err) exhaustive switch on category — adding new category without updating switch is a TS error
    - Every concrete subclass has unique machine `code` string with prefix: VAL_/AUTHN_/AUTHZ_/NF_/CONFLICT_/RATE_/INT_
    - Error constructors that could receive secrets (InvalidCredentialsError, TokenError) do NOT accept them in constructor (mirror ShellInjectionError void value pattern)
    - Test file mirrors xci's errors.test.ts structure: oneOfEachConcrete factory, instanceof chain per subclass, new.target.name round-trip, Error.cause propagation, code uniqueness (Set size == array length), exhaustive httpStatusFor mapping
  </behavior>
  <action>
**Create `packages/server/src/errors.ts`** — mirror `packages/xci/src/errors.ts` structure exactly. Header doc, HttpStatus const, XciErrorCategory type, XciServerErrorOptions interface, XciServerError abstract base, 7 area abstract base classes, then concrete subclasses:

```typescript
// src/errors.ts
//
// Full XciServerError hierarchy (D-08).
// Mirrors v1 LociError pattern from packages/xci/src/errors.ts.
// Later plans import and throw; keep additions minimal.

/** HTTP status codes mapped per category. Stable — do not renumber. */
export const HttpStatus = {
  OK: 200,
  NO_CONTENT: 204,
  BAD_REQUEST: 400,
  UNAUTHORIZED: 401,
  FORBIDDEN: 403,
  NOT_FOUND: 404,
  CONFLICT: 409,
  RATE_LIMITED: 429,
  INTERNAL: 500,
} as const;
export type HttpStatus = (typeof HttpStatus)[keyof typeof HttpStatus];

export type XciServerErrorCategory =
  | 'validation'
  | 'authn'
  | 'authz'
  | 'notfound'
  | 'conflict'
  | 'ratelimit'
  | 'internal';

export interface XciServerErrorOptions {
  /** Machine ID, e.g. "AUTHN_INVALID_CREDENTIALS". Must be unique across the entire hierarchy. */
  code: string;
  suggestion?: string;
  /** Standard ES2022 Error.cause — use for wrapping underlying errors. */
  cause?: unknown;
}

/**
 * Abstract base for all server errors. Never throw directly.
 */
export abstract class XciServerError extends Error {
  public readonly code: string;
  public abstract readonly category: XciServerErrorCategory;
  public readonly suggestion?: string;

  constructor(message: string, options: XciServerErrorOptions) {
    super(message, options.cause !== undefined ? { cause: options.cause } : undefined);
    this.name = new.target.name;
    this.code = options.code;
    if (options.suggestion !== undefined) {
      this.suggestion = options.suggestion;
    }
  }
}

/* ---------- Area base classes ---------- */

export abstract class ValidationError extends XciServerError { public readonly category = 'validation' as const; }
export abstract class AuthnError extends XciServerError { public readonly category = 'authn' as const; }
export abstract class AuthzError extends XciServerError { public readonly category = 'authz' as const; }
export abstract class NotFoundError extends XciServerError { public readonly category = 'notfound' as const; }
export abstract class ConflictError extends XciServerError { public readonly category = 'conflict' as const; }
export abstract class RateLimitError extends XciServerError { public readonly category = 'ratelimit' as const; }
export abstract class InternalError extends XciServerError { public readonly category = 'internal' as const; }

/* ---------- Concrete subclasses (Phase 7) ---------- */

// ValidationError
export class SchemaValidationError extends ValidationError {
  constructor(message: string, cause?: unknown) {
    super(message, { code: 'VAL_SCHEMA', cause, suggestion: 'Check the request body against the endpoint schema' });
  }
}
export class WeakPasswordError extends ValidationError {
  constructor() {
    super('Password must be at least 12 characters', {
      code: 'VAL_WEAK_PASSWORD',
      suggestion: 'Use a longer password; length beats complexity (NIST SP 800-63B)',
    });
  }
}

// AuthnError
export class InvalidCredentialsError extends AuthnError {
  constructor() {
    // NB: never accept email/password in the constructor — avoids accidental logging.
    super('Invalid email or password', { code: 'AUTHN_INVALID_CREDENTIALS' });
  }
}
export class SessionRequiredError extends AuthnError {
  constructor() {
    super('Authentication required', { code: 'AUTHN_SESSION_REQUIRED' });
  }
}
export class SessionExpiredError extends AuthnError {
  constructor() {
    super('Session expired', { code: 'AUTHN_SESSION_EXPIRED', suggestion: 'Log in again' });
  }
}
export class TokenInvalidError extends AuthnError {
  constructor() {
    // NB: do NOT accept the token value in the constructor — avoid logging.
    super('Token is invalid or expired', { code: 'AUTHN_TOKEN_INVALID' });
  }
}
export class EmailNotVerifiedError extends AuthnError {
  constructor() {
    super('Email address is not verified', { code: 'AUTHN_EMAIL_NOT_VERIFIED', suggestion: 'Check your inbox for the verification email' });
  }
}

// AuthzError
export class OrgMembershipRequiredError extends AuthzError {
  constructor(orgId: string) {
    // orgId is safe to include — not a secret.
    super(`Not a member of org ${orgId}`, { code: 'AUTHZ_NOT_ORG_MEMBER' });
  }
}
export class RoleInsufficientError extends AuthzError {
  constructor(requiredRole: 'owner' | 'member') {
    super(`Operation requires role ${requiredRole}`, { code: 'AUTHZ_ROLE_INSUFFICIENT' });
  }
}
export class CsrfTokenError extends AuthzError {
  constructor() {
    super('CSRF token missing or invalid', { code: 'AUTHZ_CSRF_INVALID' });
  }
}

// NotFoundError
export class UserNotFoundError extends NotFoundError {
  constructor() {
    super('User not found', { code: 'NF_USER' });
  }
}
export class OrgNotFoundError extends NotFoundError {
  constructor(orgId: string) {
    super(`Org ${orgId} not found`, { code: 'NF_ORG' });
  }
}
export class InviteNotFoundError extends NotFoundError {
  constructor() {
    super('Invite not found or already consumed', { code: 'NF_INVITE' });
  }
}

// ConflictError
export class EmailAlreadyRegisteredError extends ConflictError {
  constructor() {
    // Deliberately no email in the message to prevent enumeration via error body.
    super('Email address is already registered', { code: 'CONFLICT_EMAIL_TAKEN' });
  }
}
export class InviteAlreadyAcceptedError extends ConflictError {
  constructor() {
    super('Invite has already been accepted', { code: 'CONFLICT_INVITE_USED' });
  }
}
export class OwnerRoleImmutableError extends ConflictError {
  constructor() {
    super('Owner role cannot be removed or transferred in Phase 7', { code: 'CONFLICT_OWNER_IMMUTABLE' });
  }
}

// RateLimitError
export class RateLimitExceededError extends RateLimitError {
  constructor(retryAfterSeconds: number) {
    super('Too many requests', { code: 'RATE_EXCEEDED', suggestion: `Retry after ${retryAfterSeconds}s` });
  }
}

// InternalError
export class DatabaseError extends InternalError {
  constructor(message: string, cause?: unknown) {
    super(message, { code: 'INT_DATABASE', cause });
  }
}
export class EmailTransportError extends InternalError {
  constructor(message: string, cause?: unknown) {
    super(message, { code: 'INT_EMAIL_TRANSPORT', cause });
  }
}

/* ---------- Category → HTTP status exhaustive mapping ---------- */

export function httpStatusFor(err: XciServerError): HttpStatus {
  switch (err.category) {
    case 'validation': return HttpStatus.BAD_REQUEST;
    case 'authn': return HttpStatus.UNAUTHORIZED;
    case 'authz': return HttpStatus.FORBIDDEN;
    case 'notfound': return HttpStatus.NOT_FOUND;
    case 'conflict': return HttpStatus.CONFLICT;
    case 'ratelimit': return HttpStatus.RATE_LIMITED;
    case 'internal': return HttpStatus.INTERNAL;
    default: {
      // TS exhaustive check — adding a new category without adding a case is a compile error
      const _exhaustive: never = err.category;
      return _exhaustive;
    }
  }
}
```

**Create `packages/server/src/__tests__/errors.test.ts`** — mirror `packages/xci/src/__tests__/errors.test.ts` structure. Must include:

1. `oneOfEachConcrete()` factory returning one instance of every concrete subclass above (20 instances)
2. Test suite: "instanceof chains" — for each concrete subclass, assert instanceof Concrete → Area → XciServerError → Error
3. Test suite: "new.target.name" — every `err.name === 'ConcreteClassName'`
4. Test suite: "Error.cause propagation" — errors that accept cause (SchemaValidationError, DatabaseError, EmailTransportError) propagate it
5. Test: "every concrete subclass has a unique `code` across the hierarchy" — `expect(new Set(codes).size).toBe(codes.length)`
6. Test suite: "httpStatusFor exhaustive mapping":
   - validation → 400
   - authn → 401
   - authz → 403
   - notfound → 404
   - conflict → 409
   - ratelimit → 429
   - internal → 500
7. Test: "suggestion is optional and propagates" — WeakPasswordError has suggestion, InvalidCredentialsError does not
8. Test: "secrets-carrying errors do not accept secret args" — type-level: InvalidCredentialsError() and TokenInvalidError() are 0-arg constructors (compile-time guarantee via type-test)

Use `.js` suffix for relative imports (`../errors.js`). Mirror xci test imports at lines 1-27.

Run:
```bash
pnpm --filter @xci/server test:unit -- errors
```
Expected: ~15-20 passing tests.
  </action>
  <verify>
    <automated>test -f packages/server/src/errors.ts && grep -q "abstract class XciServerError" packages/server/src/errors.ts && grep -q "export class InvalidCredentialsError" packages/server/src/errors.ts && grep -q "AUTHN_INVALID_CREDENTIALS" packages/server/src/errors.ts && grep -q "httpStatusFor" packages/server/src/errors.ts && grep -q "_exhaustive: never" packages/server/src/errors.ts && test -f packages/server/src/__tests__/errors.test.ts && grep -q "oneOfEachConcrete" packages/server/src/__tests__/errors.test.ts && pnpm --filter @xci/server typecheck && pnpm --filter @xci/server test:unit -- errors 2>&1 | grep -qE "(passed|✓)"</automated>
  </verify>
  <done>errors.ts has 7 area bases + 20+ concrete subclasses + exhaustive httpStatusFor. errors.test.ts has oneOfEachConcrete factory + instanceof + code-uniqueness + exhaustive mapping tests. Unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crypto primitives — tokens.ts (generateToken/generateId) + password.ts (Argon2id + argon2SelfTest) + unit tests</name>
  <files>packages/server/src/crypto/tokens.ts, packages/server/src/crypto/password.ts, packages/server/src/crypto/__tests__/tokens.test.ts, packages/server/src/crypto/__tests__/password.test.ts, packages/server/src/test-utils/two-org-fixture.ts</files>
  <read_first>
    - .planning/phases/07-database-schema-auth/07-RESEARCH.md §Token generator (D-33) (lines 1128-1143 verbatim)
    - .planning/phases/07-database-schema-auth/07-RESEARCH.md §@node-rs/argon2 password service with warmup (D-31) (lines 1089-1124 verbatim)
    - .planning/phases/07-database-schema-auth/07-CONTEXT.md D-25 (xci_<prefix>_<rand> scheme), D-31 (Argon2id params 19456/2/1), D-32 (password min 12 chars — enforced by WeakPasswordError in Task 1), D-33 (all tokens randomBytes(32) base64url)
    - .planning/phases/07-database-schema-auth/07-RESEARCH.md §Common Pitfalls — Pitfall 3 (Argon2 cold-start warmup)
    - packages/server/src/test-utils/two-org-fixture.ts (Plan 02 Task 2 — replace tempId with canonical generateId import)
  </read_first>
  <behavior>
    - generateToken() returns a string of length 43 (32 bytes → 43 base64url chars, no padding)
    - generateId('org') returns "xci_org_<20-char-base64url>" total length ~28
    - generateId supports prefixes: org, usr, mem, ses, inv, ver, pwr, plan
    - hashPassword(pw) returns an encoded argon2id string starting with "$argon2id$v=19$m=19456,t=2,p=1$..."
    - verifyPassword(hash, pw) returns true for original pw, false for wrong pw
    - argon2SelfTest(logger) calls logger.info or logger.warn depending on timing; never throws
    - two-org-fixture.ts imports generateId from crypto/tokens.js (replace inline tempId)
  </behavior>
  <action>
**Create `packages/server/src/crypto/tokens.ts`** — exact copy from RESEARCH lines 1128-1143:
```typescript
import { randomBytes } from 'node:crypto';

/**
 * 256-bit opaque token — used for session IDs, email-verification tokens,
 * password-reset tokens, and org-invite tokens (D-33).
 * Never log the return value.
 */
export function generateToken(): string {
  return randomBytes(32).toString('base64url');
}

/**
 * xci_<prefix>_<base64url-rand15>  — 120-bit entropy, URL-safe (D-25).
 * 15 bytes → 20 base64url chars; total length ~28 chars for "xci_usr_..."
 */
export function generateId(prefix: 'org' | 'usr' | 'mem' | 'ses' | 'inv' | 'ver' | 'pwr' | 'plan'): string {
  return `xci_${prefix}_${randomBytes(15).toString('base64url')}`;
}
```

**Create `packages/server/src/crypto/password.ts`** — exact copy from RESEARCH lines 1091-1124:
```typescript
import { hash, verify, Algorithm } from '@node-rs/argon2';

/**
 * OWASP 2024 Argon2id params (D-31).
 * Source: https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html
 */
const ARGON2_OPTS = {
  memoryCost: 19456,  // 19 MiB
  timeCost: 2,
  parallelism: 1,
  algorithm: Algorithm.Argon2id,
} as const;

export async function hashPassword(password: string): Promise<string> {
  return hash(password, ARGON2_OPTS);
}

export async function verifyPassword(encoded: string, password: string): Promise<boolean> {
  try {
    return await verify(encoded, password, ARGON2_OPTS);
  } catch {
    // Malformed encoded hash → constant-time false (don't leak parse errors)
    return false;
  }
}

/**
 * D-31 Specifics (Pitfall 3): warmup hash at server boot to avoid event-loop
 * stall on first signup. Also times the hash and warns if params are out of range.
 */
export async function argon2SelfTest(logger: {
  info: (obj: object, msg: string) => void;
  warn: (obj: object, msg: string) => void;
}): Promise<void> {
  const start = performance.now();
  await hashPassword('xci-warmup-benchmark-not-a-real-password');
  const elapsed = performance.now() - start;
  if (elapsed < 100) {
    logger.warn({ elapsedMs: Math.round(elapsed) }, 'argon2 self-test: hash too fast, consider stronger params');
  } else if (elapsed > 2000) {
    logger.warn({ elapsedMs: Math.round(elapsed) }, 'argon2 self-test: hash too slow, will starve event loop under load');
  } else {
    logger.info({ elapsedMs: Math.round(elapsed) }, 'argon2 self-test: hash timing OK');
  }
}
```

**Create `packages/server/src/crypto/__tests__/tokens.test.ts`**:
```typescript
import { describe, it, expect } from 'vitest';
import { generateToken, generateId } from '../tokens.js';

describe('generateToken (D-33)', () => {
  it('returns a 43-char base64url string (32 bytes unpadded)', () => {
    const t = generateToken();
    expect(t).toHaveLength(43);
    expect(t).toMatch(/^[A-Za-z0-9_-]{43}$/); // base64url alphabet, no padding
  });
  it('returns a different value each call (collision risk < 2^-128)', () => {
    const tokens = new Set<string>();
    for (let i = 0; i < 100; i++) tokens.add(generateToken());
    expect(tokens.size).toBe(100);
  });
});

describe('generateId (D-25)', () => {
  it('produces xci_<prefix>_<20-char-base64url>', () => {
    const id = generateId('usr');
    expect(id).toMatch(/^xci_usr_[A-Za-z0-9_-]{20}$/);
  });
  it.each(['org', 'usr', 'mem', 'ses', 'inv', 'ver', 'pwr', 'plan'] as const)('accepts prefix %s', (p) => {
    const id = generateId(p);
    expect(id.startsWith(`xci_${p}_`)).toBe(true);
  });
  it('100 calls return 100 unique IDs', () => {
    const ids = new Set<string>();
    for (let i = 0; i < 100; i++) ids.add(generateId('usr'));
    expect(ids.size).toBe(100);
  });
});
```

**Create `packages/server/src/crypto/__tests__/password.test.ts`**:
```typescript
import { describe, it, expect } from 'vitest';
import { hashPassword, verifyPassword, argon2SelfTest } from '../password.js';

describe('hashPassword / verifyPassword (D-31)', () => {
  it('produces an argon2id encoded hash with m=19456,t=2,p=1', async () => {
    const h = await hashPassword('correct-horse-battery-staple');
    expect(h).toMatch(/^\$argon2id\$v=19\$m=19456,t=2,p=1\$/);
  });

  it('verifyPassword returns true for correct password', async () => {
    const pw = 'correct-horse-battery-staple';
    const h = await hashPassword(pw);
    expect(await verifyPassword(h, pw)).toBe(true);
  });

  it('verifyPassword returns false for wrong password', async () => {
    const h = await hashPassword('correct-horse-battery-staple');
    expect(await verifyPassword(h, 'wrong-password')).toBe(false);
  });

  it('verifyPassword returns false for malformed hash (no throw)', async () => {
    expect(await verifyPassword('not-a-valid-hash', 'anything')).toBe(false);
  });

  it('two hashes of the same password are different (random salt)', async () => {
    const a = await hashPassword('same-password');
    const b = await hashPassword('same-password');
    expect(a).not.toBe(b);
  });
}, 60_000); // argon2 hashes take ~500ms each; 5 tests × ~500ms = ~2.5s; headroom to 60s

describe('argon2SelfTest (Pitfall 3 warmup)', () => {
  it('calls logger.info when hash time is in normal range', async () => {
    const calls: Array<{ level: 'info' | 'warn'; obj: object; msg: string }> = [];
    const logger = {
      info: (obj: object, msg: string) => calls.push({ level: 'info', obj, msg }),
      warn: (obj: object, msg: string) => calls.push({ level: 'warn', obj, msg }),
    };
    await argon2SelfTest(logger);
    expect(calls).toHaveLength(1);
    expect(calls[0]?.obj).toHaveProperty('elapsedMs');
    // Most hardware: info (100-2000ms). Very slow CI: warn. Either is acceptable.
    expect(['info', 'warn']).toContain(calls[0]?.level);
  });
}, 30_000);
```

**Update `packages/server/src/test-utils/two-org-fixture.ts`** — replace inline `tempId` with canonical `generateId` import from `../crypto/tokens.js`:

- Remove the local `tempId()` function definition.
- Remove `import { randomBytes } from 'node:crypto'` unless still used for email suffixes (keep randomBytes for email uniqueness).
- Add `import { generateId } from '../crypto/tokens.js';`.
- Replace every `tempId('org')` → `generateId('org')`, `tempId('usr')` → `generateId('usr')`, `tempId('mem')` → `generateId('mem')`, `tempId('plan')` → `generateId('plan')`.

Keep the random email suffix for collision safety.

Run:
```bash
pnpm --filter @xci/server test:unit
```
Expected: tokens tests + password tests + errors tests all pass. ~20+ passing.
  </action>
  <verify>
    <automated>test -f packages/server/src/crypto/tokens.ts && grep -q "generateToken" packages/server/src/crypto/tokens.ts && grep -q "randomBytes(32).toString('base64url')" packages/server/src/crypto/tokens.ts && test -f packages/server/src/crypto/password.ts && grep -q "memoryCost: 19456" packages/server/src/crypto/password.ts && grep -q "argon2SelfTest" packages/server/src/crypto/password.ts && test -f packages/server/src/crypto/__tests__/tokens.test.ts && test -f packages/server/src/crypto/__tests__/password.test.ts && grep -q "from '../crypto/tokens.js'" packages/server/src/test-utils/two-org-fixture.ts && ! grep -q "function tempId" packages/server/src/test-utils/two-org-fixture.ts && pnpm --filter @xci/server typecheck && pnpm --filter @xci/server test:unit 2>&1 | tail -5 | grep -qE "(passed|✓)"</automated>
  </verify>
  <done>tokens.ts + password.ts + two unit test files created. two-org-fixture.ts now imports generateId (tempId removed). All unit tests pass (`pnpm --filter @xci/server test:unit`). argon2 hash format matches $argon2id$v=19$m=19456,t=2,p=1$.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Env schema (@fastify/env) + Email transport factory (3 kinds) + 5 email templates + transport unit test</name>
  <files>packages/server/src/config/env.schema.ts, packages/server/src/email/transport.ts, packages/server/src/email/templates/verify-email.ts, packages/server/src/email/templates/password-reset.ts, packages/server/src/email/templates/invite.ts, packages/server/src/email/templates/invite-revoked.ts, packages/server/src/email/templates/role-changed.ts, packages/server/src/email/__tests__/transport.test.ts</files>
  <read_first>
    - .planning/phases/07-database-schema-auth/07-RESEARCH.md §@fastify/env JSON schema (lines 754-799 verbatim — including declare module 'fastify' augmentation)
    - .planning/phases/07-database-schema-auth/07-RESEARCH.md §Nodemailer abstract transport (D-29) (lines 1145-1196 verbatim)
    - .planning/phases/07-database-schema-auth/07-RESEARCH.md §Email template verify-email example (lines 1188-1195 template shape)
    - .planning/phases/07-database-schema-auth/07-CONTEXT.md D-07 (env schema), D-29 (three transports log/stub/smtp), D-30 (TS literal templates, 5 templates: verify, reset, invite, invite-revoked, role-changed)
    - .planning/phases/07-database-schema-auth/07-PATTERNS.md §Email templates section
  </read_first>
  <behavior>
    - envSchema validates NODE_ENV enum, PORT int default 3000, DATABASE_URL postgres(ql):// pattern, SESSION_COOKIE_SECRET minLength 32, EMAIL_TRANSPORT enum [log, stub, smtp], optional SMTP_*
    - FastifyInstance.config is type-augmented with all schema fields (declare module 'fastify')
    - createTransport('log', cfg) logs { to, subject } via cfg.logger.info; returns { send }
    - createTransport('stub', cfg) returns { send, captured: [] } where each send() appends to captured
    - createTransport('smtp', cfg) throws synchronously if SMTP_HOST or SMTP_FROM missing; otherwise returns a transporter wrapping nodemailer.createTransport
    - 5 templates each export a factory function taking params object and returning {subject: string, html: string, text: string}
    - HTML in templates escapes the {link} and {email} params (simple &lt; / &gt; / &amp; / &quot; / &#39; escape) to prevent HTML injection
    - transport.test.ts covers all 3 kinds + the required-fields validation + stub capture behavior
  </behavior>
  <action>
**Create `packages/server/src/config/env.schema.ts`** — exact copy from RESEARCH lines 756-799:
```typescript
// D-07: @fastify/env JSON schema; server fails to boot on missing/invalid env.
export const envSchema = {
  type: 'object',
  required: ['DATABASE_URL', 'SESSION_COOKIE_SECRET', 'EMAIL_TRANSPORT'],
  properties: {
    NODE_ENV: { type: 'string', enum: ['development', 'test', 'production'], default: 'development' },
    PORT: { type: 'integer', default: 3000 },
    LOG_LEVEL: { type: 'string', enum: ['fatal', 'error', 'warn', 'info', 'debug', 'trace'], default: 'info' },
    DATABASE_URL: { type: 'string', pattern: '^postgres(ql)?://' },
    SESSION_COOKIE_SECRET: { type: 'string', minLength: 32 },
    EMAIL_TRANSPORT: { type: 'string', enum: ['log', 'stub', 'smtp'] },
    SMTP_HOST: { type: 'string' },
    SMTP_PORT: { type: 'integer', default: 587 },
    SMTP_USER: { type: 'string' },
    SMTP_PASS: { type: 'string' },
    SMTP_FROM: { type: 'string', format: 'email' },
  },
  additionalProperties: false,
} as const;

// Type augmentation so `fastify.config` is typed everywhere
declare module 'fastify' {
  interface FastifyInstance {
    config: {
      NODE_ENV: 'development' | 'test' | 'production';
      PORT: number;
      LOG_LEVEL: 'fatal' | 'error' | 'warn' | 'info' | 'debug' | 'trace';
      DATABASE_URL: string;
      SESSION_COOKIE_SECRET: string;
      EMAIL_TRANSPORT: 'log' | 'stub' | 'smtp';
      SMTP_HOST?: string;
      SMTP_PORT: number;
      SMTP_USER?: string;
      SMTP_PASS?: string;
      SMTP_FROM?: string;
    };
  }
}
```

Note on Assumption A1 from RESEARCH: @fastify/env doesn't support `oneOf` for conditional SMTP fields; Task 3 enforces at runtime via `createTransport` throwing on missing SMTP_HOST/SMTP_FROM.

**Create `packages/server/src/email/transport.ts`** — exact copy from RESEARCH lines 1145-1196 with small typing adjustment:
```typescript
import nodemailer, { type Transporter } from 'nodemailer';
import { EmailTransportError } from '../errors.js';

export interface EmailMessage {
  to: string;
  subject: string;
  html: string;
  text: string;
}

export interface EmailTransport {
  send(msg: EmailMessage): Promise<void>;
  /** Only set when kind === 'stub' — exposed for test inspection. */
  captured?: EmailMessage[];
}

export interface TransportConfig {
  SMTP_HOST?: string;
  SMTP_PORT?: number;
  SMTP_USER?: string;
  SMTP_PASS?: string;
  SMTP_FROM?: string;
  logger: { info: (obj: object, msg: string) => void };
}

export function createTransport(kind: 'log' | 'stub' | 'smtp', cfg: TransportConfig): EmailTransport {
  if (kind === 'log') {
    return {
      async send(msg) {
        // D-10: never log body or html — only {to, subject} metadata
        cfg.logger.info({ to: msg.to, subject: msg.subject }, '[email:log] would send');
      },
    };
  }
  if (kind === 'stub') {
    const captured: EmailMessage[] = [];
    return {
      captured,
      async send(msg) {
        captured.push(msg);
      },
    };
  }
  // kind === 'smtp'
  if (!cfg.SMTP_HOST || !cfg.SMTP_FROM) {
    throw new EmailTransportError('EMAIL_TRANSPORT=smtp requires SMTP_HOST and SMTP_FROM env vars');
  }
  const transporter: Transporter = nodemailer.createTransport({
    host: cfg.SMTP_HOST,
    port: cfg.SMTP_PORT ?? 587,
    secure: false, // STARTTLS auto-upgrade via Nodemailer
    ...(cfg.SMTP_USER !== undefined && {
      auth: { user: cfg.SMTP_USER, pass: cfg.SMTP_PASS ?? '' },
    }),
  });
  const from = cfg.SMTP_FROM;
  return {
    async send(msg) {
      try {
        await transporter.sendMail({ from, to: msg.to, subject: msg.subject, html: msg.html, text: msg.text });
      } catch (cause) {
        throw new EmailTransportError('SMTP send failed', cause);
      }
    },
  };
}
```

**Create 5 email templates — each file is ~15 lines. Include a 5-line HTML escape helper inline (no library per Research §"Don't Hand-Roll" HTML escape row).**

`packages/server/src/email/templates/_escape.ts`:
```typescript
/** 5-line HTML entity escape (Research §Don't Hand-Roll). */
export function escapeHtml(s: string): string {
  return s
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#39;');
}
```

`packages/server/src/email/templates/verify-email.ts`:
```typescript
import { escapeHtml } from './_escape.js';

export function verifyEmailTemplate(params: { link: string; email: string }) {
  const { link, email } = params;
  const safeLink = escapeHtml(link);
  const safeEmail = escapeHtml(email);
  return {
    subject: 'Verify your xci email',
    html: `<p>Hello ${safeEmail},</p><p>Click to verify your account: <a href="${safeLink}">${safeLink}</a></p><p>Link expires in 24 hours.</p>`,
    text: `Hello ${email},\n\nVerify your account: ${link}\n\nLink expires in 24 hours.`,
  };
}
```

`packages/server/src/email/templates/password-reset.ts`:
```typescript
import { escapeHtml } from './_escape.js';

export function passwordResetTemplate(params: { link: string; email: string }) {
  const safeLink = escapeHtml(params.link);
  const safeEmail = escapeHtml(params.email);
  return {
    subject: 'xci password reset',
    html: `<p>Hello ${safeEmail},</p><p>A password reset was requested. If this was you, click: <a href="${safeLink}">${safeLink}</a></p><p>Link expires in 1 hour and can only be used once. If you did not request this, ignore this email.</p>`,
    text: `Hello ${params.email},\n\nReset your password: ${params.link}\n\nLink expires in 1 hour, single-use. Ignore if you did not request this.`,
  };
}
```

`packages/server/src/email/templates/invite.ts`:
```typescript
import { escapeHtml } from './_escape.js';

export function inviteTemplate(params: { link: string; orgName: string; inviterEmail: string; role: 'member' | 'viewer' }) {
  const { link, orgName, inviterEmail, role } = params;
  const safeLink = escapeHtml(link);
  const safeOrg = escapeHtml(orgName);
  const safeInviter = escapeHtml(inviterEmail);
  return {
    subject: `You've been invited to join ${orgName} on xci`,
    html: `<p>${safeInviter} invited you to join <strong>${safeOrg}</strong> as a ${role}.</p><p><a href="${safeLink}">${safeLink}</a></p><p>This link expires in 7 days.</p>`,
    text: `${inviterEmail} invited you to join ${orgName} on xci as a ${role}.\n\nAccept: ${link}\n\nLink expires in 7 days.`,
  };
}
```

`packages/server/src/email/templates/invite-revoked.ts`:
```typescript
import { escapeHtml } from './_escape.js';

export function inviteRevokedTemplate(params: { orgName: string; revokerEmail: string }) {
  const safeOrg = escapeHtml(params.orgName);
  const safeRevoker = escapeHtml(params.revokerEmail);
  return {
    subject: `Your invite to ${params.orgName} was revoked`,
    html: `<p>${safeRevoker} has revoked your pending invitation to join ${safeOrg}.</p><p>No action is needed.</p>`,
    text: `${params.revokerEmail} has revoked your pending invitation to join ${params.orgName} on xci.`,
  };
}
```

`packages/server/src/email/templates/role-changed.ts`:
```typescript
import { escapeHtml } from './_escape.js';

export function roleChangedTemplate(params: { orgName: string; newRole: 'owner' | 'member' | 'viewer'; changedByEmail: string }) {
  const safeOrg = escapeHtml(params.orgName);
  const safeChanger = escapeHtml(params.changedByEmail);
  return {
    subject: `Your role in ${params.orgName} changed`,
    html: `<p>${safeChanger} set your role in ${safeOrg} to <strong>${params.newRole}</strong>.</p>`,
    text: `${params.changedByEmail} set your role in ${params.orgName} to ${params.newRole}.`,
  };
}
```

**Create `packages/server/src/email/__tests__/transport.test.ts`**:
```typescript
import { describe, it, expect } from 'vitest';
import { createTransport } from '../transport.js';
import { EmailTransportError } from '../../errors.js';

function fakeLogger() {
  const calls: Array<{ obj: object; msg: string }> = [];
  return { calls, logger: { info: (obj: object, msg: string) => calls.push({ obj, msg }) } };
}

describe('createTransport (D-29) — log kind', () => {
  it('send() invokes logger.info with {to, subject} metadata only (D-10 — no body/html)', async () => {
    const { calls, logger } = fakeLogger();
    const t = createTransport('log', { logger });
    await t.send({ to: 'u@example.com', subject: 'Hi', html: '<p>body</p>', text: 'body' });
    expect(calls).toHaveLength(1);
    expect(calls[0]?.obj).toEqual({ to: 'u@example.com', subject: 'Hi' });
    expect(JSON.stringify(calls[0])).not.toContain('<p>body</p>'); // body never logged
  });
});

describe('createTransport (D-29) — stub kind', () => {
  it('captures messages in order', async () => {
    const { logger } = fakeLogger();
    const t = createTransport('stub', { logger });
    expect(t.captured).toEqual([]);
    await t.send({ to: 'a@x.com', subject: 's1', html: 'h1', text: 't1' });
    await t.send({ to: 'b@x.com', subject: 's2', html: 'h2', text: 't2' });
    expect(t.captured).toHaveLength(2);
    expect(t.captured?.[0]?.to).toBe('a@x.com');
    expect(t.captured?.[1]?.subject).toBe('s2');
  });
});

describe('createTransport (D-29) — smtp kind', () => {
  it('throws EmailTransportError when SMTP_HOST is missing', () => {
    const { logger } = fakeLogger();
    expect(() => createTransport('smtp', { logger, SMTP_FROM: 'x@x.com' })).toThrow(EmailTransportError);
  });
  it('throws EmailTransportError when SMTP_FROM is missing', () => {
    const { logger } = fakeLogger();
    expect(() => createTransport('smtp', { logger, SMTP_HOST: 'smtp.example.com' })).toThrow(EmailTransportError);
  });
  it('returns a send() function when required fields present (no real SMTP call)', () => {
    const { logger } = fakeLogger();
    const t = createTransport('smtp', {
      logger,
      SMTP_HOST: 'smtp.example.com',
      SMTP_FROM: 'x@example.com',
      SMTP_USER: 'u',
      SMTP_PASS: 'p',
    });
    expect(typeof t.send).toBe('function');
  });
});
```

Run:
```bash
pnpm --filter @xci/server test:unit
```
Expected: all unit tests pass including transport tests.
  </action>
  <verify>
    <automated>test -f packages/server/src/config/env.schema.ts && grep -q "envSchema" packages/server/src/config/env.schema.ts && grep -q "declare module 'fastify'" packages/server/src/config/env.schema.ts && test -f packages/server/src/email/transport.ts && grep -q "createTransport" packages/server/src/email/transport.ts && grep -q "EmailTransportError" packages/server/src/email/transport.ts && test -f packages/server/src/email/templates/verify-email.ts && test -f packages/server/src/email/templates/password-reset.ts && test -f packages/server/src/email/templates/invite.ts && test -f packages/server/src/email/templates/invite-revoked.ts && test -f packages/server/src/email/templates/role-changed.ts && test -f packages/server/src/email/__tests__/transport.test.ts && pnpm --filter @xci/server typecheck && pnpm --filter @xci/server test:unit 2>&1 | tail -5 | grep -qE "(passed|✓)"</automated>
  </verify>
  <done>env.schema.ts has JSON schema + fastify module augmentation. transport.ts has 3 kinds + validates SMTP required fields via EmailTransportError. 5 template files exist with HTML escape. transport.test.ts has 5+ tests covering all 3 kinds + error paths. Unit suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User password → hashPassword() | Plaintext password enters the server on signup/login, is hashed by Argon2id, and never logged (D-10 redaction covers `req.body.password`) |
| Argon2 process memory → attacker | Event-loop starvation risk if params too low OR argon2 cold-start (Pitfall 3) |
| Token generation → database | 256-bit opaque tokens stored plaintext in DB (D-12 — accepted risk); never echoed in logs |
| Env vars → process.env | SESSION_COOKIE_SECRET, SMTP_PASS, DATABASE_URL enter process.env; fail-on-boot env validation catches missing/weak values |
| Email template params → recipient | User-controlled strings (email, orgName, link) are interpolated into HTML — must escape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Information disclosure | Plaintext password in error body / stack trace | mitigate | InvalidCredentialsError, WeakPasswordError, TokenInvalidError constructors take NO password/token arg (Task 1 mirrors ShellInjectionError void pattern). Pino redact.paths covers req.body.password (Plan 04). |
| T-07-03-02 | Spoofing | Argon2 params too weak → brute-force password DB | mitigate | D-31 locks m=19456, t=2, p=1 (OWASP 2024). Task 2 test asserts exact hash format `$argon2id$v=19$m=19456,t=2,p=1$`. argon2SelfTest warns if <100ms (params too weak). |
| T-07-03-03 | DoS | Argon2 cold-start blocks event loop for ~1s on first signup (Pitfall 3) | mitigate | argon2SelfTest called from server.ts before listen() (Plan 04 enforces). Task 2 tests argon2SelfTest timing logic. |
| T-07-03-04 | DoS | Argon2 params too slow → event-loop starvation under load | mitigate | argon2SelfTest warns when >2000ms; OWASP 2024 params (19MiB) tuned for shared-resource containers; documented in D-31 Specifics. |
| T-07-03-05 | Information disclosure | Token leaked via verifyPassword error message | mitigate | verifyPassword wraps argon2 verify in try/catch returning false on any error — no parse-error leak. Task 2 test covers malformed hash returning false. |
| T-07-03-06 | Tampering | Stored XSS via user email/orgName in invite email HTML | mitigate | All 5 templates use escapeHtml() on every interpolated string. escapeHtml() handles & < > " ' — covers OWASP XSS cheatsheet basic set. HTML emails are not a typical XSS target (mail clients sanitize) but defense-in-depth. |
| T-07-03-07 | Tampering | SMTP credentials in env leaked via log | mitigate | Env schema validates SMTP_PASS is a plain string but logger never logs process.env; pino redaction (Plan 04) does not cover process.env but there's no code path that logs process.env. |
| T-07-03-08 | DoS | Missing SMTP_HOST at boot with EMAIL_TRANSPORT=smtp → silent failure on first email send | mitigate | createTransport() throws EmailTransportError synchronously at server boot (or first transport creation) when SMTP_HOST/SMTP_FROM missing. Task 3 test covers both missing-field throws. |
| T-07-03-09 | Tampering | @node-rs/argon2 on Alpine musl edge case (Pitfall 9) | accept | Phase 7 tests run on host (not in a container); argon2 uses host glibc. Phase 14 Docker base is node:22-slim (Debian glibc) per ROADMAP. Alpine is used only for Postgres testcontainer, not Node runtime. |
| T-07-03-10 | Information disclosure | Token stored plaintext in DB (sessions, email_verifications, password_resets, org_invites) | accept | Per D-12 CONTEXT: 256-bit entropy, never leaves DB except in response cookie/email body. Future hardening (DB-read defense) may hash tokens at rest — deferred. |

Severity: all low/medium. No high without mitigation. The argon2 cold-start is highest-impact DoS but is definitively mitigated by argon2SelfTest warmup in server.ts (Plan 04).
</threat_model>

<verification>
```bash
# Errors hierarchy
test -f packages/server/src/errors.ts
grep -c "extends ValidationError\|extends AuthnError\|extends AuthzError\|extends NotFoundError\|extends ConflictError\|extends RateLimitError\|extends InternalError" packages/server/src/errors.ts  # ≥ 15 concrete classes
grep -q "_exhaustive: never" packages/server/src/errors.ts

# Crypto
grep -q "memoryCost: 19456" packages/server/src/crypto/password.ts
grep -q "randomBytes(32).toString('base64url')" packages/server/src/crypto/tokens.ts

# Env
grep -q "minLength: 32" packages/server/src/config/env.schema.ts
grep -q "declare module 'fastify'" packages/server/src/config/env.schema.ts

# Email
ls packages/server/src/email/templates/*.ts | wc -l  # ≥ 6 (5 templates + _escape)
grep -q "nodemailer.createTransport" packages/server/src/email/transport.ts

# Unit tests green
pnpm --filter @xci/server typecheck
pnpm --filter @xci/server test:unit

# v1 fence
pnpm --filter xci test
```
</verification>

<success_criteria>
- errors.ts has 7 area base classes + 15+ concrete subclasses + HttpStatus const + exhaustive httpStatusFor switch.
- errors.test.ts has oneOfEachConcrete factory + instanceof + code-uniqueness (Set size == length) + exhaustive httpStatusFor mapping tests.
- tokens.ts exports generateToken (43-char base64url) + generateId (xci_<prefix>_<20char>).
- password.ts exports hashPassword/verifyPassword with exact OWASP 2024 Argon2id params (m=19456/t=2/p=1) + argon2SelfTest with <100ms/>2000ms branching.
- crypto/__tests__/{tokens,password}.test.ts pass (5+ tests each).
- two-org-fixture.ts imports generateId from crypto/tokens.js (inline tempId removed).
- config/env.schema.ts has JSON schema with required DATABASE_URL/SESSION_COOKIE_SECRET (minLength 32)/EMAIL_TRANSPORT + FastifyInstance.config augmentation.
- email/transport.ts exports createTransport (3 kinds) + EmailMessage + EmailTransport; smtp kind throws EmailTransportError when SMTP_HOST or SMTP_FROM missing.
- 5 email templates in email/templates/ each export a factory returning {subject, html, text}; all use escapeHtml() for interpolated strings.
- email/__tests__/transport.test.ts covers log (metadata-only log), stub (captured array), smtp (throws on missing fields).
- `pnpm --filter @xci/server typecheck` + `pnpm --filter @xci/server test:unit` exit 0.
- `pnpm --filter xci test` exits 0 (D-39 fence).
</success_criteria>

<output>
After completion, create `.planning/phases/07-database-schema-auth/07-03-SUMMARY.md` documenting: exact list of concrete error subclasses created + their codes, argon2 hash format captured from a real run, typical argon2SelfTest timing on dev hardware, env schema field list with defaults, template file sizes, any diversion from RESEARCH verbatim code.
</output>
