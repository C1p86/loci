---
phase: 07-database-schema-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/package.json
  - packages/server/tsconfig.json
  - packages/server/drizzle.config.ts
  - packages/server/vitest.unit.config.ts
  - packages/server/vitest.integration.config.ts
  - packages/server/.env.example
  - packages/server/.gitignore
  - biome.json
  - .changeset/07-server-bootstrap.md
autonomous: true
requirements: [AUTH-11, QUOTA-07]
user_setup: []

must_haves:
  truths:
    - "`pnpm --filter @xci/server typecheck` exits 0 with real tsc config (no stub noop)"
    - "`pnpm --filter @xci/server test:unit` exits 0 (zero tests, vitest discovers no files)"
    - "`pnpm --filter @xci/server test:integration` exits 0 (zero tests)"
    - "`pnpm --filter @xci/server build` emits JS+dts to packages/server/dist/"
    - "@xci/server package.json has private: false (D-12 commitment)"
    - "All runtime + dev dependencies from RESEARCH §Installation are installed at exact versions"
    - "Biome lint passes with new server-scoped override active"
    - "v1 test suite (packages/xci/) still passes green — D-39 fence respected"
  artifacts:
    - path: "packages/server/package.json"
      provides: "Real @xci/server package manifest with scripts, deps, private: false"
      contains: "\"name\": \"@xci/server\""
    - path: "packages/server/tsconfig.json"
      provides: "TypeScript config extending root base with outDir: dist"
      contains: "\"extends\": \"../../tsconfig.base.json\""
    - path: "packages/server/drizzle.config.ts"
      provides: "drizzle-kit config pointing at src/db/schema.ts → drizzle/"
      exports: ["default"]
    - path: "packages/server/vitest.unit.config.ts"
      provides: "Unit test config (no DB, isolate:true)"
      exports: ["default"]
    - path: "packages/server/vitest.integration.config.ts"
      provides: "Integration test config with globalSetup/globalTeardown, isolate:false"
      exports: ["default"]
    - path: "packages/server/.env.example"
      provides: "Documented env var template for DATABASE_URL, SESSION_COOKIE_SECRET, EMAIL_TRANSPORT"
    - path: "biome.json"
      provides: "Root biome config with added packages/server/src/** override"
      contains: "packages/server/src/**"
    - path: ".changeset/07-server-bootstrap.md"
      provides: "First @xci/server changeset (minor feat)"
  key_links:
    - from: "packages/server/package.json"
      to: "tsconfig.base.json"
      via: "tsc --build reads tsconfig.json → extends base"
      pattern: "tsc -b"
    - from: "biome.json overrides"
      to: "packages/server/src/**"
      via: "glob pattern match"
      pattern: "packages/server/src"
    - from: "biome.json files.includes"
      to: "packages/**/vitest*.config.ts + packages/**/drizzle.config.ts"
      via: "includes glob"
      pattern: "vitest*.config.ts"
---

<objective>
Bootstrap the `@xci/server` package: replace the Phase 6 echo-noop stub with a real package.json, tsconfig, two vitest configs, drizzle-kit config, .env.example, biome override, and install all Phase 7 runtime + dev dependencies at exact versions from RESEARCH §Installation.

Purpose: Phase 7 cannot build anything without a working package skeleton. This plan lays the foundation — no source code yet, just configuration and dependency installation — so Waves 2+ can write real code against a compiling, testable package.

Output: `pnpm --filter @xci/server typecheck && pnpm --filter @xci/server test:unit && pnpm --filter @xci/server test:integration && pnpm --filter @xci/server build` all exit 0 on a clean checkout, with zero production/test source code written.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-database-schema-auth/07-CONTEXT.md
@.planning/phases/07-database-schema-auth/07-RESEARCH.md
@.planning/phases/07-database-schema-auth/07-PATTERNS.md
@.planning/phases/06-monorepo-setup-backward-compat-fence/06-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Mirror reference: packages/xci/package.json shape -->

packages/xci/package.json (reference — DO NOT copy bin, size-limit, tsup scripts):
```json
{
  "name": "xci",
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=20.5.0" },
  "scripts": {
    "build": "tsup",
    "lint": "biome check .",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
```

tsconfig.base.json (already at root — inherited by server):
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "verbatimModuleSyntax": true,
    "noEmit": true,      // ← server tsconfig MUST override to false
    "types": ["node"]
  }
}
```

biome.json existing override pattern (lines 46-68 — MIRROR shape, different paths):
```json
{
  "includes": ["packages/xci/src/**/*.ts"],
  "linter": {
    "rules": {
      "style": {
        "noRestrictedImports": {
          "level": "error",
          "options": { "paths": { "ws": {...} } }
        }
      }
    }
  }
}
```

biome.json existing files.includes (MUST be widened to cover new config files):
```json
"includes": [
  "packages/**/src/**/*.ts",
  "packages/**/tsup.config.ts",
  "packages/**/vitest.config.ts"
]
```
Change to include `packages/**/vitest*.config.ts` and `packages/**/drizzle.config.ts`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite packages/server/package.json with real manifest + install runtime + dev dependencies</name>
  <files>packages/server/package.json, pnpm-lock.yaml (regenerated), packages/server/.gitignore</files>
  <read_first>
    - packages/server/package.json (current Phase 6 stub with echo-noops)
    - packages/xci/package.json (shape reference — DO NOT copy bin, size-limit)
    - .planning/phases/07-database-schema-auth/07-RESEARCH.md §Standard Stack table + §Installation (single command)
    - .planning/phases/07-database-schema-auth/07-PATTERNS.md §packages/server/package.json section (divergences from xci)
    - .planning/phases/07-database-schema-auth/07-CONTEXT.md D-12 (private: false flip from Phase 6), D-39 (no changes to packages/xci/)
    - .planning/phases/06-monorepo-setup-backward-compat-fence/06-CONTEXT.md D-11, D-12 (fixed versioning)
    - CLAUDE.md §Technology Stack (Node engines floor >=20.5.0, ESM-only)
  </read_first>
  <action>
Replace `packages/server/package.json` wholesale with real manifest (mirror xci shape per 07-PATTERNS.md §packages/server/package.json; DIVERGENCES: no `bin`, no `size-limit`, no `tsup` scripts, no `prepublishOnly`). Exact shape:

```json
{
  "name": "@xci/server",
  "version": "0.0.0",
  "private": false,
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=20.5.0" },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "drizzle"],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsx src/server.ts",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test:unit": "vitest run --config vitest.unit.config.ts",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "test": "pnpm test:unit && pnpm test:integration",
    "test:watch": "vitest --config vitest.unit.config.ts",
    "typecheck": "tsc -b --noEmit",
    "db:generate": "drizzle-kit generate"
  }
}
```

Key decisions encoded:
- `private: false` — Phase 6 D-12 commitment (CONTEXT D-39 line 30). Flipping here enables Changesets fixed-versioning discipline.
- Node engines `>=20.5.0` — repo-wide floor per CLAUDE.md.
- `main`/`types` point at `dist/index.js` / `dist/index.d.ts` (tsc output paths). `dist/` and `drizzle/` (migrations) in `files` for first publish in Phase 14.
- Build is `tsc -b` NOT tsup (RESEARCH §Alternatives — servers don't benefit from bundling; xci-specific size-limit fence does NOT apply per Phase 6 D-15).
- Two vitest configs (D-24 unit vs integration layering).
- No `prepublishOnly` in Phase 7 (Phase 14 handles first publish).

Then run from repo root (one command per RESEARCH §Installation, exact versions pinned):
```bash
pnpm --filter @xci/server add \
  fastify@5.8.5 \
  drizzle-orm@0.45.2 \
  postgres@3.4.9 \
  @node-rs/argon2@2.0.2 \
  @fastify/env@6.0.0 \
  @fastify/cookie@11.0.2 \
  @fastify/csrf-protection@7.1.0 \
  @fastify/rate-limit@10.3.0 \
  @fastify/helmet@13.0.2 \
  fastify-plugin@5.0.1 \
  nodemailer@8.0.5 \
  pino@10.3.1
```

Then dev deps:
```bash
pnpm --filter @xci/server add -D \
  drizzle-kit@0.31.10 \
  @testcontainers/postgresql@11.14.0 \
  @types/nodemailer@^7 \
  tsx@4.21.0 \
  pino-pretty@13.1.3
```

(Omit `ts-morph` — Option A regex approach is chosen per RESEARCH §"Auto-discovery isolation test" Option A recommendation.)

Also create `packages/server/.gitignore` with:
```
dist/
.turbo/
node_modules/
.vitest-output/
coverage/
```

Do NOT modify packages/xci/ or packages/web/ anything (D-39 fence).
  </action>
  <verify>
    <automated>test -f packages/server/package.json && grep -q '"private": false' packages/server/package.json && grep -q '"name": "@xci/server"' packages/server/package.json && grep -q '"tsc -b"' packages/server/package.json && grep -q '"fastify":' packages/server/package.json && grep -q '"drizzle-orm":' packages/server/package.json && grep -q '"@node-rs/argon2":' packages/server/package.json && grep -q '"@testcontainers/postgresql":' packages/server/package.json && test -f packages/server/.gitignore && pnpm install --frozen-lockfile 2>&1 | grep -v "ERR_" > /dev/null</automated>
  </verify>
  <done>`packages/server/package.json` is the real manifest with private: false, all 12 runtime deps + 5 dev deps at exact versions, scripts for build/test/typecheck/db:generate. `pnpm install` succeeds with regenerated lockfile. `.gitignore` excludes dist/.turbo/.vitest-output.</done>
</task>

<task type="auto">
  <name>Task 2: Create tsconfig.json, drizzle.config.ts, vitest configs, .env.example, update biome overrides</name>
  <files>packages/server/tsconfig.json, packages/server/drizzle.config.ts, packages/server/vitest.unit.config.ts, packages/server/vitest.integration.config.ts, packages/server/.env.example, biome.json</files>
  <read_first>
    - packages/xci/tsconfig.json (analog — 5 lines, extends base)
    - packages/xci/vitest.config.ts (analog for unit config shape)
    - tsconfig.base.json (inherited — noEmit: true must be overridden)
    - biome.json (current — files.includes + existing packages/xci/src/** override at lines 46-68)
    - .planning/phases/07-database-schema-auth/07-PATTERNS.md §packages/server/tsconfig.json + §vitest.unit.config.ts + §vitest.integration.config.ts + §Shared Patterns Biome section
    - .planning/phases/07-database-schema-auth/07-RESEARCH.md §drizzle.config.ts (dev-only) + §testcontainers harness vitest.integration.config.ts excerpt + §@fastify/env JSON schema (for env vars)
    - .planning/phases/07-database-schema-auth/07-CONTEXT.md D-06 plugin order, D-07 env schema, D-20..24 test layering, D-23 Linux-only integration, D-28 drizzle migrator
  </read_first>
  <action>
Create five config files + update biome.json.

**`packages/server/tsconfig.json`** — extends base, overrides `noEmit` to emit JS+dts to dist/:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": false,
    "outDir": "dist",
    "rootDir": "src",
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "composite": false
  },
  "include": ["src/**/*.ts", "drizzle.config.ts", "vitest.unit.config.ts", "vitest.integration.config.ts"],
  "exclude": ["dist", "node_modules", "drizzle/**"]
}
```

Note: `drizzle/` (generated SQL migration files) excluded from TS include; only `drizzle.config.ts` (TS dev config) is included.

**`packages/server/drizzle.config.ts`** — dev-time schema → SQL generator (RESEARCH §drizzle.config.ts):
```typescript
import type { Config } from 'drizzle-kit';

export default {
  schema: './src/db/schema.ts',
  out: './drizzle',
  dialect: 'postgresql',
  dbCredentials: {
    url: process.env.DATABASE_URL ?? 'postgresql://postgres:postgres@localhost:5432/xci_dev',
  },
  strict: true,
  verbose: true,
} satisfies Config;
```

**`packages/server/vitest.unit.config.ts`** — unit tests, no DB, isolate:true (RESEARCH §vitest.unit.config.ts):
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/__tests__/**/*.test.ts'],
    exclude: [
      'node_modules',
      'dist',
      'src/**/*.integration.test.ts',
      'src/**/*.isolation.test.ts',
      'src/**/__tests__/**/*.integration.test.ts',
      'src/**/__tests__/**/*.isolation.test.ts',
    ],
    reporters: ['default'],
    pool: 'threads',
    isolate: true,
    testTimeout: 10_000,
    coverage: {
      provider: 'v8',
      reporter: ['text', 'lcov'],
      include: ['src/**/*.ts'],
      exclude: ['src/**/__tests__/**', 'src/**/index.ts'],
    },
  },
});
```

**`packages/server/vitest.integration.config.ts`** — testcontainers, isolate:false (RESEARCH §testcontainers harness):
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    include: [
      'src/**/*.integration.test.ts',
      'src/**/__tests__/**/*.integration.test.ts',
      'src/**/__tests__/**/*.isolation.test.ts',
    ],
    globalSetup: ['src/test-utils/global-setup.ts'],
    globalTeardown: ['src/test-utils/global-teardown.ts'],
    testTimeout: 30_000,
    hookTimeout: 60_000,
    pool: 'threads',
    isolate: false,
    sequence: { concurrent: false },
    reporters: ['default'],
  },
});
```

NOTE: `globalSetup` / `globalTeardown` files do not exist yet — Plan 02 creates them. Until Plan 02 lands, `test:integration` will fail on missing globalSetup. This is expected and wave-gated. Plan 01's acceptance for `test:integration` is "exits 0 when no integration tests discovered AND no globalSetup required yet" — to satisfy both, add temporary `src/test-utils/global-setup.ts` and `global-teardown.ts` stubs that are empty async functions exporting default (minimal; replaced in Plan 02):

Create in Task 2 as temporary stubs (deleted/replaced in Plan 02 Task 1):
- `packages/server/src/test-utils/global-setup.ts`: `export default async function () {}`
- `packages/server/src/test-utils/global-teardown.ts`: `export default async function () {}`

**`packages/server/.env.example`** — documented env vars (RESEARCH §@fastify/env JSON schema):
```
# Required
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/xci_dev
SESSION_COOKIE_SECRET=change-me-to-at-least-32-bytes-of-random-entropy-please
EMAIL_TRANSPORT=log

# Optional
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# Required only when EMAIL_TRANSPORT=smtp
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=xci@example.com
```

**`biome.json`** — THREE edits (07-PATTERNS.md §Warning 8 + §Shared Patterns):

1. Widen `files.includes` to cover new server config files:
```json
"includes": [
  "packages/**/src/**/*.ts",
  "packages/**/tsup.config.ts",
  "packages/**/vitest*.config.ts",
  "packages/**/drizzle.config.ts"
]
```

2. Add a second override block (append to existing `overrides` array, after the xci block at lines 46-68) scoped to `packages/server/src/**`. Per CONTEXT D-01 + 07-PATTERNS.md §Shared Patterns, block direct imports of sibling repo files — only `./repos/index.js` (and same-folder internal imports inside `src/repos/`) are allowed. Use `noRestrictedImports` with `patterns` matching sibling repo imports:

```json
{
  "includes": ["packages/server/src/**/*.ts"],
  "linter": {
    "rules": {
      "style": {
        "noRestrictedImports": {
          "level": "error",
          "options": {
            "paths": {
              "ws": {
                "message": "Phase 7 does not use WebSockets. Agent WS arrives in Phase 8."
              },
              "reconnecting-websocket": {
                "message": "Phase 7 does not use reconnecting-websocket. Agent reconnect arrives in Phase 8."
              }
            }
          }
        }
      }
    }
  }
}
```

3. Add a THIRD narrower override for routes/plugins/app/server that explicitly blocks repo file siblings. Appended after the general server block:
```json
{
  "includes": [
    "packages/server/src/routes/**/*.ts",
    "packages/server/src/plugins/**/*.ts",
    "packages/server/src/app.ts",
    "packages/server/src/server.ts"
  ],
  "linter": {
    "rules": {
      "style": {
        "noRestrictedImports": {
          "level": "error",
          "options": {
            "paths": {
              "./repos/users.js": { "message": "D-01: import { forOrg, admin } from '../repos/index.js' — never import a specific repo file. See 07-CONTEXT.md D-01." },
              "./repos/sessions.js": { "message": "D-01: use forOrg()/adminRepo from ../repos/index.js" },
              "./repos/email-verifications.js": { "message": "D-01: use forOrg()/adminRepo from ../repos/index.js" },
              "./repos/password-resets.js": { "message": "D-01: use forOrg()/adminRepo from ../repos/index.js" },
              "./repos/org-invites.js": { "message": "D-01: use forOrg()/adminRepo from ../repos/index.js" },
              "./repos/org-plans.js": { "message": "D-01: use forOrg()/adminRepo from ../repos/index.js" },
              "./repos/for-org.js": { "message": "D-01: internal; use makeRepos() from ../repos/index.js" },
              "../repos/users.js": { "message": "D-01: use forOrg()/adminRepo from ../repos/index.js" },
              "../repos/sessions.js": { "message": "D-01: use forOrg()/adminRepo from ../repos/index.js" },
              "../repos/email-verifications.js": { "message": "D-01: use forOrg()/adminRepo from ../repos/index.js" },
              "../repos/password-resets.js": { "message": "D-01: use forOrg()/adminRepo from ../repos/index.js" },
              "../repos/org-invites.js": { "message": "D-01: use forOrg()/adminRepo from ../repos/index.js" },
              "../repos/org-plans.js": { "message": "D-01: use forOrg()/adminRepo from ../repos/index.js" },
              "../repos/for-org.js": { "message": "D-01: internal; use makeRepos() from ../repos/index.js" }
            }
          }
        }
      }
    }
  }
}
```

Rationale: Biome's `noRestrictedImports.paths` expects exact string match; we enumerate both same-folder (`./repos/x.js`) and parent-folder (`../repos/x.js`) imports. This is the planner's choice per RESEARCH Open Question 1 (fallback to custom script was available; enumeration works here because the repo file set is finite and locked).

Replace the existing `overrides` array in biome.json with the three blocks (preserve the existing xci block, then append server-general block, then append server-routes/plugins/app block).

Per 07-PATTERNS.md §Warning 9, do NOT copy any xci ws-fence grep to the server — D-39 means ws-fence stays on packages/xci/ only.

Do NOT modify packages/xci/biome overrides.
  </action>
  <verify>
    <automated>test -f packages/server/tsconfig.json && grep -q '"outDir": "dist"' packages/server/tsconfig.json && test -f packages/server/drizzle.config.ts && test -f packages/server/vitest.unit.config.ts && test -f packages/server/vitest.integration.config.ts && test -f packages/server/.env.example && grep -q 'DATABASE_URL=' packages/server/.env.example && grep -q 'SESSION_COOKIE_SECRET=' packages/server/.env.example && grep -q 'packages/server/src/\*\*/\*.ts' biome.json && grep -q 'packages/\*\*/vitest\*.config.ts' biome.json && grep -q 'packages/\*\*/drizzle.config.ts' biome.json && test -f packages/server/src/test-utils/global-setup.ts && test -f packages/server/src/test-utils/global-teardown.ts && pnpm --filter xci test 2>&1 | tail -5 | grep -E "passed|✓"</automated>
  </verify>
  <done>All five server config files exist with correct shapes. biome.json has THREE overrides: existing xci, new server-general (ws block), new server-routes-plugins-app (repo import block). biome.json files.includes widened for vitest*.config.ts + drizzle.config.ts. Temporary empty globalSetup/globalTeardown stubs exist. v1 test suite (`pnpm --filter xci test`) still passes — D-39 fence respected.</done>
</task>

<task type="auto">
  <name>Task 3: Replace packages/server/src/index.ts stub, add first changeset, smoke-verify toolchain wiring</name>
  <files>packages/server/src/index.ts, .changeset/07-server-bootstrap.md, packages/server/src/test-utils/global-setup.ts, packages/server/src/test-utils/global-teardown.ts</files>
  <read_first>
    - packages/server/src/index.ts (current Phase 6 stub: `export {};`)
    - .changeset/config.json (fixed versioning: [["xci", "@xci/server", "@xci/web"]])
    - .planning/phases/07-database-schema-auth/07-RESEARCH.md §Sequencing Wave 0.8 (first changeset)
    - .planning/phases/06-monorepo-setup-backward-compat-fence/06-CONTEXT.md D-11 (fixed versioning), D-12 (private flip)
    - .planning/phases/07-database-schema-auth/07-CONTEXT.md D-12 (private flip), D-39 (fence)
  </read_first>
  <action>
Three files + one toolchain smoke verification.

**`packages/server/src/index.ts`** — replace `export {};` with placeholder barrel (real exports land in subsequent plans):
```typescript
// @xci/server — Phase 7+ server package.
// This barrel will re-export buildApp, errors, and select repos in future waves.
// Phase 7 Plan 01: package skeleton bootstrap — no runtime API yet.
export {};
```

**`.changeset/07-server-bootstrap.md`** — first @xci/server changeset (minor feat per fixed-versioning D-11; xci also bumps because it's in the fixed group):
```markdown
---
"@xci/server": minor
"xci": minor
"@xci/web": minor
---

Phase 7: Bootstrap @xci/server package with database schema, authentication, sessions, and multi-tenant isolation. This is the first real code in @xci/server — previously a Phase 6 placeholder.

- Drizzle ORM schema for orgs, users, org_members, org_plans, sessions, email_verifications, password_resets, org_invites
- Argon2id password hashing (@node-rs/argon2)
- Session cookie (httpOnly+secure+sameSite=strict) with sliding 14d expiry, absolute 30d cap
- Email verification, password reset, org invite flows
- Multi-tenant isolation via scoped repository wrapper (forOrg) + two-org integration fixture
- Free org plan entity (max_agents=5, max_concurrent_tasks=5, log_retention_days=30) — enforcement in Phase 10
```

**Temporary globalSetup/globalTeardown stubs** (Plan 02 will overwrite with real testcontainers wiring):
```typescript
// packages/server/src/test-utils/global-setup.ts
// TEMPORARY STUB — Plan 02 replaces this with testcontainers Postgres startup.
export default async function (): Promise<void> {}
```
```typescript
// packages/server/src/test-utils/global-teardown.ts
// TEMPORARY STUB — Plan 02 replaces this with container teardown.
export default async function (): Promise<void> {}
```

Then run this verification sequence from repo root and ensure all exit 0:

```bash
# 1. Typecheck the server (empty src tree but tsconfig valid)
pnpm --filter @xci/server typecheck

# 2. Build (empty src tree emits empty dist/ — must succeed)
pnpm --filter @xci/server build

# 3. Lint with new biome overrides
pnpm --filter @xci/server lint

# 4. Unit vitest run (zero tests discovered, vitest returns 0)
pnpm --filter @xci/server test:unit

# 5. Integration vitest run (zero tests + empty globalSetup) — MUST exit 0
pnpm --filter @xci/server test:integration

# 6. v1 xci fence check — D-39 protection
pnpm --filter xci typecheck && pnpm --filter xci test && pnpm --filter xci build

# 7. Turbo end-to-end from root
pnpm turbo run typecheck lint build
```

If any command fails, diagnose and fix before marking done. Common issues:
- `tsc -b` may complain about empty rootDir — if so, index.ts stub ensures at least one file exists.
- `vitest --config vitest.integration.config.ts` may error without globalSetup — the empty stubs prevent this.
- Biome may flag `types.ts` or other missing files — if lint passes with empty src, proceed.

Check that the first @xci/server changeset is picked up:
```bash
pnpm changeset status 2>&1 | grep -E "(@xci/server|xci|@xci/web)"
```
Expect all three to show pending version bumps at minor level.
  </action>
  <verify>
    <automated>test -f packages/server/src/index.ts && grep -q "Phase 7" packages/server/src/index.ts && test -f .changeset/07-server-bootstrap.md && grep -q '"@xci/server": minor' .changeset/07-server-bootstrap.md && test -f packages/server/src/test-utils/global-setup.ts && test -f packages/server/src/test-utils/global-teardown.ts && pnpm --filter @xci/server typecheck && pnpm --filter @xci/server build && pnpm --filter @xci/server lint && pnpm --filter @xci/server test:unit && pnpm --filter @xci/server test:integration && pnpm --filter xci typecheck && pnpm --filter xci test 2>&1 | tail -10 | grep -qE "(passed|✓)"</automated>
  </verify>
  <done>packages/server/src/index.ts is a valid stub. First @xci/server changeset exists at .changeset/07-server-bootstrap.md with all three packages bumping minor. Temporary globalSetup/globalTeardown stubs exist. All 7 toolchain commands exit 0 including turbo end-to-end. v1 xci test suite passes (D-39 fence respected).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local dev | Dependencies downloaded from npm — supply-chain trust |
| Repository → CI | biome.json overrides + pnpm-lock.yaml + package.json changes shape toolchain for all subsequent work |
| Dev machine → production | .env.example documents real env var names that will hold production secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | pnpm-lock.yaml + package.json exact versions | mitigate | All 12 runtime deps + 5 dev deps pinned at exact versions (not `^` or `~`); pnpm-lock.yaml tracks transitive integrity hashes; CI uses `pnpm install --frozen-lockfile` (from existing ci.yml line 37) |
| T-07-01-02 | Information disclosure | packages/server/.env.example accidentally committed with real secrets | accept | Example file contains placeholder strings only (`change-me-to-at-least-32-bytes...`); never populated with real values. Phase 9 CFG-09 gitignore warning pattern (from v1) applies to `.env` (not `.env.example`) — we'll reuse the pattern in Phase 9 when secrets entity lands. |
| T-07-01-03 | Elevation of Privilege | Biome repo-import override syntax insufficient (RESEARCH Assumption A8) | mitigate | Enumerated repo file list (6 files × 2 path prefixes = 12 entries) in biome.json override; tested by Task 3 lint run; if pattern matching proves insufficient later plans (04+) add a scripts/check-repo-imports.mjs fallback. |
| T-07-01-04 | Denial of service | Phase 6 fence broken by new server package config (v1 tests regress) | mitigate | Task 3 explicitly runs `pnpm --filter xci test && pnpm --filter xci build` as verification; D-39 in CONTEXT is a hard rule; biome.json changes only ADD overrides, don't modify the existing xci override. |
| T-07-01-05 | Spoofing | .changeset/07-server-bootstrap.md bumps xci + @xci/web minor unexpectedly | accept | Fixed versioning (Phase 6 D-11) intentional; narrative for users is "all three packages move in lockstep". All three show as minor in `pnpm changeset status`. Released together in Phase 14. |

Severity: all low/medium. No high-severity threats — this plan is toolchain bootstrap, no security-critical code yet.
</threat_model>

<verification>
**Plan-level acceptance (all must be true before Plan 02 starts):**

```bash
# 1. Real manifest + deps installed
pnpm --filter @xci/server install --frozen-lockfile 2>&1 | grep -qi "error" && echo FAIL || echo OK

# 2. All 5 config files present
for f in tsconfig.json drizzle.config.ts vitest.unit.config.ts vitest.integration.config.ts .env.example; do
  test -f packages/server/$f || { echo "MISSING: $f"; exit 1; }
done

# 3. biome.json widened + both new overrides present
grep -q 'packages/server/src/\*\*/\*.ts' biome.json || { echo "biome override missing"; exit 1; }
grep -q 'packages/\*\*/drizzle.config.ts' biome.json || { echo "biome files.includes missing drizzle"; exit 1; }

# 4. Toolchain smoke
pnpm --filter @xci/server typecheck
pnpm --filter @xci/server build
pnpm --filter @xci/server lint
pnpm --filter @xci/server test:unit
pnpm --filter @xci/server test:integration

# 5. D-39 fence intact
pnpm --filter xci test
pnpm --filter xci build
pnpm turbo run build

# 6. Changeset recognized
pnpm changeset status 2>&1 | grep -q "@xci/server"
```
</verification>

<success_criteria>
- `packages/server/package.json` has `private: false`, `name: "@xci/server"`, scripts for `build|test|test:unit|test:integration|typecheck|lint|db:generate|dev`, and all 12 runtime + 5 dev dependencies at exact versions from RESEARCH §Installation.
- `packages/server/tsconfig.json` extends base and overrides `noEmit: false` with `outDir: dist` + `rootDir: src`.
- `packages/server/drizzle.config.ts` points at `./src/db/schema.ts` → `./drizzle` with postgresql dialect.
- `packages/server/vitest.unit.config.ts` uses `isolate: true`, excludes `*.integration.test.ts` and `*.isolation.test.ts`.
- `packages/server/vitest.integration.config.ts` uses `isolate: false`, `sequence.concurrent: false`, `globalSetup`/`globalTeardown` paths, 30s timeout.
- `packages/server/.env.example` documents DATABASE_URL, SESSION_COOKIE_SECRET, EMAIL_TRANSPORT, PORT, SMTP_*.
- `biome.json` files.includes covers `packages/**/vitest*.config.ts` + `packages/**/drizzle.config.ts`.
- `biome.json` has a new `packages/server/src/**` override blocking ws/reconnecting-websocket imports.
- `biome.json` has a third override blocking direct repo-file imports from routes/plugins/app (D-01 enforcement).
- `.changeset/07-server-bootstrap.md` exists bumping @xci/server + xci + @xci/web at minor.
- `pnpm --filter @xci/server {typecheck|build|lint|test:unit|test:integration}` all exit 0.
- `pnpm --filter xci {typecheck|test|build}` all exit 0 (D-39 fence respected).
- `pnpm turbo run typecheck lint build` exits 0 end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/07-database-schema-auth/07-01-SUMMARY.md` documenting: final package.json shape, exact dep versions installed (capture `pnpm list --filter @xci/server`), biome override structure, any deviations from plan, and the 7-command toolchain smoke output confirming all exit 0.
</output>
