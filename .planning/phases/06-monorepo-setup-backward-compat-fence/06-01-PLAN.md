---
phase: 06-monorepo-setup-backward-compat-fence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-monorepo-setup-backward-compat-fence/06-01-NPM-SCOPE-VERIFY.md
autonomous: false
requirements: [PKG-01, PKG-03]
must_haves:
  truths:
    - "The npm scope @xci is verified as available (404 on both @xci/server and @xci/web) before any package.json uses the @xci/* name"
    - "If the scope is taken, execution halts with a clear blocker for the user to resolve (fallback name decision)"
  artifacts:
    - path: ".planning/phases/06-monorepo-setup-backward-compat-fence/06-01-NPM-SCOPE-VERIFY.md"
      provides: "Record of npm view results for @xci/server and @xci/web"
      contains: "npm view @xci/server"
  key_links:
    - from: "blocking pre-flight check"
      to: "subsequent plans 02-06"
      via: "verified @xci scope availability"
      pattern: "PROCEED / HALT gate"
---

<objective>
Pre-flight blocking check (per D-14): verify the npm scope `@xci` is available for `@xci/server` and `@xci/web` BEFORE any plan commits those package names to the codebase. If either name is taken, halt with a clear error and escalate to the user for a fallback name decision (e.g., `@xcihq`, `@xci-io`).

Purpose: D-14 mandates this as a blocking pre-flight. Committing `@xci/server` and `@xci/web` to package.json without verifying availability risks rework if the scope is taken. This plan is cheap (two npm-view calls) and de-risks the entire phase.

Output: A short markdown record of the two `npm view` results, a decision note ("SCOPE AVAILABLE — proceed to Plan 02" or "SCOPE TAKEN — user must decide fallback before proceeding"), and (if taken) a checkpoint for user input.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-monorepo-setup-backward-compat-fence/06-CONTEXT.md
@.planning/phases/06-monorepo-setup-backward-compat-fence/06-RESEARCH.md

<!-- D-14 (CONTEXT.md): "npm scope @xci — availability NOT yet verified. Phase 6 must include an early task: npm view @xci/server + npm view @xci/web (expect 404 = available). If taken, fallback to @xcihq or similar — treat as a blocking pre-flight." -->
<!-- RESEARCH.md §Environment Availability confirms: no external pre-check has been done yet. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Query npm registry for @xci/server and @xci/web availability</name>
  <files>.planning/phases/06-monorepo-setup-backward-compat-fence/06-01-NPM-SCOPE-VERIFY.md</files>
  <read_first>
    - .planning/phases/06-monorepo-setup-backward-compat-fence/06-CONTEXT.md §decisions D-14 (lines 48 of CONTEXT.md — the exact blocking-pre-flight requirement)
    - .planning/phases/06-monorepo-setup-backward-compat-fence/06-RESEARCH.md §Environment Availability (the "NPM_TOKEN" and registry availability tables)
  </read_first>
  <action>
Run the following two commands and capture their raw output verbatim (exit code, stdout, stderr) into the record file:

```bash
npm view @xci/server 2>&1; echo "EXIT=$?"
npm view @xci/web 2>&1; echo "EXIT=$?"
```

Expected result: both commands exit non-zero with "npm error 404 Not Found" or "npm error code E404" — this means the scope is AVAILABLE.

Then create `.planning/phases/06-monorepo-setup-backward-compat-fence/06-01-NPM-SCOPE-VERIFY.md` with EXACTLY this structure (substitute real output where marked):

```markdown
# Phase 6 Plan 01 — npm scope @xci verification (per D-14)

**Executed:** <YYYY-MM-DD>
**Decision:** <AVAILABLE | TAKEN>

## Command: `npm view @xci/server`

```
<paste raw stdout + stderr verbatim>
EXIT=<N>
```

## Command: `npm view @xci/web`

```
<paste raw stdout + stderr verbatim>
EXIT=<N>
```

## Interpretation

- Exit code non-zero + "404" / "E404" in stderr → name is AVAILABLE (we can publish under this name).
- Exit code 0 + JSON manifest on stdout → name is TAKEN (someone else owns it).

## Decision

- [ ] AVAILABLE: both commands returned 404/E404. Proceed to Plan 02 unchanged — `@xci/server` and `@xci/web` are the confirmed package names.
- [ ] TAKEN: at least one command returned a manifest. HALT Phase 6 execution. Escalate to user via `checkpoint:decision` in next task to choose a fallback scope (e.g. `@xcihq/server` + `@xcihq/web`, or `@xci-io/server` + `@xci-io/web`). Plans 02-06 must be updated to the chosen scope before execution.

## Result summary (fill one)

- [x] Both names 404 — PROCEED (default expected outcome)
- [ ] `@xci/server` TAKEN — HALT
- [ ] `@xci/web` TAKEN — HALT
- [ ] Both TAKEN — HALT
```

Do NOT modify any package.json files in this plan. Do NOT create any @xci/* manifests. This is a read-only pre-flight.

If either query returns a real manifest (scope TAKEN), STOP immediately after writing the record file — the next task (Task 2 checkpoint) will gate the decision. Do NOT proceed to Plan 02.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-monorepo-setup-backward-compat-fence/06-01-NPM-SCOPE-VERIFY.md && grep -qE "npm view @xci/server" .planning/phases/06-monorepo-setup-backward-compat-fence/06-01-NPM-SCOPE-VERIFY.md && grep -qE "npm view @xci/web" .planning/phases/06-monorepo-setup-backward-compat-fence/06-01-NPM-SCOPE-VERIFY.md && grep -qE "EXIT=" .planning/phases/06-monorepo-setup-backward-compat-fence/06-01-NPM-SCOPE-VERIFY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/06-monorepo-setup-backward-compat-fence/06-01-NPM-SCOPE-VERIFY.md` exists
    - File contains the literal string `npm view @xci/server`
    - File contains the literal string `npm view @xci/web`
    - File contains at least two `EXIT=` markers with numeric exit codes
    - File contains a "Decision" section with one of the four checklist boxes checked
    - No package.json file under `packages/` has been created yet in this plan (verify: `ls packages/ 2>/dev/null | wc -l` returns 0)
  </acceptance_criteria>
  <done>
The verification record exists on disk with real npm-view output for both names and a locked decision (AVAILABLE vs TAKEN). No other files have been created or modified.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm scope decision before Plan 02 executes</name>
  <files>.planning/phases/06-monorepo-setup-backward-compat-fence/06-01-NPM-SCOPE-VERIFY.md</files>
  <what-built>
A pre-flight record file at `.planning/phases/06-monorepo-setup-backward-compat-fence/06-01-NPM-SCOPE-VERIFY.md` documenting the npm-view results for `@xci/server` and `@xci/web`.
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/06-monorepo-setup-backward-compat-fence/06-01-NPM-SCOPE-VERIFY.md`
2. Read the two "Command:" blocks — confirm both show non-zero EXIT and a 404/E404 message (expected: scope available)
3. Read the "Decision" section — confirm ONE checkbox is marked and it matches your reading of the output
4. If Decision = AVAILABLE: type "approved" to proceed to Plan 02 as planned
5. If Decision = TAKEN: describe the fallback scope you want to use (e.g., `@xcihq`, `@xci-io`) and which specific packages are taken. Plans 02-06 will be updated to the new scope before continuing
  </how-to-verify>
  <action>Checkpoint task — pause execution and wait for user approval. Claude should present the contents of 06-01-NPM-SCOPE-VERIFY.md to the user and wait for the resume-signal. If user signals "approved", proceed to Plan 02. If user describes a fallback scope, STOP and escalate: Plans 02-06 must be re-planned with the new scope before execution can resume.</action>
  <verify>
    <automated>test -f .planning/phases/06-monorepo-setup-backward-compat-fence/06-01-NPM-SCOPE-VERIFY.md</automated>
  </verify>
  <done>User has provided the resume-signal. If "approved": Phase 6 can proceed to Plan 02. If a fallback scope was specified: Phase 6 halts here and the fallback scope is documented in 06-01-SUMMARY.md.</done>
  <resume-signal>Type "approved" (scope available, proceed unchanged) OR describe the fallback scope if any name is taken</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local machine → npm registry (registry.npmjs.org) | untrusted response from public registry crosses the HTTPS boundary into our decision process |
| planning artifact → subsequent plan execution | the decision recorded in this plan gates all subsequent Phase 6 plans that commit @xci/* names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | `npm view` response from registry.npmjs.org | accept | npm CLI uses TLS + integrity checks against registry.npmjs.org. Low-value target (read-only existence check). No sensitive data transmitted. |
| T-06-02 | Tampering | 06-01-NPM-SCOPE-VERIFY.md planning artifact | mitigate | File is committed to git immediately; any tampering is visible in git history. Downstream plans will fail at Plan 02 if the decision is not recorded honestly. |
| T-06-03 | Information Disclosure | npm-view output | accept | Query is for public package existence; no credentials, no private data. Output safe to commit. |
| T-06-04 | Repudiation | "decision already confirmed" claim without actual verification | mitigate | Checkpoint task 2 is a `checkpoint:human-verify` gate — user must explicitly approve the recorded decision before Plan 02 runs. The record file preserves the raw EXIT codes as evidence. |

</threat_model>

<verification>
- `npm view @xci/server` and `npm view @xci/web` outputs are captured verbatim in `06-01-NPM-SCOPE-VERIFY.md`
- A decision is explicitly recorded (AVAILABLE or TAKEN with fallback)
- User has approved the decision via checkpoint before downstream plans execute
- No `packages/` directory tree has been created yet (the user may still need to redirect the scope)
</verification>

<success_criteria>
- `.planning/phases/06-monorepo-setup-backward-compat-fence/06-01-NPM-SCOPE-VERIFY.md` exists and contains raw outputs from both `npm view` commands
- The file contains a locked decision (one of four checkboxes marked)
- User approval has been recorded via the checkpoint resume-signal
- If AVAILABLE: downstream plans proceed with `@xci/server` and `@xci/web` unchanged
- If TAKEN: Phase 6 halts; downstream plans must be updated to the user-chosen fallback scope before proceeding
</success_criteria>

<output>
After completion, create `.planning/phases/06-monorepo-setup-backward-compat-fence/06-01-SUMMARY.md` with:
- Decision: AVAILABLE or TAKEN
- Actual EXIT codes observed
- Confirmation that no package.json files have been created yet
- Go/no-go for Plan 02
</output>
